# CHANGELOG 归档 · 每日韩语 (daily-hangul)

> 以下为旧格式的历史记录，已从 CHANGELOG.md 归档至此。
> 新条目请按 CHANGELOG_RULES.md 规范写入 CHANGELOG.md。

---

## 📅 2026-07-24 18:30

### 📝 一句话总结

项目规范化建设：创建 CLAUDE.md 开发指南 + 完成首次全项目产品评审（零代码修改）。

---

### 🎯 本次更新目标

本次没有修改任何代码。目标是为项目建立两样长期基础设施：

1. **CLAUDE.md** —— 让 Claude Code（以及未来的 AI 协作伙伴）在每次开发前都能理解项目的产品理念、设计原则和开发规范，而不是从零开始猜测。
2. **首次全项目产品评审** —— 从产品经理、UI/UX 设计师、前端工程师三个角度，对当前 5347 行单文件应用进行一次完整审视，为后续迭代提供优先级参考。

为什么现在做？

- 项目已经从"简单的单词网页"成长为一个功能丰富的学习工具，但代码和产品层面都开始出现早期信号：功能堆积、路径混乱、代码重复、维护成本攀升。
- 在继续加功能之前，先停下来看清楚现状，比盲目向前更重要。

---

### ✨ 本次更新

#### 新增文件

- **CLAUDE.md**（项目根目录）
  - 包含完整的项目介绍、产品目标、开发理念
  - 定义了 5 条产品设计原则：学习优先 / 做减法 / 一个页面一个任务 / 信息层级清晰 / 降低思考成本
  - 明确了 UI 风格方向（韩系、简洁、留白、圆角、克制）
  - 制定了开发流程规范（先读 CLAUDE.md → 分析 → 给出方案 → 确认 → 小步修改 → 测试）
  - 建立了产品决策原则（允许质疑需求、多方案对比、冲突优先指出）
  - 规定了每次开发结束后的产品 Review 流程

#### 产品评审

完成了一次深度项目评审，核心发现：

| 视角 | 最重要的问题 |
|------|------------|
| 产品经理 | 首页功能堆积（6个入口+10课目录）、复习/测验概念重叠 |
| UI/UX | **SRS评分按钮在卡片正面（翻面前即可评分，违背闪卡学习原则）** |
| 前端 | 词卡渲染代码重复4次、单文件5347行巨石架构、ES5/ES6混用 |

评审报告详细记录了每个问题的分析、影响评估和优化优先级（P0-P3），可作为后续迭代路线图参考。

---

### 👤 用户体验变化

本次未修改代码，用户体验无直接变化。

但本次评审发现的核心 UX 问题——**SRS 评分按钮在卡片正面**——影响每一位用户每一次学习：

- 修改前：用户看到韩语单词正面，还没翻面看答案，就已经能看到"忘记了/困难/一般/简单"四个评分按钮
- 这违背了闪卡学习的核心原则：先回忆 → 再验证 → 最后评分
- 该问题被标记为 **P0**，建议最优先修复

---

### 💭 设计思考

**为什么 CLAUDE.md 这么详细？**

一个长期维护的个人项目，最大的风险不是技术债，而是"开发者本人忘记当初为什么这样设计"。CLAUDE.md 的作用不仅是给 AI 看，也是给未来的自己看——当项目发展到第 6 个月、第 12 个月时，这份文档能让每一次决策都保持与最初产品理念的一致。

**为什么评审不做代码修改？**

CLAUDE.md 中明确规定了开发流程：先分析 → 给出方案 → 等待确认 → 再修改。本次严格遵守了这一流程。评审中发现的 10+ 个问题，每一个都需要用户确认优先级后再动手。

**遵循了哪些设计原则？**

- 做减法：评审不是为了找到"可以加什么"，而是找到"可以删什么、合并什么"
- 降低思考成本：评审重点之一是用户在首页面临的决策成本
- 学习优先：最严重的问题（评分按钮位置）直接关系到学习效果

---

### 📌 下一步建议

基于本次评审，建议按以下优先级推进：

1. **P0 · 修复 SRS 评分按钮位置**：将按钮从 `.card-front` 移到 `.card-back`，翻面后才显示。改动量小（~30行），影响最大。
2. **P1 · 精简首页**：合并快捷入口、减少课程目录展示数量，让用户打开后能更快开始学习。
3. **P1 · 抽取公共词卡渲染函数**：将 `openLesson` / `showStarred` / `renderWeakWords` / `doSearch` 中重复的卡片 HTML 生成逻辑抽取为一个 `renderWordCard()` 函数。
4. **P2 · 合并复习/测验入口**：减少用户在"该用哪个"上的决策成本，将测验作为闪卡学习后的可选环节。
5. **P2 · 渐进式拆分单文件**：先拆数据层 → 再拆 CSS → 再拆工具函数，降低每次修改的风险。

---

## 📅 2026-07-24 单词卡片 Grid 对齐 + UI 全局紧凑化 + 测验记录独立标签 + Bug 修复

### 1. 📝 改动概述 (Overview)

- **涉及文件：** `每日韩语-源码.html`、`daily-hangul-勿删-部署用/index.html`、`CHANGELOG.md`
- **主要目的：** 单词卡片 Grid 三行对齐（四页统一）、全局 UI 手机适配缩小、测验页双标签（测验/记录）拆分、修复易错本已掌握 Bug、测验按钮静默销毁进度、收藏页事件委托失效等 9 个严重/中等问题
- **部署状态：** ✅ 已部署 `https://rrbiit.github.io/daily-hangul/`

### 2. 🚀 新增功能与 UI 效果 (Added Features & UI)

- **单词卡片 Grid 三行左右对齐（全新布局）**
  - `.word-item` 从 flex row 改为 `display: grid; grid-template-columns: 1fr auto; grid-template-rows: auto auto auto`
  - `left-col` 和 `right-col` 改为 `display: contents`，子元素直接参与网格
  - 三行严格对齐：第 1 行 韩语 ‖ 星级 / 第 2 行 词性标签 ‖ 🔊喇叭+掌握徽章 / 第 3 行 中文含义 ‖ [▶ 学习]按钮
  - 四页统一生效：单词列表、易错本、收藏、搜索
  - `pos` 词性标签去掉外层 `<div>` 包裹层，改为直接子元素参与 grid

- **点击空白处播放读音（交互变更）**
  - 单词卡片空白处点击：原来的"展开例句"改为**播放读音** + 蓝色闪烁动画（`.word-item.speaking .kr` 350ms）
  - 🔊 喇叭按钮保留，两种方式都能触发发音
  - 例句不再在列表中展开，统一在学习卡片内查看
  - 学习按钮 `[▶ 学习]` 放在每个卡片右下角，点击直接进入翻卡模式

- **测验记录独立标签**
  - 测验页顶部新增双标签：`[📝 测验] [📋 记录]`
  - 点"记录"直接查看历史，不再和测验设置混在一起滚到底部
  - 测验答题中自动隐藏标签栏，回设置页自动恢复
  - `switchQuizTab(tab)` 函数管理标签切换逻辑

- **测验记录增强**
  - 每条测验历史保存错词详情（韩语+中文+词性+课程）
  - 历史列表倒序显示，包含得分圆点 🟢🟡🔴、模式、日期、薄弱词列表
  - `renderQuizHistory()` 渲染历史列表，无记录时显示空状态

- **易错本简化**
  - 标记已掌握 → `badCount` 归零 → 词从易错本消失（`isWeak` 要求 lv<4 且 badCount≥1）
  - 删除过滤栏 HTML / `setWeakFilter` / `weakFilter` / `wasEverWeak`，易错本只保留未掌握薄弱词

- **视觉与样式更新（全局紧凑化）：**
  - 首页 Hero 卡片：padding 18×20→14×16，emoji 30→26px，问候语 22→20px
  - 统计卡片：padding 16×10→12×6，图标 20→16px，数字 20→18px
  - 进度条：padding 14×18→10×14，高度 8→6px，字号 13/18→12/16px
  - 快捷入口：padding 16×14→12×12，图标 40→36px，标题 14→13px
  - 课程列表 `.lesson-item`：padding 14×18→10×14，标题 15→14px，进度条 60→50px
  - 首页课程 `.course-item`：padding 16×20→10×14，标题 16→14px，副标题 14→12px，进度条宽 80→60px，新增 max-width 460px
  - 测验/复习框 `.review-option`：padding 16×18→10×14，图标 24→20px，标题 16→14px，词数 18→16px，新增 max-width 460px
  - 全局 tab-bar：gap 4→2px，padding 4→3px，按钮 8→6px，字号 14→12px
  - 测验模式标签：`🇰🇷→🇨🇳 韩→中` → `韩→中`（去冗余图标和重复文字）
  - 测验范围按钮：字号 13→12px，padding 6→5px
  - 测验开始按钮：padding 14→12px，字号 17→15px
  - 容器上内边距：48→32px
  - 页面标题：22→20px，副标题 14→13px
  - 页头下边距：32→20px

### 3. 🛠️ 核心代码改动 (Key Code Changes)

**Grid 三行对齐 CSS：**

```css
.word-item {
  display: grid;
  grid-template-columns: 1fr auto;
  grid-template-rows: auto auto auto;
  gap: 2px 8px;
  align-items: center;
}
.word-item .left-col,
.word-item .right-col { display: contents; }
.word-item .kr                 { grid-row: 1; grid-column: 1; }
.word-item .word-stars-clickable { grid-row: 1; grid-column: 2; justify-self: end; }
.word-item .pos                { grid-row: 2; grid-column: 1; justify-self: start; }
.word-item .word-actions-under-stars { grid-row: 2; grid-column: 2; justify-self: end; }
.word-item .cn                 { grid-row: 3; grid-column: 1; }
.word-item .word-learn-btn     { grid-row: 3; grid-column: 2; justify-self: end; }
```

### 4. ⚠️ 遇到的问题与报错 (Issues & Challenges)

1. **易错本"已掌握"永远为空** - 标记掌握时 `badCount` 被丢弃，解决：标记掌握 → badCount 归零，词直接从易错本消失
2. **掌握徽章在收藏/搜索页点击会跳转到单词列表页** - 事件委托分支不完整，解决：增加各页面刷新分支
3. **测验进行中点底部"测验"按钮静默销毁进度** - 导航栏测验按钮绕过了拦截器，解决：新增 `quizNavGuard()`
4. **收藏页取消收藏后卡片不消失** - 事件委托只刷新星星 HTML，解决：检测收藏页取消 → 从 DOM 移除
5. **测验页脚和底部导航栏重叠** - 解决：`showPage` 中测验页也隐藏底部导航
6. **CSS display: contents 导致 pos 无法定位** - `pos` 外多一层 `<div>`，解决：四个渲染函数去包装 div

### 5. 💡 解决方案与避坑经验 (Solutions & Takeaways)

- **display: contents 是 double-edged sword：** 让子元素直接参与 grid 时非常干净，但要求 DOM 层级完全扁平
- **修改全局样式前先 grep 所有引用：** `.word-item` 被四个不同页面的渲染函数使用
- **测验按钮不能绕开页面切换拦截器：** 底部导航中任何可能产生数据丢失的按钮都应该走 `showPage` 或自行做确认检测
- **事件委托按页面分流刷新时，要覆盖所有可能页面**
- **UI 压缩要照顾手机触控：** 触摸目标必须保持 44px 级可点击
- **测验历史数据结构要考虑向后兼容：** 旧数据没有 `errors` 字段，渲染时需安全检查

---

## 📅 2026-07-24 测验模式上线 + UI 全面修复

### 1. 📝 改动概述 (Overview)

- **涉及文件：** `每日韩语-源码.html`、`daily-hangul-勿删-部署用/index.html`、`daily-hangul-勿删-部署用/sw.js`、`每日韩语-产品复盘.md`、`每日韩语-项目说明.docx`
- **主要目的：** 新增测验模式（三种题型 + 单元自选 + 错题复习）、修复底部栏透明穿透、喇叭与掌握框横排布局、hash 路由实现手机返回键与刷新恢复、修复多处透明与 CSS 优先级问题、底部导航重构
- **部署状态：** ✅ 已部署 `https://rrbiit.github.io/daily-hangul/`

### 2. 🚀 新增功能与 UI 效果 (Added Features & UI)

- **测验模式（全新模块）**
  - 三种测验类型：韩→中四选一 / 中→韩四选一 / 听音选义四选一
  - 范围选择：全部 / 未掌握 / 收藏 / 薄弱词，四按钮筛选
  - 单元自选：课程列表复选框，和复习模式一致的交互，可勾选任意单元
  - 题量可选：5 / 10 / 15 / 20 题，顶部 tab 切换
  - 答对 0.8 秒自动跳到下一题；答错停在当前题
  - 错题自动加入易错本（badCount 累加），结果页可一键跳转闪卡复习错题
  - 成绩历史记录，按模式分类显示分数和正确率

- **hash 路由 + 手机返回键**
  - `showPage()` 中自动写入 `location.hash`
  - 正常导航 `pushState`，返回键 `replaceState`，`_fromPopstate` 防死循环
  - 页面刷新后读取 hash 自动恢复

- **底部导航重构**
  - 收藏从底部导航移除（首页入口保留），替换为"测验"
  - 底部导航现在为：首页 / 课程 / 测验 / 易错本 / 设置

- **视觉与样式更新：**
  - CSS 变量 `--surface` 从 `rgba` 改为纯色
  - 全局删除所有 `backdrop-filter` 毛玻璃效果
  - `.word-actions-under-stars` 改为横排
  - 星星 12→10px，掌握徽标 18×18 正圆
  - 搜索页补上 🔊 喇叭按钮
  - sw.js 缓存版本 v3→v4

### 4. ⚠️ 遇到的问题与报错 (Issues & Challenges)

1. **改了背景色还是透明** - 三重修复：改 `--surface` + 删 `backdrop-filter` + 加 `z-index`
2. **hash 路由死循环** - `_fromPopstate` 标志位 + `replaceState`
3. **测验中底部导航跳走不提示** - `origShowPage` 包装函数
4. **`showNav()` 逻辑缺失** - `showPage` 中恢复条件
5. **SW 缓存导致部署后看不到更新** - sw.js 升版本号

### 5. 💡 解决方案与避坑经验 (Solutions & Takeaways)

- **CSS 变量是双刃剑：** `--surface` 定义成 `rgba`，全局几十处引用，处处微透
- **透明问题的排查顺序：** CSS 变量 → 元素背景 → `backdrop-filter` → `z-index`
- **z-index 不是可选项：** 任何 `position: fixed` 必须显式设 `z-index`
- **hash 路由的两个关键细节：** popstate 用 `replaceState`；初始加载读 hash
- **Service Worker 版本号要跟着每次部署更新**
- **答题交互"答对跳、答错停"的设计原理：** 答对快速推进保持节奏；答错停住让用户确认

---

## 📅 2026-07-22 网页修改与更新总结

### 1. 📝 改动概述 (Overview)

- **涉及文件：** `daily-hangul-勿删-部署用/index.html`、`daily-hangul-勿删-部署用/sw.js`、`每日韩语-源码.html`、`CHANGELOG.md`
- **主要目的：** 引入 SRS 间隔重复系统替换二元掌握机制、补齐延世第一册缺失词汇（+89词）、为高频词添加手工例句（21词×5句）、修复发音 TTS 语音残留 bug、修复 ES6 语法兼容性导致收藏/掌握失效、部署迁移至 GitHub Pages
- **部署状态：** ✅ 已部署 `https://rrbiit.github.io/daily-hangul/`

### 2. 🚀 新增功能与 UI 效果 (Added Features & UI)

- **SRS 间隔重复系统（替换旧的"已掌握"二元标记）**
  - SM-2 算法：四级评分（忘记/困难/一般/简单），按记忆曲线计算下次复习时间
  - "已掌握"按钮一键直达 lv=4
  - 复习模式新增"待复习"标签
  - 首页统计改为 ✅已掌握 / 📖学习中 / ⭐已收藏
  - 进度条统计"至少复习过一次"的词（lv≥1）
  - 旧数据自动迁移

- **词汇数据扩充** - 总计新增约 89 词，从 495 → 约 563 词
- **手工例句扩展** - 为 21 个高频词添加手写例句（每词 5 句）
- **TTS 语音残留修复** - `speak()` 函数顶部无条件取消旧语音合成
- **Service Worker network-first 策略**

### 4. ⚠️ 遇到的问题与报错 (Issues & Challenges)

1. **ES6 语法 `Object.assign` 导致收藏/掌握全部失效** - 微信 X5 等老浏览器不兼容，改为纯 ES5 循环
2. **SRS 阈值过高导致进度条不动** - lv≥5 门槛过高，改为 lv≥1 即有反馈
3. **Netlify 额度耗尽** - 迁移至 GitHub Pages
4. **发音连续点击时不停止旧语音** - `speak()` 顶部无条件 cancel
5. **Service Worker 缓存** - cache-first → network-first
6. **ctrl-label 选择器指向错误** - 改为 `parentNode.querySelector`

### 5. 💡 解决方案与避坑经验 (Solutions & Takeaways)

- **ES5 兼容性是底线：** 项目需跑在微信 X5、夸克等老内核
- **SRS 设计与 UX 的平衡：** 一键标记 + SRS 评分共存
- **进度条感知阈值：** 第一次操作时就应有变化
- **GitHub API 可靠部署通道**
- **TTS 双路径必须互斥**

---

## 📅 2026-07-24 网页修改与更新总结

### 1. 📝 改动概述 (Overview)

- **涉及文件：** `每日韩语-源码.html`、`每日韩语-产品复盘.md`、`每日韩语-项目说明.docx`、`daily-hangul-勿删-部署用/index.html`
- **主要目的：** 单词列表页全面升级（掌握标注 + 排序 + 过滤 + 收藏 + 发音 + 一键标记）+ SRS 间隔重复系统完善 + 易错本（薄弱词自动收集）+ 搜索增强 + 学习模式体验打磨 + 部署至 GitHub Pages
- **部署状态：** ✅ 已部署 `https://rrbiit.github.io/daily-hangul/`

### 2. 🚀 新增功能与 UI 效果 (Added Features & UI)

- **单词列表页全面升级** - 掌握徽标、智能排序、过滤栏、收藏星标、发音按钮、一键整课标记
- **SRS 间隔重复系统完善** - 评分按钮移至卡片正面底部（position: absolute）
- **易错本（薄弱词系统）** - badCount 追踪、独立列表页、底部导航入口
- **搜索页增强** - 中/韩/词性三方搜索、高亮、操作按钮
- **学习模式智能开始** - 自动跳到第一个未掌握词
- **滑动手势** - 水平翻页、上滑翻转
- **底部控制栏不透化**

### 4. ⚠️ 遇到的问题与报错 (Issues & Challenges)

1. **评分按钮被底部控制栏遮挡** - 移入 card-front，absolute 定位
2. **评分按钮挤走单词** - position: absolute 独立定位
3. **发音按钮点击后词卡也展开** - onclick 入口加 closest 守卫
4. **底部控制栏透明** - 改为纯色背景
5. **测验模式按钮 URL 不响应** - 用户选择删除，待后续重构

### 5. 💡 解决方案与避坑经验 (Solutions & Takeaways)

- **操作按钮与容器 click 冲突：** `e.target.closest()` 守卫比 `stopPropagation` 更可靠
- **卡片内元素定位策略：** flex 居中 + absolute 浮动
- **评分 UI 从分离式到内嵌式：** 卡片内部视觉上下文更清晰
- **薄弱词数据设计：** badCount 内嵌 SRS，完全向后兼容
- **多页面共享事件委托：** document 级别单一委托 + 按 page 分流

---

## 📅 2026-07-19 网页修改与更新总结

### 1. 📝 改动概述 (Overview)

- **涉及文件：** `每日韩语-源码.html`、`每日韩语-产品复盘.md`、`每日韩语-产品复盘.docx`、`manifest.json`、`sw.js`
- **主要目的：** 代码审查后修复 5 个中等 Bug + 单词列表布局优化 + 部署
- **部署状态：** ✅ `https://daily-hangul.netlify.app`

### 2. 🚀 新增功能与 UI 效果 (Added Features & UI)

- **Bug 修复（代码审查）**
  - 删除重复 CSS 定义
  - 课程页统计实时填入（不再永远显示 0）
  - renderGrammarExampleRow 补充 HTML 转义
  - 同词跨课共享状态冲突修复：新增 `wk(w, num)` 辅助函数，key 从 `w.kr` 改为 `num|w.kr`
- **视觉更新：** 单词列表从单列改为双列网格

### 4. ⚠️ 遇到的问题与报错 (Issues & Challenges)

1. **PowerShell 正则替换损坏代码** - 放弃批量正则，逐一定位
2. **Netlify CLI 认证反复失效** - 手动 `netlify link --id` 重连
3. **Edit 工具匹配大段 CSS 失败** - 小段标记文字定位

### 5. 💡 解决方案与避坑经验 (Solutions & Takeaways)

- **用 `node --check` 做语法守卫**
- **全局 key 设计是数据基础：** `num|w.kr` 影响 15 处，应早期设计
- **Netlify CLI：** 清除缓存 + 精确绑定 site ID

---

## 📅 早期版本：例句与变形升级

- **涉及文件：** `yonsei-study.html`、`CHANGELOG.md`（新建）
- **主要目的：** 修复复习页 Bug、补全例句生成、每词 1→5 条例句、名词分类、18 种动词变形

### 2. 🚀 新增功能

- 每词 5 条例句（原来 1 条）
- 名词智能分类：场所/人物/食物/物品
- 动词/形容词自动变形：18+ 种形态
- 韩语助词自动选择（받침）

### 4. ⚠️ 遇到的问题

1. **复习页变量遮蔽导致点击无反应** - 局部变量与全局 Set 重名
2. **语法错误：多余括号** - 旧代码残留
3. **中文引号在 JS 字符串中未转义**
4. **Node.js 全局模块路径不匹配**
5. **Edit 工具无法匹配含韩文/中文的字符串**

### 5. 💡 经验

- 变量命名规范：局部变量不与全局状态同名
- 大段替换策略：PowerShell 切割 + Write
- 语法检查：每次改后跑 `node --check`
- 形容词不能用进行时（~고 있다）

---

## 📅 早期版本记录 (历史概要)

### 初始版本
- 创建单文件 HTML 应用，包含 VOCAB 数据（~480 词）、GRAMMAR 数据（~50 条）
- 实现页面切换、课程列表、单词浏览、语法展示

### 卡片学习模式
- CSS 3D 翻转动画 + 左右滑动切换
- 收藏、已掌握标记与 localStorage 持久化
- 底部导航栏、页面路由

### 搜索与复习
- 全课全文搜索（韩语/中文）、200ms 防抖
- 复习模式：按类型筛选 + Fisher-Yates 随机打乱
- 键盘快捷键支持

### 收藏与语法
- 单词 + 语法独立收藏系统
- 收藏页标签切换
- 语法收藏跳转到对应课的语法标签

### 例句与变形
- 初次实现 `generateExample` 按词性生成单条例句
- 后续扩展为按词性+特殊词手工匹配
- 升级为每词 5 条 + 名词分类 + 18 种动词变形
