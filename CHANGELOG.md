# CHANGELOG · Daily Hangul (每日韩语)

> 旧格式历史记录已归档至 [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md)。
> 新条目请按 [CHANGELOG_RULES.md](CHANGELOG_RULES.md) 规范编写。

---

## 📅 2026-08-15 22:42 · v1.25.2

### 📝 一句话总结
全面替换浏览器原生弹窗：新增通用毛玻璃弹窗（确认型/提示型/警示型），9 处 alert / confirm 全部换成自绘弹窗，视觉与网站统一，不再出现系统默认弹框。

### 🎯 本次更新目标
- 为什么修改？项目此前多处仍用浏览器原生 `alert` / `confirm`：样式突兀、与网站韩系简洁风格割裂，手机上体验生硬。测验词数不足弹窗已自绘（v1.25.1），这次把其余高频/低频入口全部统一。
- 解决了哪些问题？① 9 处原生弹窗的视觉割裂；② 破坏性操作（导入覆盖、清除数据）在手机上容易误触且无警示感；③ 原生弹窗无法承载品牌化文案与引导。
- 希望提升哪些体验？所有提示/确认都在统一的毛玻璃弹窗体系内完成，视觉一致、轻重分明（破坏性操作用警示色按钮）。

### ✨ 本次更新
- 新组件：通用毛玻璃弹窗 `showAppDialog`（`app.js` + `index.html` + `style.css`）
  - 确认型（主按钮 + 次按钮）/ 提示型（单按钮）/ 警示型（主按钮为粉红警示渐变，用于覆盖导入、清除数据、取消全部掌握）
  - 毛玻璃半透明卡片，样式与测验词数弹窗同体系（圆角 20px、渐变主按钮、深色模式适配）
- 替换清单（9 处，原 alert / confirm 全部移除）：
  - 单词列表：取消本课全部已掌握（警示）、本课全部标记为掌握
  - 设置页：导入备份覆盖确认（新/旧格式，警示）、导入成功（确认后刷新）、导入失败、清除所有数据（警示，保留 6 项清单）、清除成功（确认后刷新）
  - 测验：退出测验确认（异步化：确认后保存已答记录并重置，✕ 按钮/底部导航/页面跳转/手机返回键四路共用，支持确认后跳转；返回键触发时先 `history.pushState` 把历史顶回测验页再弹确认，避免「已经返回了又弹确认」的状态脱节）
- 重构：测验「重置回设置页」抽为 `resetQuizToSetup`，`quizBackToSetup` 与退出逻辑共用
- 全站返回键守卫（`app.js`）：返回键按下时按优先级处理，页面回退不再与弹窗/浮层状态脱节
  - 搜索浮层 / 学完总结弹窗 / 通用弹窗 / 测验词数弹窗打开时 → 先关闭该层并顶回历史（pushBackState），不让页面后退
  - 学习模式（闪卡）→ 干净退出（停语音 + 保存进度）后放行回退
  - 返回键放行时同步页面内返回栈（syncBackStack），修复「返回键 + 页内返回按钮」混用导致的返回错乱
- 测试：`scripts/test-quiz.js` 新增 U 段 9 项断言（退出弹窗文案 / 确认后保存并重置 / 未答题文案区分）

### 👤 用户体验变化
- 修改前：到处是浏览器默认的灰色 alert / confirm，样式与网站完全脱节。
- 修改后：所有确认与提示都是统一的毛玻璃圆角卡片；清除数据、覆盖导入这类危险操作配警示色按钮，一眼可辨；测验退出确认后行为与之前完全一致（已答计记录、可取消）。
- 学习流程是否更加自然？提示不再打断视觉节奏，破坏性操作更有分量感，整体像同一个产品。

### 💭 设计思考
- 为什么做一个通用弹窗而不是逐个复制？9 处场景本质只有三种形态（确认/提示/警示），一个 `showAppDialog(opts)` 函数即可覆盖，后续新增场景一行调用；样式也只有一套毛玻璃体系，避免重复。
- 为什么成功提示用弹窗而不用 toast？导入/清除成功后页面会立即刷新，toast 一闪而过看不清；用提示型弹窗让用户确认后再刷新。
- 为什么测验退出要异步化？原生 confirm 是同步阻塞，自绘弹窗是异步的；把「确认后动作」用回调传入，三路调用（✕ 退出 / 底部导航 / 页面跳转）行为保持一致。

### ⚠️ 开发记录
- 测试环境补桩：`showAppDialog` / `addQuizSummary` / `unmarkMastered` / `getStudyDay`（Node VM 未加载 app.js）。
- 发现并修正：未答题时退出原来也会弹确认（只是文案不含「计入记录」），不是直接重置——测试断言据此校准。
- 手机返回键拦截：popstate 触发时浏览器历史已回退、URL 已变，仅拦截页面切换会留下「已返回却弹确认」的错位；需先 `history.pushState({page:'page-quiz'},'','#quiz')` 顶回再弹确认，确认退出后才放行。返回键路径需真机验证（Android 返回键 / iOS 返回手势 / 桌面浏览器后退）。
- 全站守卫层级：同一套「popstate 时历史已回退」原理，模态层（搜索/学完/通用/词数不足）用 `pushBackState` 顶回当前页；学习模式放行路径无需顶回（退出学习正是回退目标）。`syncBackStack` 只做「栈顶恰好等于回退目标」时的弹出，避免破坏 navigateTo/goBack 的正常入栈弹栈。真机验证清单：搜索浮层返回键、弹窗返回键、学习模式返回键、返回键与页内返回按钮混用。

### 📌 下一步建议
- 易错本空状态（study.js「还没有薄弱词！」alert）是最后一处原生弹窗，可换提示型弹窗并加「去学几个新词」引导
- 通用弹窗可考虑支持「输入框」形态，为未来的自定义复习计划等做准备
- 弹窗动画（fadeIn/slideUp）已沿用，未来可统一为系统级动画时长

---

## 📅 2026-08-15 22:24 · v1.25.1

### 📝 一句话总结
修复测验「词汇不足」体验：剩余 1~3 个词时弹毛玻璃确认卡片可直接开始小测验（题数=实际词数），0 个词时引导切换范围，不再弹生硬的系统报错。

### 🎯 本次更新目标
- 为什么修改？「自动已掌握」上线后，未掌握词池快速缩小，用户测到最后点「再来一轮」会弹「词汇不足」无法开始——学得越好反而越测不了。
- 解决了哪些问题？① 生硬的原生 alert「词汇不足」；② 1~3 个词时明明还能测却直接拒绝；③ 0 个词时没有引导，用户不知道怎么办。
- 希望提升哪些体验？让「快学完」的状态也能顺利完成最后一轮小测验，形成「全部掌握」的成就感闭环。

### ✨ 本次更新
- 新交互：词数不足确认弹窗（`quiz.js` + `index.html` + `style.css`）
  - 剩余 1~3 个词：毛玻璃半透明确认卡片「只剩 N 个词 / 开始 N 题 / 先不测」，确认后用这 N 个词开局（题数 = 实际词数）
  - 剩余 0 个词：引导卡片「没有可测的词啦」，提示切换到「全部」「已掌握」等范围
  - 弹窗沿用现有弹窗风格（圆角卡片 / 渐变主按钮 / 描边次要按钮 / 深色模式适配），卡片与遮罩均为半透明 + backdrop-filter 毛玻璃
- 门槛放宽：开始按钮禁用条件从「少于 4 词」改为「0 词」；选择题干扰项本就取自全教材，小词池也能正常出 4 选 1（听写模式更不受限）
- 测试：`scripts/test-quiz.js` 新增 T 段 11 项断言（小池出题 / 弹窗显示与文案 / 确认开局 / 0 词引导）

### 👤 用户体验变化
- 修改前：测到最后弹「词汇不足」alert，只能放弃或手动切换范围。
- 修改后：弹好看的确认卡片，点一下就用剩余词开一轮小测验；测到 0 个词时给出明确的方向提示。
- 学习流程是否更加自然？「最后一轮」不再是死胡同，而是顺理成章的收尾。

### 💭 设计思考
- 为什么用毛玻璃半透明？弹窗是测验场景的高频确认，半透明 + 模糊让遮罩后的页面若隐若现，视觉更轻盈柔和，也避免 100% 不透明的沉重感，与网站「韩系简洁、柔和」的整体气质一致。
- 为什么小词池也能测？干扰项来自整本教材而非筛选池（`buildQuizOptions` 用 `buildBookItems()`），所以哪怕只剩 1 个词也能出 4 选 1，门槛 4 只是人为设定的「一轮最少题数」，没有技术必要。
- 为什么不自动从其他范围补词？违背用户筛选意图——选了「未掌握」却混进已掌握词会误导判断。

### ⚠️ 开发记录
- 测试环境补桩：`document.createElement` / `hideNav` / `showNav` / `markStudyDay`（Node VM 未加载 app.js 与 index.html 内联脚本）。
- `testPoolOf` 会跨课重复收集同形词，词数不确定，弹窗测试改用 `testItemByKey` 构造确定词数的池子。
- 版本说明：v1.25.0（自动已掌握）已推送，本批改动作为独立修复版本 v1.25.1。

### 📌 下一步建议
- 「最后一轮」结束后可在结果页给一句庆祝文案（如「未掌握词已全部清空 🎉」），强化成就感
- 单词列表/学习卡上的「N/4」进度角标（此前建议过，本次仍按「做减法」未加）
- 阈值 4 与「最少 4 题」的设定后续可放入设置页

---

## 📅 2026-08-15 20:59 · v1.25.0

### 📝 一句话总结
测验（Quiz）新增「答对累计自动判定已掌握」：同一个单词在测验中累计答对 4 次，系统自动把它标记为已掌握，不再只靠用户手动判断。

### 🎯 本次更新目标
- 为什么修改？「已掌握」之前只能靠用户手动标记或 SRS 复习升级，缺少一条「根据真实测验表现自动判定掌握」的通道。用户希望系统基于实际答题表现来判定掌握程度。
- 解决了哪些问题？① 测验答对次数此前没有任何记录；② 「已掌握」完全依赖主观判断，缺少客观依据；③ 答对多次但从未手动标记的词，进度上一直显示未掌握。
- 希望提升哪些体验？让「已掌握」从「用户自己判断我会了」逐步变为「系统根据真实测试表现判断你已经掌握了」，并给用户清晰的进度反馈（答对 3/4 → 再答对 1 次自动掌握）。

### ✨ 本次更新
- 新功能：测验答对累计 → 自动已掌握（`quiz.js`）
  - 每个单词在测验中每答对一次，累计次数 +1（封顶 4，不会出现 5/4）
  - 累计达到 4 次 → 自动走现有「已掌握」通道（lv=4，与手动标记同一入口，所有页面立即生效）
  - 答错不增加、不清零；已掌握的词继续答对不重复处理
  - 若被现有规则（测验答错降级 / SRS 重学）移出已掌握，累计次数保留，下次答对按同一规则自动恢复
  - 次数按「书|课|韩语」key 存储（复用单词唯一标识体系），不同教材的同形词互不影响；次数存放在现有单词学习记录（srs）内，随现有保存/备份/导入自动持久化，老数据无字段时按 0 处理，零迁移风险
- UI 反馈（沿用现有 design system，不新增卡片/弹窗）
  - 答对反馈行下方一行小字：该词已答对 N/4 · 再答对 M 次自动掌握（弱化色）
  - 达到 4 次那一刻：🎉 已掌握！累计答对 4 次（绿色），自动跳下一题前多停留片刻
  - 测验结果页：✨ N 个词累计答对 4 次，已自动标记为已掌握（与答错降级提示同款式，正向绿色）
- 测试：`scripts/test-quiz.js` 新增 S 段 22 项断言（正常累计 / 答错不清零 / 封顶 / 多教材隔离 / 手动掌握不冲突 / 降级后恢复 / 听写"很接近"计入 / 持久化）

### 👤 用户体验变化
- 修改前：测验答对只计本轮分数，答对多少次都不会影响「已掌握」，一切靠手动。
- 修改后：答对时能看到进度（1/4 → 2/4 → 3/4 → 🎉 已掌握），到 4 次自动变已掌握；刷新、重开、切换教材、备份导入，次数都在；不同教材里的同一个词各算各的。
- 学习流程是否更加自然？答对本身就有了长期意义——每答对一次都在积累「掌握证据」，进度可见、动力更强。

### 💭 设计思考
- 为什么把次数放进现有单词学习记录而不是新建存储？项目里已有「答错次数」（badCount）存这里的先例；这样导出备份、导入恢复、多教材切换自动跟随，且手动「取消已掌握」时记录整体清除、次数一并归零，语义一致。改动面最小，不碰老数据。
- 为什么封顶 4 而不是继续累计？达到阈值后继续累计没有信息价值，封顶避免无意义增长和「5/4」这类奇怪展示；内部不会再触发重复处理。
- 为什么保留「答错降级已掌握」的现有行为？这是项目既有的复习逻辑（已掌握词答错 → 移出已掌握进入复习），用户明确要求不改动；本功能只在「答对」时动作，二者不冲突。被降级后次数保留、再答对即恢复，符合「答错不清零、不因一次答错就失去掌握」的产品意图。
- 为什么阈值用固定 4？用户需求明确「累计答对 4 次」，规则简单可预期；后续如需可改为按难度/模式差异化，本次不做。

### ⚠️ 开发记录
- 测试环境（Node VM）未加载 app.js，`srs` / `markMastered` / `saveUserData` 以桩提供；`scripts/test-quiz.js` 的上下文已补充，不影响既有 60 项断言。
- 听写「很接近」按现有判分口径（判对）同样计入答对次数，与计分一致。

### 📌 下一步建议
- 可在单词列表/学习卡上显示「N/4」小角标（本次按「做减法」未加，如需要可后续加）
- 阈值 4 目前写死，后续可考虑放进设置或按词性/难度差异化
- 考虑在统计页增加「累计答对次数」维度，让用户看到长期积累
- 已掌握判定目前是单向加分制，未来可结合 SRS 遗忘曲线做更细的「重新评估」

---

## 📅 2026-08-14 17:56 · v1.24.0

### 📝 一句话总结
单词发音改为「预生成 mp3 本地文件」：不联网、任何浏览器都能播——手机夸克/微信等连不上在线发音服务的环境，单词发音终于能听了。例句走 Google TTS，删除 Edge TTS 通道。

### 🎯 本次更新目标
- 为什么修改？在线发音服务（Google 被墙、微软 Edge TTS 在手机/夸克等环境连接失败）在用户的真实使用环境里不可用，导致"点单词没声音"。发音是学习核心功能，必须保证任何环境可听。
- 解决了哪些问题？① 1192 个单词（延世1+延世2）全部预生成为 mp3 随网站发布，点词播放本地文件，物理上消灭"连不上"；② 例句走 Google TTS（用户可翻墙环境能听），失败静默不打扰；③ 删除手机环境实测连不上的 Edge TTS 通道与原生语音降级提示。
- 希望提升哪些体验？单词发音在任何设备、任何浏览器（含夸克/微信）都能听；不再弹烦人的失败提示。

### ✨ 本次更新
- 新功能：单词音频预生成
  - 新增 `audio/` 目录：1192 个单词 mp3（`audio/<书号>/<课号>/<韩语>.mp3`，共 12.2MB）
  - 批量合成：无头浏览器调用微软 Edge TTS 协议生成（1192 词全部成功、零失败），按需加载（点哪个词下哪个，单文件约 10KB），PWA 缓存后离线可播
- 播放逻辑重构（`study.js`）：
  - `speak(text, lang, localSrc)`：本地 mp3 优先 → Google TTS 兜底（失败静默）
  - `speakLocal(w, num, lang)`：词条级本地播放（列表/学习卡/收藏/易错本/搜索/测验听音）
  - `speakExample(text, lang)`：例句走 Google TTS（用户翻墙环境可用），失败静默
  - 统计页词条（掌握/错词）自动解析课号 → 本地播放
- 移除：Edge TTS 通道（WebSocket 合成、GEC 签名、连接复用/预热，约 300 行）、原生语音降级与失败提示（Edge 移除后仅剩弹提示作用）

### 👤 用户体验变化
- 修改前：手机上单词发音连不上在线服务，全部无声；例句/词条点击还可能弹"不支持发音"提示。
- 修改后：**单词发音任何环境必能听**（本地文件）；例句电脑（翻墙）可听、手机静默不打扰；不再有失败提示。
- 网站体积增加 12.2MB（仓库 100MB 限制内，按需加载无感）。

### 💭 设计思考
- 为什么预生成而不是继续在线合成？用户实际环境（手机、夸克、微信）连不上任何在线发音服务；预生成本地文件是唯一"任何环境可听"的方案。可用性优先于优雅。
- 为什么例句不预生成？例句 4700+ 句约 70-80MB，会把仓库逼近 100MB 限制、延世3 无空间；例句是辅助理解（看文字即可），走在线 Google TTS 是性价比选择。
- 为什么删 Edge TTS？实测用户常用环境（手机三浏览器、电脑夸克）均连不上；保留只会增加代码复杂度与维护成本。发音链路简化为「本地 mp3 → Google」。

### ⚠️ 开发记录
- 批量合成管道：Node 原生 WebSocket（undici）与服务端握手不兼容（收不到响应），改用无头 Edge 页面内合成 → CDP 取回 base64 → 写文件；1192 词零失败。
- 播放兼容：`ws.binaryType` 教训不适用于本地文件；本地播放用 `a.muted=true` 启动绕过自动播放策略（学习卡自动发音可用）；`a.onerror` 回退 Google。
- 验证（全部通过）：`check-examples.js`；`test-quiz.js` 59/59；`test-confusion.js` 44/44；`test-browser.js` 44/44；真实浏览器本地播放 7/7（本地文件正常播、缺失回退 Google 不崩溃）。

### 📌 下一步建议
- 真机验证：手机夸克/微信打开线上地址，确认单词发音（本次修复的核心目标）
- Y1：学习日志（ys-daily-log）保留策略（防 localStorage 触顶导致进度静默丢失）
- Y3：测验模式/范围/题数偏好持久化
- Y4：词列表发音/掌握/学习小按钮触控区域放大
- Y5：浅色模式小字对比度提升

---

## 📅 2026-08-14 17:17 · v1.23.0

### 📝 一句话总结
发音方案 A 完整上线：发音主通道从被墙的 Google TTS 换成国内直连的微软 Edge TTS（韩语神经语音），并加入连接复用与预热——不翻墙也能听，且连发多词不再重复握手。

### 🎯 本次更新目标
- 为什么修改？原有发音依赖 Google TTS，在国内被墙（实测连接超时），降级到浏览器原生语音后桌面又常无韩语语音包，导致"无声"；且每次发音都新建 WebSocket 连接，国内连微软服务器握手就要 0.5-1 秒，体验慢。
- 解决了哪些问题？① 发音主通道换成 Edge TTS（speech.platform.bing.com，实测国内直连可用、免费、韩语为神经语音）；② 连接复用 + 首次点击预热，后续发音不再重复握手；③ 发音失败保留可见提示（v1.22.0 引入）。
- 希望提升哪些体验？关不关梯子都能听；点词发音从"等 1-2 秒"降到"接近即时"。

### ✨ 本次更新
- 功能改进（发音）：
  - `study.js` `speak()` 主通道：Google TTS → 微软 Edge TTS（`ko-KR-SunHiNeural` 韩语神经语音，中文 `zh-CN-XiaoxiaoNeural`）
  - 新增 `sha256Hex()`（纯 JS，不依赖 crypto.subtle）与 `edgeTtsGec()`（Sec-MS-GEC 签名，与 edge-tts Python 参考实现逐字节对照验证）
  - 新增 `speakEdgeTTS()`：WebSocket 合成协议（speech.config → ssml → MP3 音频流），MP3 同步字定位取音频，对服务端头格式变化鲁棒
  - **连接复用**：同一连接连续多轮合成（实测 5 轮全部成功），空闲 60 秒自动关闭，页面离开即释放
  - **预热**：首次用户点击（pointerdown）即提前建连，随后发音免握手
  - **请求排队**：快速连点时串行处理（最新优先），不丢请求
  - 关键修复：`ws.binaryType = 'arraybuffer'`（二进制消息默认是 Blob，不设置收不到音频）；只有 `Path:turn.end` 才结束请求（turn.start/response 是中间信号）
  - 播放前静音启动再取消静音，绕过浏览器自动播放策略，学习卡片"自动发音"在桌面也能出声

### 👤 用户体验变化
- 修改前：国内打开基本无声（Google 被墙 → 降级原生 → 桌面无韩语语音）；有声音时每次发音要等 1-2 秒。
- 修改后：不翻墙直连，点击即听；学习卡片自动发音可用；连续发音明显变快。
- 已知边界：夸克浏览器对"本地 localhost 页面连接外网 wss"有限制（实测连接被拒），线上 HTTPS 正常域名待真机验证；若夸克线上也受限，后续可用"预生成音频文件"方案兜底。

### 💭 设计思考
- 为什么选 Edge TTS 而不是国内云 TTS？Edge TTS 免费、无需注册和 key、国内直连（实测）、韩语神经语音质量好；纯前端可实现，不引入服务端。
- 为什么做连接复用 + 预热而不是一次一连接？发音延迟主要来自 TLS 握手（国内连微软 0.5-1s），复用后每次发音只剩合成+传输时间。协议实测支持同一连接连续多轮合成。
- 为什么保留原生语音降级 + 失败提示？多一层保险：Edge TTS 不可用（老内核/被拦）时降级原生，失败时明确提示，不静默。

### ⚠️ 开发记录
- 调试过程记录（供后续维护参考）：
  - Sec-MS-GEC 是纯 SHA-256（时间戳字符串+Token 拼接），不是旧版 HMAC；用 Python 参考实现同秒对照验证一致
  - 服务端二进制帧格式：2 字节大端头长度 + 头文本 + MP3 数据；用 MP3 同步字（0xFF 0xE0~）扫描定位最鲁棒
  - `window.speechSynthesis` 是只读 WebIDL 属性，测试桩需用 `Object.defineProperty` 注入
  - CDP 无头输入模拟会偶发丢失连续点击事件（JS 触发验证 3 次全正常，属测试环境限制非应用 bug）
  - headless 环境即使 muted 也会拒绝自动播放（与真机 Chrome 策略不同），真机以实际听感为准
- 验证（全部通过）：`check-examples.js`；`test-quiz.js` 59/59；`test-confusion.js` 44/44；`test-browser.js` 44/44；多教材 jsdom 39/39；真实 Edge 实测连接复用 5 轮全成功、全程 1 条连接、降级路径与失败提示正常、控制台 0 错误。

### 📌 下一步建议
- 真机验证：夸克/微信里打开线上 HTTPS 地址确认发音；如仍受限，实施"预生成单词音频文件"方案（本地 mp3，任何环境可播）
- Y1：学习日志（ys-daily-log）保留策略（防 localStorage 触顶导致进度静默丢失）
- Y3：测验模式/范围/题数偏好持久化
- Y4：词列表发音/掌握/学习小按钮触控区域放大
- Y5：浅色模式小字对比度提升

---

## 📅 2026-08-14 16:10 · v1.22.0

### 📝 一句话总结
新增「自动备份提醒」安全网，并修复发音失败时无任何提示的问题——数据多一层保障，发音出问题时不再"无声装死"。

### 🎯 本次更新目标
- 为什么修改？全面审查时确认两点：① 学习数据都存在浏览器 localStorage，清缓存/换设备可能全丢——用户已有导出备份功能，但靠手动记得，缺少提醒；② 发音依赖 Google TTS 在线接口，失败时降级原生语音，两条路都失效时完全无声且无提示，用户会以为功能坏了。
- 解决了哪些问题？备份从"靠自觉"变成"温和提醒"；发音失败从"静默"变成"可见提示"。
- 希望提升哪些体验？数据安全多一层保障；发音问题可感知、可排查。

### ✨ 本次更新
- 新功能：自动备份提醒（纯前端，不引入后端）
  - 首页进度条下方新增提醒条：「💾 距上次备份已超过 7 天」+ [去备份] [暂不]
  - 触发条件：已有学习数据（进度/收藏/日志任一项）且距上次处理超过 7 天；新用户无数据不打扰
  - 「去备份」复用现有导出功能下载 JSON；「暂不」同样记录时间；设置页手动导出也会记录——三种方式后 7 天内不再提醒
  - 新增存储 key：`ys-last-backup`（`ys-` 前缀，随现有导出/导入/清除体系自然覆盖）
- Bug 修复：发音失败提示（`study.js` `speak()`）
  - 原生语音缺失 → 提示「⚠️ 发音失败，当前环境不支持发音」
  - 原生语音报错或静默无声（3 秒兜底计时，`onstart`/`onend` 会取消计时避免误报）→ 提示「⚠️ 发音失败，请检查网络」
  - 自动播放策略拦截（非点击触发的自动发音被浏览器拒绝）→ 不提示，避免每张卡片都弹
  - 连续发音时旧语音的兜底计时器会先清掉，不会误报

### 👤 用户体验变化
- 修改前：备份全靠自己记得；发音挂了完全无声，像功能坏了。
- 修改后：超过 7 天没备份，首页出现一行温和提醒，一键导出；发音失败会明确告诉你是网络/环境问题。
- 正常发音、正常学习流程无感知变化；提醒条只在"有数据且超 7 天"时出现，点一下就静默 7 天。

### 💭 设计思考
- 为什么做纯前端提醒而不是引入云同步？单用户本地使用场景下，导出备份是最低成本的安全网；"温和提醒"（不弹窗、点一次静默 7 天）避免打扰每天的学习节奏，符合"做减法"原则。
- 发音失败提示为什么区分"自动播放拦截"？非点击触发的自动发音被浏览器拦截是正常现象（学习卡片自动播放），提示反而每张卡都烦；只有"真失败"（网络/环境）才提示，把信号留给真正的问题。

### ⚠️ 开发记录
- 验证（全部通过）：`check-examples.js`；`test-quiz.js` 59/59；`test-confusion.js` 44/44；`test-browser.js`（jsdom 页面回归）44/44；真实无头 Edge 23 项断言（Y2 五种失败/正常路径 + 备份提醒 10 项 + 多教材回归 + 320/390px 无溢出 + 控制台 0 错误）。
- 测试中发现：`window.speechSynthesis` 是浏览器只读属性，普通赋值无效，测试桩需用 `Object.defineProperty` 注入——应用代码本身无此问题。

### 📌 下一步建议
- Y1：学习日志（`ys-daily-log`）增加保留策略（如只保留最近 90 天），防止长期使用后 localStorage 触顶导致进度静默写不进去（数据安全优先项）
- Y3：测验模式/范围/题数偏好持久化，进入测验页不再每次重置为默认
- Y4：词列表的发音/掌握/学习小按钮触控区域放大（当前 16-21px，建议 28px+）
- Y5：浅色模式下小字对比度提升（`--text-dim` / `--text-subtle`）

---

## 📅 2026-08-14 15:44 · v1.21.1

### 📝 一句话总结
修复两个小问题：刷新/深链接进入页面后退出学习会错误回到首页；学习记录页的每日列表漏掉"只学单词"的日子（与顶部总览口径不一致）。

### 🎯 本次更新目标
- 为什么修改？对项目做了一次全面审查（完整阅读代码 + 实际打开网页逐页检查 + 多教材隔离验证），确认多教材数据完全隔离、无大问题，但发现两个真实小 Bug：
  ① 刷新或直接通过 #hash 深链接进入页面（如 #words）后，程序只切换了显示、没有同步内部页面状态，导致从学习模式退出时 `goBack()` 弹回的是"首页"而不是来源页；
  ② 统计页顶部总览会统计"今天学习了 X 个单词"，但下方每日列表只收录做过测验或点过掌握的日子，只背单词的一天在列表里消失，两处口径矛盾。
- 解决了哪些问题？退出学习不再"走错门"；学习记录页每日列表与总览口径统一。
- 希望提升哪些体验？位置感知更准确、学习记录更可信。

### ✨ 本次更新
- Bug 修复：
  - `app.js`：新增 `restorePage()` 页面恢复函数（刷新/深链接的统一入口：重置导航栈 + 同步内部当前页 + 显示目标页），让后续进入学习模式时能拿到正确来源页
  - `index.html`：启动脚本的 hash 恢复逻辑从 `showPage()` 改为 `restorePage()`
  - `app.js`：`popstate` 处理补边界情况——无历史标记（刷新/深链接进入的历史条目没有 pushState 状态）时，按当前 URL 的 #hash 推断目标页，不再一律回首页
  - `stats.js`：每日列表筛选条件增加 `(e.words || 0) > 0`，只学单词的日子也进入列表

### 👤 用户体验变化
- 修改前：#words 刷新 → 点学习 → 退出 → 回到首页，位置丢失；只背单词（没做测验、没点掌握）的日子，总览显示"今天学习了 X 个单词"但每日列表看不到。
- 修改后：退出学习回到正确来源页（单词列表 / 收藏 / 易错本）；每日列表与总览一致。
- 正常点击导航、浏览器返回键（有历史标记时）、教材切换、测验等行为完全不变。

### 💭 设计思考
- 为什么新增 `restorePage()` 而不是直接改 `showPage()`？"从 URL 恢复页面"（刷新/深链接）与"正常导航"语义不同：恢复需要重置导航栈并同步 `_currentPage`。单独一个入口函数，既修复了退出学习的来源页问题，也让以后所有刷新/深链接场景有统一复用点，不触碰正常导航逻辑。
- 返回键无历史标记时按 hash 推断：整页加载的历史条目浏览器不保留 pushState 状态（`e.state` 为 null），但 URL hash 始终可靠，用它推断目标页是最小且正确的方案；未知 hash 兜底首页。

### ⚠️ 开发记录
- 验证（全部通过，无回归）：
  - `check-examples.js`：通过（1192 词全手写例句）
  - `test-quiz.js`：59/59；`test-confusion.js`：44/44
  - `test-browser.js`（jsdom 页面回归）：44/44
  - 真实无头 Edge 实测：H1（#words/#starred/#weak 深链接 → 学习 → 退出回来源页、返回键 hash 推断）与 H2（只学 1 词 → 统计页总览与每日列表都出现）共 42 项断言全部通过
  - 多教材回归：延世1/延世2 学习、切换、统计页、深链接均正常，无串书；390px / 320px 无横向溢出；控制台 0 错误

### 📌 下一步建议
- Y1：学习日志（ys-daily-log）增加保留策略（如只保留最近 90 天），防止长期使用后 localStorage 触顶导致学习数据静默写不进去
- Y3：测验模式/范围/题数偏好持久化，进入测验页不再每次重置为默认
- Y2：发音接口（Google TTS）失败时给出可见提示，避免无声时用户以为功能坏了
- Y4：词列表的发音/掌握/学习小按钮触控区域放大（当前 16-21px，建议 28px+）
- Y5：浅色模式下小字对比度提升（--text-dim / --text-subtle）

---

## 📅 2026-08-14 02:02 · v1.21.0

### 📝 一句话总结
易混词功能收尾（阶段4）：补全"清除所有数据"对个人混淆记录的清理、matchMedia 防御性保护，并新增 jsdom 真实页面流程验证（五种模式完整回归），三套测试全绿。

### 🎯 本次更新目标
- 为什么修改？四个阶段的功能已完成，本阶段做最终收尾：①清除数据要能真正清掉个人混淆关系；②真实 DOM 页面流程验证五种模式与 SRS/易错本/历史联动；③发现并修复加载期 matchMedia 无保护问题。
- 解决了哪些问题？①"设置 → 清除所有数据"原本会删除 ys-confusions（ys- 前缀已在删除范围），但 clearConfusions 会把空对象重新写回存储，留下空壳 key——调整为先清内存再删存储，净结果零残留；②app.js 与 index.html 启动早期脚本调用 window.matchMedia 无保护，缺失该 API 的环境（旧 WebView）会中断启动——加防御降级；③新增 jsdom 真实页面流程验证，把"只靠 Node 单元测试"升级为"真实 HTML + 真实点击流程"。
- 目标体验：功能完整、清理干净、启动稳健，并有一套可重复的页面级回归测试。

### ✨ 本次更新
- `app.js`：
  - `resetAllData` 顺序调整：先清内存（含 `clearConfusions()`），再删除全部本地存储 key——避免清空后被重新写回空壳 `ys-confusions`
  - 清除确认框文案补充"个人易混记录（易混词辨析）"
  - `_themeMedia`（matchMedia）防御：缺失环境降级为不监听系统主题，不影响启动；applyTheme 与 change 监听同步加保护
- `index.html`：启动早期主题脚本的 matchMedia 调用加 `window.matchMedia &&` 保护
- 新增 `scripts/test-browser.js`：jsdom + 本地 HTTP 服务器真实加载 index.html 与全部模块，驱动真实 UI 流程（35 项断言）
  - 应用启动无错误 / 五种模式按钮与顺序 / 听音→韩语真实出题（韩语词形选项）与完整 5 题答题 / 结果页与测验历史 / 听写判分 / 答错路径下 SRS badCount、易错本、测验历史联动 / 韩→中·中→韩·听音→中文快速回归 / 无脚本异常 / 清除所有数据（含 ys-confusions）端到端
- `scripts/test-confusion.js` / `scripts/test-quiz.js`：维持 44 / 59 项全绿

### 👤 用户体验变化
- 修改前：清除所有数据后，个人混淆关系可能残留空壳；极老环境的 WebView 可能因 matchMedia 缺失启动失败。
- 修改后：清除数据零残留（内存与存储都不留旧混淆）；启动更稳健；测验五种模式都经过页面级流程回归验证。
- 学习流程不受影响：所有功能行为与阶段1~3一致，本阶段以收尾与验证为主。

### 💭 设计思考
- 为什么用 jsdom 而非只跑 Node 单元测试？单元测试验证逻辑，页面级测试验证"真实 HTML 结构 + 真实事件流"（按钮、选项渲染、点击答题、结果页、localStorage 持久化）。本机无 Chrome/Edge，jsdom 是可达的最接近真实浏览器的方案；发音（Audio/speechSynthesis）与 matchMedia 用桩/防御处理，并在报告中说明局限（无真实排版引擎，溢出靠 CSS 结构检查）。
- 为什么"先清内存再删存储"？clearConfusions 的职责是"清空并持久化空对象"，在重置流程里必须先清内存、让删除步骤把空对象一起删掉，才能做到存储零残留。

### ⚠️ 开发记录
- 测试发现并修复：`resetAllData` 原顺序（先删存储、后清内存）会让 `clearConfusions()` 把 `ys-confusions='{}'` 写回，localStorage 留下空壳 key——调整顺序后彻底清除。
- 测试发现并修复：app.js 加载期 `window.matchMedia(...)` 无保护，jsdom 环境直接抛错；真实浏览器均有该 API，但旧 WebView 缺失时会中断启动——已加防御。
- jsdom 环境噪音（非应用问题）：`Window's scrollTo()` 未实现、发音桩 reject，均不影响判定。
- 补充（导出/导入回归）：新增 test-browser.js 第 8 节，端到端验证 `ys-confusions` 随备份导出、全量清空后导入可完整恢复（含方向计数 ba=2 精确一致），固化 9 项断言，防止导出/导入悄悄退化。

### 📌 下一步建议
- 有真实测验数据后评估权重参数（0.85 折扣 / 0.5 门槛 / 30 天半衰期 / 20% 对数上限 ≈ 40% 题目）
- 未来可在真机（Chrome/Edge/微信）上做一轮人工冒烟测试，确认 jsdom 无法覆盖的排版细节（模式栏 5 按钮在小屏是否换行、选项文字溢出）
- 若引入云同步，ys-confusions 需纳入同步清单（当前备份/导入已通过 ys- 前缀覆盖）

---

## 📅 2026-08-14 01:50 · v1.20.0

### 📝 一句话总结
动态出题权重（阶段3）：个人混淆对按"权重加权"进入辨析题、连续答对渐进降权、时间衰减持续生效，普通题与辨析题动态共存，五种测试模式共享同一套权重体系。

### 🎯 本次更新目标
- 为什么修改？阶段2 的易混词接入还是"固定概率"，没有真正让"用户越容易混的对 → 越被重点测试"。本阶段把出题变成行为驱动：权重高的混淆对更常考，已稳定区分的对逐渐少考但不消失。
- 解决了哪些问题？①辨析题按个人混淆权重加权选取（不再是固定 60% 概率）；②连续答对 → 权重渐进下降（0.85^连续答对次数），答错 → 权重回升（次数+1、连续答对清零）；③时间衰减持续生效，长期未出现的对退出题位但保留存储；④防止单一超高权重对霸占整局（每对最多 2 题、每词一局最多进一个辨析对、辨析题 ≤⌊题数×20%⌋ 对 ≈ 40% 题目）；⑤系统词形相似（预设候选）只作干扰项来源，不参与辨析题选位。
- 目标体验：测验随用户真实混淆情况自适应——总混淆的组反复辨析，已区分的组自然淡出，普通随机测试始终保留。

### ✨ 本次更新
- `confusion.js`：
  - 权重公式：`次数 × 时间衰减（30 天半衰期）× 0.85^连续答对次数`；连续答对折扣从 0.8 调整为 0.85（更渐进）
  - 新增 `CONFUSION_ACTIVE_MIN_WEIGHT = 0.5`：个人混淆对权重低于该值 → 退出辨析题位（仍保留存储，重新混淆即恢复）
  - 新增 `getActivePersonalPairs(bookId)`：次数 ≥2 且权重 ≥0.5 的"可进入辨析题"的个人混淆对
- `quiz.js`：
  - 用 `planQuizSlots` 取代固定近邻插入：辨析对数量 = min(⌊N×20%⌋, 词池内活跃个人混淆对数)，没有活跃对 → 全是普通题（不强行凑比例）
  - `pickWeightedPairs`：按权重加权随机选取辨析对（权重高 → 更容易被选中）；已选对涉及的词自动从候选中排除
  - `placePairWords`：每对两词间隔 2~3 题放置（不机械相邻）
  - `buildQuizOptions` 注入概率动态化：有个人混淆记录 → 0.8；仅预设候选 → 0.4；无 → 0（原来是固定 0.6）
  - 题目新增 `contrastKeys`：记录实际进入选项的易混伙伴；`quizAnswer` 答对时对其逐对 `recordConfusionResolved`（渐进降权），答错照旧 `recordConfusion`（权重回升）
  - 听写模式无选项，靠辨析对近邻出现形成对照；五种模式共用同一套规划与权重
- `scripts/test-quiz.js`：新增 K~R 共 24 项阶段3 断言（权重公式、插槽、加权采样统计、连续答对接线、霸占防护、时间衰减、听写共享、多教材隔离）

### 👤 用户体验变化
- 修改前：易混词进入干扰项是固定概率，与用户实际混淆情况无关；混淆对的"考不考"基本靠运气。
- 修改后：①总把 싸다 答成 사다 → 这两个词会越来越频繁地一起出现（间隔 2~3 题，不机械相邻）；②连续答对 5 次 → 这组权重降到约 44%，逐渐少考；③一旦又答错 → 权重回升，重新重点考；④一个月没碰 → 权重衰减，不再占用辨析题位，但不会从系统里消失。
- 学习流程更自然：普通随机测试始终保留（辨析题最多占约 40% 题目，即 ≤⌊题数×20%⌋ 对），用户不会被易混词淹没。

### 💭 设计思考
- 为什么权重 = 次数 × 时间衰减 × 连续答对折扣？三要素分别对应"历史混淆程度""最近是否活跃""最近是否已区分"，行为数据驱动的动态调整，而不是拍一个固定比例。
- 为什么辨析题上限约 40% 题目（≤⌊题数×20%⌋ 对）？"普通测试 + 易混辨析测试"共存，辨析是增强不是替代；上限 + 每对最多 2 题 + 每词一局只进一个对，三重防霸占。
- 为什么权重低于 0.5 就退出题位、而不是删除？"逐渐降低出现频率，但不要突然永久消失"——存储保留，用户一旦重新混淆，权重立刻回升。
- 为什么预设候选不参与辨析题选位？"只有系统自动发现、用户从未混淆过 → 保留为普通候选"——它们只作为选项干扰项来源（0.4 概率），不占题位、不赋予个人权重。
- 为什么答对只对"本题实际出现的伙伴"降权（contrastKeys）？只有真正同屏出现过、用户也确实没选错的配对，才是"已区分"的有效证据；没同屏出现过的不降权（保守）。

### ⚠️ 开发记录
- 加权采样用"不放回 + 已选词排除"：同一局内一个词最多进一个辨析对，避免 싸다 同时出现在多个配对里霸占题目。
- 统计验证：三对权重 4/3/2，300 局实测出现次数 243/214/143，严格按权重排序；单局辨析对数最大 2（上限生效）。
- 设置页"清除所有数据"暂未包含 ys-confusions，待阶段4 回归时一并处理。

### 📌 下一步建议
- 阶段4：全模式回归验证（移动端 UI、退出测验逻辑、历史记录、清空数据逻辑补全 ys-confusions）
- 有真实测验数据后评估：权重公式参数（0.85 折扣 / 0.5 门槛 / 30 天半衰期 / 20% 对数上限 ≈ 40% 题目）是否需要按实际体验调整
- 未来可考虑：辨析对答对后增加"短期冷却"（同对在当天不重复考），进一步降低重复感

---

## 📅 2026-08-14 01:42 · v1.19.0

### 📝 一句话总结
易混词底层能力接入测验（阶段2）：四种测试题目携带词 key、选择题优先注入易混词干扰项、答题 A→B 与听写实际输入词记录混淆（不改变判分），并新增「听音→韩语」测试方向，原「听音→中文」保留。

### 🎯 本次更新目标
- 为什么修改？阶段1 建立了易混词/混淆关系层，本阶段把它接入测验出题与判分：让"能不能区分易混词"真正进入每次测验。
- 解决了哪些问题？①题目内部携带正确答案词 key 与选项词 key，判分时知道用户实际选了哪个词；②选择题（韩→中/中→韩/听音→中文/听音→韩语）优先用易混词作干扰项；③用户把 A 答成 B 时记录定向混淆；④听写记录"实际输入词 → 正确词"的混淆，且不改变原有判分（含"很接近"被判对的情况）；⑤新增「听音→韩语」方向，从相似发音/词形中辨认；⑥原「听音→中文」原样保留。
- 目标体验：测验不再只测"认不认识"，而是逐步积累"这个用户到底混淆哪些词"，为阶段3 的动态加权出题提供行为数据。

### ✨ 本次更新
- `confusion.js`：
  - 新增 `getConfusionPartners(key)`：某词的全部易混伙伴，排序 = 个人混淆权重优先 → 预设候选 jamo 相似度近优先
  - 新增韩文 jamo 相似度：紧音/送气音同组（ㅅㅆ、ㄱㄲㅋ、ㄷㄸㅌ、ㅂㅃㅍ、ㅈㅉㅊ）替换记 0.5，其余替换/插入删除记 1——让 싸다 的伙伴排序中 사다（紧音对 ㅆ/ㅅ）排在 있다/가다 之前，避免干扰项被无关同形词淹没
  - `findNearestWordKey` 增加同距离时"等长优先"（避免 비사다 这类少写一个字的输入被误配到别的短词）
- `quiz.js`：
  - 题目对象携带 `targetKey` / `optionKeys`（选项与词 key 同步打乱，`correctIdx` 跟随）
  - `buildQuizOptions`：易混词干扰项优先注入（60% 概率、最多 2 个，个人混淆/预设候选按排序取），随机补齐 + 显示去重（同形词/同义词不会出现重复选项）；普通随机题路径保留
  - `applyNearbyContrast`：同一易混组的词在题目序列中靠近出现（间隔 2~3 题、40% 概率），不强制机械相邻；听写题也受益（无选项，靠邻近对照）
  - `quizAnswer`：答错且实际选了另一个词 → `recordConfusion(target, selected)`（无关选项只记错题，不产生混淆）
  - `dictSubmit`：输入命中了另一个"词形相似"的真实词（距离 ≤1 且比正确词更接近该词）→ 记录混淆，**不改变原判分**（含"很接近"算对的场景）
  - 新增「听音→韩语」（listen-kr）模式分支：听发音 → 选项为韩语词形；模式提示、历史标签同步
- `index.html`：模式栏新增「🔊 听词」按钮（data-mode="listen-kr"）
- 新增 `scripts/test-quiz.js`：阶段2 集成验证（最小 DOM 桩驱动真实判分路径），35 项断言全绿

### 👤 用户体验变化
- 修改前：测验选项全部随机，几乎不会出现易混词；答错只记"错了"；没有"听发音选韩语词"的辨析模式。
- 修改后：①考 싸다 时选项里优先出现 사다（买）等相似词的中文/韩语，逼用户区分；②把 사다 答成 싸다 这类错误开始被系统识别为"定向混淆"；③听写打出 사다（该写 싸다）会被记录，但判分规则不变；④新增「听音→听词」模式，专门练相似发音辨认。
- 学习流程更自然：普通随机测试完全保留，易混词只是"加进来当干扰项"，不喧宾夺主。

### 💭 设计思考
- 为什么题目要携带词 key？只有知道"用户选的选项对应哪个词"，才能还原"A 答成 B"，这是混淆记录的数据基础。
- 为什么混淆记录不改判分？判分是既定学习标准（正确/很接近/错误），混淆是诊断信息——两者分离，避免为了诊断牺牲既有体验。
- 为什么"听音→韩语"作为新模式而非替换"听音→中文"？两个方向测的能力不同：前者测"听音辨形"（区分 싸다/사다），后者测"听懂意思"（韩语→中文理解）。用户明确要求都保留。
- 为什么 60% 概率注入、上限 2 个？"易混词只是增加辨析能力"——普通随机题必须继续存在；注入概率与上限让辨析成分有存在感但不主导。
- 实现中发现并修正：单纯按编辑距离排序时，싸다 有 94 个"距离 1"的候选伙伴（所有共享 다 结尾的两字词），导致注入的是 있다/가다 而非 사다——引入韩文 jamo 相似度（紧音/送气音对折扣）后，真正的极近似词（사다 0.5）排在前面。候选集本身不变（仍全部保留），只影响排序。

### ⚠️ 开发记录
- 听写"很接近"的混淆记录：如正确词 비싸다（3 字）、用户输入 싸다（距离 1）→ 仍判"很接近=正确"（原判分不变），同时识别为 비싸다→싸다 潜在混淆并记录；输入纯拼写错误（乱码）不匹配任何词，不记录。
- 同形词去重：쓰다（写/苦）等一课多词条的词，选项按显示文本去重，不会出现两个一模一样的选项。
- 设置页"清除所有数据"暂未包含 ys-confusions，待阶段4 回归时一并处理。

### 📌 下一步建议
- 阶段3：动态出题权重（普通题+易混辨析题混合比例、个人混淆对提高出现概率、连续答对降权）
- 阶段4：全模式回归验证（移动端 UI、退出测验逻辑、历史记录）+ 清空数据逻辑补全
- 有真实测验数据后评估：易混词注入概率 60%、jamo 相似度阈值、"距离≤1"听写阈值是否需要调整

---

## 📅 2026-08-14 01:18 · v1.18.0

### 📝 一句话总结
新增「易混词 / 混淆关系」测验底层能力（阶段1）：系统按词形相似自动发现候选易混词 + 用户 A→B 定向混淆独立记录，多教材隔离，测验行为暂不变，为后续易混辨析测验打地基。

### 🎯 本次更新目标
- 为什么修改？现有测验只能测"认不认识"，测不出"能不能区分易混词"（如 싸다/사다 单独看都认识、放一起就混）。随机抽词出题时，干扰项几乎不会出现相似词。
- 解决了哪些问题？①建立统一的易混词/混淆关系层（不针对单一测验模式写死逻辑）；②明确区分"系统判断词形相似"与"用户实际混淆行为"——自动发现只作候选，不直接加权；③严格区分"答错"与"把 A 答成 B"——只有实际选择/输入了另一个词形相似的词才记录定向混淆；④多教材（延世1/延世2）数据完全隔离。
- 目标体验：为测验提供底层能力——易混词进入干扰项、A→B 记录、动态加权，全部建立在这一层之上；本阶段不改变任何现有测验行为。

### ✨ 本次更新
- 新增 `confusion.js`：易混词/混淆关系层（纯逻辑、无 DOM、可 Node 加载）
  - 系统预设易混词（候选）：按"词形相似"规则从教材数据自动发现——拼写距离 ≤1 且等长，或 ≤2 且长度差 ≤1 且距离 < 较长词长度；不做语义判断（留待以后接 AI）。命中规则的组合全部保留为候选（不设每词伙伴上限，避免核心配对被挤掉）
  - 用户个人混淆关系：独立 localStorage key `ys-confusions`，与 `yonsei-study-data` 完全解耦；记录定向混淆 ab/ba 次数、最近时间、连续答对消除次数；达到活跃阈值（≥2 次）才作为个人易混关系
  - A→B 记录：仅当用户实际选择/输入了另一个"词形相似"的词才记录；完全无关的选项/输入只走现有错题逻辑，不产生混淆关系
  - 听写最近词匹配 `findNearestWordKey` + 距离 ≤1 疑似混淆判定（先按保守阈值，待真实数据评估）
  - 权重：定向次数 × 30 天半衰期时间衰减 × 连续答对 0.8 折扣
  - 多教材隔离：全部 key 为 `书ID|课号|韩语`，按书遍历、跨书不关联
- 新增 `scripts/test-confusion.js`：阶段1 验证脚本（vm 加载、无浏览器依赖），44 项断言全绿
- `index.html`：加载顺序加入 confusion.js（stats.js 之后、quiz.js 之前）
- `sw.js`：FILES 加入 confusion.js，CACHE hangul-v59 → hangul-v60
- `data-books.js`：APP_CONFIG.version → 1.18.0

### 👤 用户体验变化
- 修改前：测验只随机抽词，测不出易混词区分能力；答错只记"错了"，不知道是不是把 A 当成了 B。
- 修改后：本阶段为纯底层能力，测验行为暂无可见变化；答题数据开始在后台积累 A→B 混淆关系，为下一阶段的易混辨析测验做准备。

### 💭 设计思考
- 为什么自动发现而非手写易混词表？要求"通过现有单词数据与规则自动发现"，自动适配现有及未来教材（如延世3）；v1 不做语义相似判断（需真正理解词义）。
- 为什么候选与个人混淆分开？"系统觉得像"≠"用户真的混"——自动发现的相似词只作候选，只有用户实际反复出现 A→B 并达到活跃阈值，才成为个人易混关系用于动态出题。宁可保守，避免词形相似产生大量错误的个人混淆关系。
- 为什么独立存储 ys-confusions？与现有用户数据（yonsei-study-data / quiz-history 等）零接触，老用户启动无感，现有保存/加载逻辑一行不改。
- 遵循"做减法"：阶段1 不新增任何用户可见页面/UI，易错本等现有功能不动。
- 实现中发现的方案修正：原计划"每词最多 6 个伙伴"上限会把核心配对挤掉（사다 的伙伴被 사전/크다 等先占满，싸다↔사다 被截断出局），已改为不设上限、候选全保留，词伙选择交给消费端按优先级挑选。

### ⚠️ 开发记录
- 听写判分现有"很接近"宽容规则（距离≤1 且 ≥3 字算对）会把 사다/싸다 这类典型混淆算作"正确"——阶段1 的匹配层已能识别该输入，阶段2 接入判分时需把"很接近"同时作为混淆信号处理（不改变得分，只额外记录混淆）。
- 清空学习数据（设置页）暂未包含 ys-confusions，待阶段4 回归时一并处理。
- 词形相似规则的保守边界：两字词要求距离 ≤1（即至少一个字符相同），否则任意两字词两两成组产生大量无意义候选；쓰다(写)/싸다 距离 1 正常入选。若有真实数据证明阈值过严/过宽，再评估调整。

### 📌 下一步建议
- 阶段2：quiz.js 接入（题目携带选项词 key、判分后记录 A→B、新增"听音→韩语"方向）
- 阶段3：动态出题权重（普通题+易混辨析题混合、近邻对照、连续答对降权）
- 阶段4：全模式回归验证 + 清空数据逻辑补全
- 有真实测验数据后评估听写"距离≤1"阈值与两字词相似规则

---

## 📅 2026-08-14 00:48 · v1.17.0

### 📝 一句话总结
延世1 全十课例句按「已学词汇 + 活人感 + 不模板化」标准完成越级清理：L1/L2（144 词）例句全量重写为正式敬语对话风格，L3~L10 逐课移除未学词汇 60+ 处（제일/선물/과일/음식/연락/기분/핸드폰/냉면/이번 주/다음 달/약속 시간/도착/출발/여행지/오래/계획/모임/직원/호텔/세탁소 等），保留 방/부엌/건강/주말/정말 等超基础例外词；新增 scripts/audit-y1.js 越级词审计工具，check-examples 全部通过。

### 🎯 本次更新目标
- 为什么修改？落实「学到哪就用哪些」内容标准：Y1 第 n 课例句只能使用第 1~n 课已学词汇（+ 少量超基础例外词），原例句混入大量未学词（如 L3 用 L6 的 선물/음식、L5 用 L10 的 제일/출발、L7 用 L8 的 연락）。
- 解决了哪些问题？①第1课（56 词）例句全量重写：只用 L1 词汇语法（입니다/입니까/아닙니다/은는/이가 있다없다 + ㅂ니다），对话式/感叹/转折/否定修正，活人感风格；②第2课（88 词）例句全量重写：场所位置（에 있다/과와/위아래앞뒤옆안밖/여기거기저기）+ 位置问-答对话，替换全部越级旧例句；③第3~10课逐课清理越级词 60+ 处，保留超基础例外词（방/부엌/건강/형제/약/감기/손님/역/안전/주말/정말/같이/오늘/어제/내일 等）。
- 目标体验：学完延世1 每一课，看到的例句全部由已学词汇组成，且自然口语化、有对话感、不模板化。

### ✨ 本次更新
- 第1课（56 词）：例句全量重写（正式敬语 + 问-答对话 + 活人感风格），校验通过
- 第2课（88 词）：例句全量重写（场所位置活用 + 位置问-答），校验通过
- 第3课：清理 29 处越级词（과일/선물/음식/연락/기분/제일/핸드폰/냉면/이번 주/다음 달/생신/간호사/아파트/편지/목소리/벽/열심히/인터넷/호텔/세탁기/여행 等）
- 第4课：清理 23 处（제일/지난주/케이크/과일/선물/자리/고향/여행/접시/소풍/분위기/오래/예약 等）
- 第5课：清理 19 处（제일/잠깐만/출발/한복/열심히/약속/도착/동안/방학/결혼식/휴가/직접 等）
- 第6课：清理 15 处（제일/오른쪽/공항/직접/세탁소/옷걸이/호텔/정장/짐/오래/직원 等）
- 第7课：清理 6 处（약속 장소/구경/요금/공항/연락 等）
- 第8课：清理 9 处（요금/편지/에어컨/약속 시간/도착/동안/무료 等）
- 第9课：清理 5 处（오래/계획/모임/결혼식 等）
- 第10课：清理 5 处（여행지/오래/도시/직원 等）
- 新增 scripts/audit-y1.js：Y1 每课越级词审计工具（substring 匹配 + 来源标注 + 人工复查提示，可指定课号）

### 👤 用户体验变化
- 修改前：Y1 例句混入未学词汇（如 L3 出现 선물/음식、L5 出现 제일/출발），学习者会遇到看不懂的句子。
- 修改后：Y1 全十课 563 词例句严格贴合学习进度，逐课只使用已学词汇语法；L1/L2 为正式敬语对话风，L3~L10 保持原句风格仅替换越级词。

### 📌 下一步建议
- 语法例句按同标准核对（本次聚焦单词例句）
- 后续新增内容继续用 scripts/audit-y1.js 把关
- 越级词审计支持活用形匹配（如 맵다→매워요）可作后续增强

---

## 📅 2026-08-14 00:04 · v1.16.5

### 📝 一句话总结
延世2 最后三课（병원 59 + 여행 57 + 집안일 64，共 180 词 × 4 句）例句全手写，30 个遗留 3 句词全部扩到 4 句并清空 LEGACY_THREE —— **延世2 十课（629 词）手写覆盖率 100% 收官**。语法随课全部解锁：L8 -기 위해서/아무/달라고，L9 밖에/-던/만큼，L10 -을수록/덕분에/처럼/-으면서。

### 🎯 本次更新目标
- 为什么修改？延续「已学词汇 + 已学语法 + 句式多样化」标准，完成延世2 最后一程；同时清掉 30 个 v1.15.1 旧标准遗留的 3 句词。
- 解决了哪些问题？①第8课（병원）：就诊/买药/各科场景，-기 위해서（치료를 받으러... 未使用但可用）、-지 말고（약을 많이 먹지 말고）、아무、달라고（진단서를 써 달라고 했어요）落地，遗留 16 词扩 4 句；②第9课（여행）：订票/住宿/观光场景，밖에/-던/만큼（기대가 되는 만큼 실망도 커요）落地，遗留 3 词扩 4 句；③第10课（집안일）：搬家/打扫/洗衣场景，-을수록/덕분에/처럼/-으면서 全部可用，遗留 11 词扩 4 句。
- 目标体验：学完延世2 就能就医买药、订票旅行、搬家打扫洗衣服，十课累计 629 词 × 4 句全部是学过的词汇语法组成的真实对话。

### ✨ 本次更新
- 第8课（병원 59 词）：병원/의사/환자/진료/주사/처방전/수술/각科 —— 遗留 16 词扩 4 句 + 43 新词
- 第9课（여행 57 词）：여행지/예매/호텔/민박/일출/추억 —— 遗留 3 词扩 4 句 + 54 新词
- 第10课（집안일 64 词）：이사/계약/청소/세탁/빨래 —— 遗留 11 词扩 4 句 + 53 新词
- LEGACY_THREE 清空（45 → 0），check-examples 不再有遗留 3 句词
- 越级修正：L8 遗留句 힘들어요 → 어려워요（힘들다 为 L10 词）
- 修复 check-examples.js 编码事故（git 恢复 + 重做移除）

### 👤 用户体验变化
- 修改前：延世2 第8~10课例句是生成器病句或 3 句旧标准。
- 修改后：延世2 全十课 629 词例句 100% 手写，语法覆盖随课推进到全解锁，无遗留 3 句词。

### 📌 下一步建议
- 按同标准重写延世1 全部 10 课（563 词）：重点是 L1/L2 的 117 个生成器词 + 逐课核对越级表达与句式重复
- 重写后同样逐课提交 + CHANGELOG + 版本同步，最终延世1/延世2 双双 100%

---

## 📅 2026-08-13 23:48 · v1.16.4

### 📝 一句话总结
延世2 第4~7课例句全手写收官：초대(60)+교통(57)+공공기관(78)+전화(58，含 15 个遗留 3 句词扩到 4 句并移出 LEGACY_THREE) 共 253 词 × 4 句，全部按「已学词汇 + 已学语法 + 句式多样化」标准；语法随课推进：L4 用 반말（-자/-을지/반말형），L5 用 -려면/-아어 보이다/-다가，L6 用 -에 대해/-는 동안/-어 있다/-거나，L7 用间接引语（-다고 하다）/ -을래 / -게 되다。

### 🎯 本次更新目标
- 为什么修改？延续 v1.16.2/1.16.3 确立的标准，把延世2 后半部分逐课补齐；L7 的 15 个「每词 3 句」遗留词（v1.15.1 旧标准产物）统一扩到 4 句。
- 解决了哪些问题？①第4课（초대）：邀请/聚会/婚礼场景，반말 语法落地（뒤풀이에 같이 가자 / 노래를 부르자），생일 파티/결혼식/축의금/예식장 全流程；②第5课（교통）：乘车/换乘/问路场景，-려면（부자가 되려면→修正为 -고 싶으면 因越级）、-다가（쭉 가다가 역이 있어요）合理使用，KTX/막차/환승역 等交通词落地；③第6课（공공기관）：图书馆/银行/邮局/出入境场景，-에 대해/-는 동안/-어 있다（책이 놓여 있어요）等 L6 语法；④第7课（전화）：通话/短信/预约场景，间接引语（진단서를 써 달라고 했어요）、-을래（음악회에 같이 갈래?）落地。
- 目标体验：学完第4~7课就能邀请朋友、坐车问路、跑银行邮局、打电话发短信。

### ✨ 本次更新
- 第4课（초대 60 词）：동아리/송별회/집들이/결혼식/축의금/예식장/돌잔치/한턱내다 等 —— 반말(자) + -을까 + -을지 落地
- 第5课（교통 57 词）：버스/지하철/KTX/무궁화호/환승역/막차/노선도/보행자 통행금지 等 —— -다가(2处) + -려면 谨慎使用（发现越级后修正）
- 第6课（공공기관 78 词）：도서관/환전/송금/우체국/택배/비자/외국인등록증 等 —— -에 대해(2处) + -는 동안(3处) + -어 있다(놓여 있어요)
- 第7课（전화 58 词）：15 个遗留 3 句词全部扩到 4 句并移出 LEGACY_THREE（遗留 45→30）；통화하다/문자 메시지/벨소리/진동/초대권/콘서트/출장 等 43 词新增 —— -을래(음악회에 같이 갈래?) + 间接引语(달라고)

### 👤 用户体验变化
- 修改前：第4~7课例句是生成器病句或 3 句旧标准，越级词混杂。
- 修改后：253 词 × 4 句全部是学过的词汇语法组成的真实对话，且每课语法随进度解锁（반말→-려면→-에 대해→间接引语），学一课巩固一课。

### 📌 下一步建议
- 延世2 第8课（병원 59 词，含 16 个遗留 3 句词）+ 第9课（여행 57 词，含 3 个遗留）+ 第10课（집안일 64 词，含 11 个遗留）：-기 위해서/-지 말고/아무(L8)、밖에/-던/만큼(L9)、-을수록/덕분에/처럼(L10) 将依次解锁
- 全部完成后按同标准重写延世1

---

## 📅 2026-08-13 23:26 · v1.16.3

### 📝 一句话总结
延世2 第2课（한국 음식 64 词）+ 第3课（시장 74 词）例句全手写收官：138 词 × 4 句全部用「已学词汇 + 已学语法」，第2课紧扣 L2 语法（-아/어 보다/-(으)ㄴ 적이 있다/-아/어야 하다/-아/어도 되다/-으면 안 되다），第3课用上 L3 语法（-을까/-어도/-기는 하지만），全部是能直接用的点菜/做饭/逛街对话。

### 🎯 本次更新目标
- 为什么修改？延续 v1.16.2 确立的标准：例句必须与学习进度匹配（只用 Y1 全部 + 到本课为止的词汇语法），且实用。第2、3课此前 0 手写，例句全是生成器病句（「새 설렁탕을 샀어요」「닭갈비를 빌릴 수 있을까요」级别）。
- 解决了哪些问题？①第2课 64 词：菜名配点菜/尝味场景（먹어 본 적이 있어요?/맛봐도 돼요?），烹饪动词配真实做饭动作（냄비에 물을 넣고 끓여요/간을 맞추려고 소금을 넣어요），口味形容词配家常评价（이 국은 너무 짜요/싱거우면 소금을 조금 넣어요）；②第3课 74 词：购物全流程（시장에서 과일을 샀어요 → 카드로 계산해요 → 배달이 왔어요），衣服/颜色/花纹用 L3 新语法（입어 봐도 돼요?/좀 비싸도 괜찮아요/비싸기는 하지만 정말 좋아요）。
- 目标体验：学完第2课就能点菜、评价味道、问"吃过吗"；学完第3课就能逛街、挑颜色花纹、试穿、结账、砍价。

### ✨ 本次更新
**延世2 第2课（한국 음식 · 64 词全手写）**
- 菜名 16 词：설렁탕/닭갈비/김치찌개/비빔밥/삼계탕/갈비탕/순두부찌개/잡채/우동/스파게티/셀러드/튀김/짬뽕/갈비/반찬 —— -(으)ㄴ 적이 있다（먹어 본 적이 있어요?）、-아/어 보다（먹어 봤어요?）、-으면（추운 날에 먹으면 따뜻해요）
- 调味 3 词：된장/고추장/간장 —— 传统/颜色/味道（된장찌개는 된장으로 끓여요）
- 烹饪动词 16 词：넣다/말다/비비다/찍다/덜다/뿌리다/굽다/튀기다/볶다/끓이다/삶다/찌다/다지다/썰다/젓다/맛보다 —— 做饭全流程 + -아/어야 하다（잘 젓어야 돼요）、-아/어도 되다（맛봐도 돼요?）
- 口味形容词 5 词：맵다/달다/짜다/싱겁다/구수하다 —— -으면 안 되다（매운 음식을 너무 많이 먹으면 안 돼요）
- 餐具/餐桌 10 词：냄비/식탁/밥그릇/국그릇/숟가락/젓가락/접시/컵/그릇/상차림
- 餐桌礼仪 3 词：식사 예절/윗사람/편안하다
- 其他 8 词：요리하다/재료/간/간을 맞추다/우선/먼저/유명하다/건강/전통/오래/냄새/벽（12 词）

**延世2 第3课（시장 · 74 词全手写）**
- 场所 5 词：시장/백화점/슈퍼마켓/가게/매장 —— 市场比价（시장 물건이 백화점보다 싸요）
- 服装 12 词：옷/정장/한복/양복/원피스/치마/바지/티셔츠/와이셔츠/반바지/구두 —— -아/어 보다（입어 봐요）、-아/어도 되다（입어 봐도 돼요?）
- 颜色 8 词：색깔/하얀색/까만색/노란색/빨간색/분홍색/하늘색/파란색 —— 配色搭配（하얀색과 검은색이 잘 어울려요）
- 花纹 5 词：무늬/꽃무늬/줄무늬/체크무늬/물방울무늬 —— 试穿/流行（체크무늬가 유행해요）
- 用品 13 词：가방/배낭/냉장고/텔레비전/휴지/비누/주스/봉투/카드/향수/선물/꽃다발/와인/장갑/넥타이（15 词）
- 购物动词 9 词：사다/고르다/찾다/계산하다/주문하다/배달하다/세일하다/어울리다/마음에 들다 —— 结账/送餐全流程（카드로 계산해요/배달이 왔어요）
- 穿鞋/流行 3 词：입다/신다/유행하다
- 形容词 10 词：비싸다/싸다/가볍다/시원하다/편하다/불편하다/단순하다/복잡하다/귀엽다/멋있다/하얗다 —— L3 新语法（-기는 하지만：비싸기는 하지만 정말 좋아요；-어도：좀 비싸도 괜찮아요）
- 人物/其他 7 词：손님/주인/점원/직접/주소/고기/라면

**说明（厨房基础词最小例外）**：第2课烹饪动词（다지다/썰다 等）需要宾语，但教材只给了动词没给食材词。为让例句能写出来，引入最小厨房基础词集 6 个：고기/감자/파/마늘/소금/설탕（均为超基础生活词，下一课即可正式学到），如不需要可再调整。

### 👤 用户体验变化
- 修改前：第2、3课例句是生成器病句（「새 설렁탕을 샀어요」「닭갈비를 빌릴 수 있을까요」），且每次翻卡例句随机变。
- 修改后：点菜、做饭、逛街、试穿、结账的例句全部是学过的词汇语法组成的真实对话，第2课用 L2 语法、第3课用 L3 语法，学一课巩固一课。

### 💭 设计思考
- 为什么第2课语法用 L2、第3课用 L3？例句即复习：第2课的「먹어 본 적이 있어요」「맛봐도 돼요」正好巩固本课新语法；第3课的「좀 비싸도 괜찮아요」「비싸기는 하지만」同理。
- 为什么引入 6 个厨房基础词例外？「实用优先」优先于「严格词汇表」：烹饪动词没有宾语写不出自然句，而这几个词是任何韩语学习者在第一年必然高频接触的日常词，属可接受的最小越级。

### 📌 下一步建议
- 延世2 第4课（초대 60 词）按同标准推进：`node scripts/dump.js yonsei2 4` 先出病句清单
- 第4课起语法可用 L4（반말/청유/명령），例句可引入朋友间口语，注意和敬语体区分
- 长线：把「越级词/越级语法检查」固化为 check-examples.js 的正式检查项

---

## 📅 2026-08-13 23:10 · v1.16.2

### 📝 一句话总结
延世2 第1课例句按「已学词汇 + 已学语法」标准全面重写：例句只用延世1 全部 10 课 + 延世2 第1课学过的词汇与语法，清掉约 40 个越级词（승무원/예절/면접/논문/대학원/유명하다/박물관/칭찬 等）和 6 条越级语法（-(으)려면/-기 위해서/-을수록/덕분에/-아어 보이다/-고 나서），全部换成实用日常句。

### 🎯 本次更新目标
- 为什么修改？确立例句质量新标准：例句必须与学习进度匹配——只用学过的词汇 + 学过的语法（到延世2 第1课为止 = 延世1 全部 + 第1课 8 条语法），且优先实用。初版第1课例句虽然语法正确、场景完整，但混入了未学的中级词汇和延世2 第2课以后才教的语法，学习者会在例句里"撞见"没学过的内容，违背渐进学习原则。
- 解决了哪些问题？①词汇：清掉约 40 个越级词，改用已学词替换（승무원/예절/교육 → 일하다/비행기/취직；면접/논문/대학원 → 준비/공부；유명하다 → 좋다；마을/곳 → 시골 等）；②语法：-(으)려면（第5课）→ -(고) 싶으면，-기 위해서（第8课）→ -(으)려고，-을수록（第10课）→ 가까운/먼 形容词，덕분에（第10课）→ -아서，-아어 보이다（第5课）→ -ㄴ데，-고 나서（未教）→ -고；③例句更口语实用（「질문이 있으면 언제든지 물어보세요」「피곤할 때는 잠을 자세요」）。
- 目标体验：学习者打开第1课卡片，例句里出现的每一个词、每一条语法都是自己学过的——复习巩固，而不是提前剧透。

### ✨ 本次更新
**延世2 第1课（소개）例句全面重写（58 词 × 4 句）**
- 公司/单位 6 词：무역 회사/신문사/여행사/증권 회사/항공사/잡지사 —— 일하다 + -(으)ㄴ 지/-(으)려고/-기 때문에 全落地（다니다 → 일하다 统一）
- 职位/专业 7 词：과장/전공/경제학/경영학/국어국문학/역사학/의학 —— 去定义句改口语（국어국문학 → 「전공해서 한국어 선생님이 되고 싶어요」）
- 学校/升学就业 5 词：대학교/고등학교/졸업하다/입학하다/취직하다 —— 졸업→입학→취직链（-기 위해서 → 졸업하려고）
- 同学/聚会 2 词：동창/모임 —— 「동창 모임에 갔는데 정말 재미있었어요」
- 家庭/亲戚 13 词：친척/부모님/형제/사촌/육촌/큰아버지/작은아버지/고모/이모/외삼촌/조카/촌수/사촌 동생 —— 称谓定义用已学词（큰아버지는 아버지의 형이에요）；-을수록 → 가까운/먼
- 故乡/风景 7 词：고향/시골/도시/관광지/유적지/바닷가/경치 —— -아지다 落地（마음이 편해져요）；관광지 「제일 좋은 관광지예요」
- 动词 7 词：소개하다/배우다/사귀다/익숙해지다/다녀오다/부러워하다/슬퍼하다 —— -아하다 落地（부러워해요）；「좋은 친구를 사귀고 싶으면 먼저 인사하세요」
- 形容词/副词 7 词：아름답다/맑다/친절하다/피곤하다/복잡하다/언제든지/열심히 —— 「피곤하신데 괜찮아요?」「맑은 날에는 기분이 더 좋아요」
- 问候语 2 词微调：말씀 많이 들었습니다/잘 부탁합니다 第4句换已学词（과장님/새 회사）

### 👤 用户体验变化
- 修改前：第1课例句混入未学词汇（승무원/예절/논문/유명하다…）和未学语法（-(으)려면/-기 위해서…），学习者会提前"剧透"。
- 修改后：例句 100% 由已学词汇（延世1 全 10 课 + 第1课）+ 已学语法构成，学第1课时例句里没有陌生内容，全部可用于真实自我介绍对话。

### 💭 设计思考
- 为什么"已学词汇 + 已学语法"是硬标准？韩语学习者的记忆负担主要来自"生词 × 新语法"的乘积；例句若引入未学内容，等于每张卡片偷偷加料。延世1 例句（第3~10课手写版）就是这个口径——本课词汇 + 已学语法。
- 实用优先的取舍：个别词的例句信息量降低（如 촌수 「촌수가 가까운 친척과 자주 만나요」），但换来的是学习者真正能开口用的句子；定义式长句（如「국어국문학은 … 학과입니다」）全部改为场景句。

### 📌 下一步建议
- 用相同标准推进延世2 第2课（한국 음식）：`node scripts/dump.js yonsei2 2` 出病句清单后手写
- 可考虑把「越级词/越级语法检查」做成 check-examples.js 的正式检查项（当前为人工审核 + 抽查脚本）

---

## 📅 2026-08-13 22:50 · v1.16.1

### 📝 一句话总结
例句审核工具升级 + 延世2 第1课例句手写收官：dump.js 泛化为任意书任意课，check-examples.js 把 697 个生成器兜底词纳入校验并新增「每课手写覆盖率」报告；延世2 第1课（소개 自我介绍）58 词全部手写 4 句例句，紧扣本课 8 个语法点，4 个问候语由遗留 3 句扩到 4 句。

### 🎯 本次更新目标
- 为什么修改？项目审计发现：延世2 有 580 词（92%）例句完全依赖模板生成器，而生成器对中级词汇（무역 회사/촌수/상차림 等）会产出病句（「새 무역 회사를 샀어요」「어제 증권 회사를 잃어버렸어요」「촌수가 되고 싶어요」）；且 check-examples.js 只检查手写词，697 个生成器词完全绕过质量门禁，手写工程没有进度可见性。
- 解决了哪些问题？①dump.js 泛化：`node scripts/dump.js <书> <课>` 即可 dump 任意书任意课的生成器例句（开工前审核病句清单），替换写死课号的 dump9/dump10 临时脚本；②check-examples.js 扩展：无手写词的生成器输出抽样 3 次过结构检查（词组名词化 / 裸자모 / 어제·오늘·내일+에 / 无输出），并输出每课手写覆盖率报告；③延世2 第1课 54 个无手写词全部手写 4 句 + 4 个问候语从遗留 3 句扩到 4 句。
- 目标体验：学延世2 第1课看到的例句都是真实的自我介绍对话（公司/专业/升学/家庭/故乡），而不是生成器病句；手写工程逐课推进时，每课进度在校验报告里一目了然。

### ✨ 本次更新
**工具（scripts/）**
- 新增 `scripts/dump.js`：`node scripts/dump.js <bookId> <lessonNum> [--all|--hand]`，默认只输出无手写词的生成器例句（审核工作流），`--all` 全词总览，`--hand` 只看手写词；删除临时脚本 dump9.js / dump10.js
- check-examples.js 扩展：生成器兜底词审计（无 w.ex 的词抽样 3 次过结构检查）+ 每课手写覆盖率报告（手写 X/共 Y + 待手写 N 词，不达标仅提示不判失败）；沙箱加载 utils.js
- LEGACY_THREE 移除 yonsei2|1|* 4 词（已扩到 4 句），遗留 49→45

**内容（延世2 第1课 소개 · 58 词全手写）**
- 公司/单位名词 6 词：무역 회사/신문사/여행사/증권 회사/항공사/잡지사 —— 就业场景 + -(으)ㄴ 지（다닌 지 3년）、-(으)려고（취직하려고）、-기 때문에（다니시기 때문에）
- 职位/专业 7 词：과장/전공/경제학/경영학/국어국문학/역사학/의학 —— 自我介绍（제 전공은 경제학입니다 / 역사학을 공부하려고 박물관에 자주 가요）
- 学校/升学就业 5 词：대학교/고등학교/졸업하다/입학하다/취직하다 —— 入学→毕业→就职链 + -(으)ㄴ 지
- 同学/聚会 2 词：동창/모임 —— 同学聚会（동창 모임에 갔는데 반가운 얼굴들이 많았어요）
- 家庭/亲戚 13 词：친척/부모님/형제/사촌/육촌/큰아버지/작은아버지/고모/이모/외삼촌/조카/촌수/사촌 동생 —— 称谓全落地（큰아버지는 아버지의 큰형입니다）
- 故乡/风景 7 词：고향/시골/도시/관광지/유적지/바닷가/경치 —— 故乡介绍 + -아지다（마음이 편해져요 / 기분이 좋아졌어요）
- 动词 7 词：소개하다/배우다/사귀다/익숙해지다/다녀오다/부러워하다/슬퍼하다 —— -아하다（부러워해요 / 슬퍼해요）、-(으)ㄴ 지（사귄 지 1년）
- 形容词/副词 7 词：아름답다/맑다/친절하다/피곤하다/복잡하다/언제든지/열심히 —— -든지 落地（언제든지 물어보세요）
- 问候语 4 词：처음 뵙겠습니다/말씀 많이 들었습니다/잘 부탁합니다/오래간만입니다 —— 遗留 3 句扩到 4 句，加入 -겠군요（요즘 많이 바쁘시겠군요）

### 👤 用户体验变化
- 修改前：延世2 第1课单词例句经常是生成器病句（「새 무역 회사를 샀어요」「증권 회사를 잃어버렸어요」「촌수가 되고 싶어요」），且每次翻卡例句随机变化；手写工程没有进度可见性。
- 修改后：第1课每词 4 句都是真实的自我介绍对话，紧扣本课 8 个语法点；每课手写进度在 check-examples 覆盖率报告里逐课可见。

### 💭 设计思考
- 为什么先升级工具再手写？延世1 的经验（v1.15.5~v1.15.13）证明「dump 审核病句清单 → 逐词手写 → 回归校验」是最可靠的内容生产流程；先把校验范围从手写词扩展到生成器词，让未完成的课在报告中「可见」，手写工程才能逐课闭环。
- 为什么生成器词只做结构检查不做语义检查？语义病句（「새 무역 회사를 샀어요」）无法用规则自动识别，但结构问题（词组名词化 / 裸자모 / 无输出）可以；覆盖率报告负责把「语义质量由人审核」的工作量可视化。

### 📌 下一步建议
- 延世2 第2课（한국 음식 · 64 词）例句手写：`node scripts/dump.js yonsei2 2` 先出病句清单再逐词手写
- 逐课手写时同步把 LEGACY_THREE 中对应课的 3 句词扩到 4 句并移除
- 长线：生成器对同一词固定种子输出（消除翻卡随机例句）；延世2 语法卡补 notes 字段

---

## 📅 2026-08-12 01:04 · v1.16.0

### 📝 一句话总结
多教材架构全面打磨：**搜索 / 收藏 / 易错本 全局化**（延世1+延世2 一起搜、一起收、一起记薄弱词，卡片带教材标签），修复 Y2 单词卡溢出、书架卡尺寸不齐、语法卡误按压缩放、课程页副标题切书不更新等 8 处问题，导出升级为**全量备份**（两本书进度/测验/打卡/设置一键带走），一键清空加二次确认防误点。

### 🎯 本次更新目标
- 为什么修改？加入延世韩国语2 后暴露出一批"只认当前教材"的遗留逻辑：搜索只搜当前书、收藏页只显示当前书、易错本切到 Y2 就看不到 Y1 薄弱词；设置页"总词汇"只算当前书；导出/导入只带收藏与 SRS 进度，测验历史、打卡、连续天数、进度快照、当前教材全丢。
- 解决了哪些问题？①搜索全局：输入 한글/中文 命中两本书，结果带 연세1/연세2 课标签；②收藏全局：单词+语法跨书显示，按书分组的标题带教材标签（如 연세2 제3과），学习按钮可直接学任何一本书的收藏词；③易错本全局：Y1+Y2 薄弱词同屏，顶部"进入复习"一次复习两本书的薄弱词；④导出全量：测验历史、打卡、连续天数、每书进度快照、当前教材、主题/语速等设置全部打包；⑤导入覆盖前二次确认、清空所有记录二次确认。
- 目标体验：学延世2 时，延世1 的薄弱词不会"消失"；备份一个文件就能完整搬家。

### ✨ 本次更新
**全局化（跨教材）**
- 搜索：遍历全部教材，命中项显示 `연세N 제N과` 标签，学习上下文可跨书
- 收藏页：单词 + 语法跨书展示，取消收藏即时移除；**单词列表改为与易错本一致的扁平列表——每张卡片带「연세N 제N과」小标签**（不再用分组标题）；语法按 `书|课` 分组
- 易错本：跨书收集薄弱词，列表带书标签，"进入复习"覆盖两本书
- 收藏/易错本数据模型本就是全局的（key 带书前缀），本次把展示层对齐数据层

**修复**
- 延世2 长词卡溢出：`.word-item` 网格 `minmax(0,1fr)` + `min-width:0` + 换行兜底（root cause：`keep-all` + 网格最小宽度自动 ≥ 内容宽）
- 书架卡尺寸统一：所有卡 `min-height:64px`，标题/元信息单行省略；移除 📗/🔒 emoji 封面
- 语法卡去掉整卡按压缩放（只保留需要的点击反馈）
- 课程页副标题「延世韩国语1 ·」切书不更新 → 新增 `course-book-cn` 随书刷新
- 设置页"总词汇/掌握"改为全部教材合计
- 切书时一并清掉测验选课残留（quizSelectedLessons）

**数据**
- 导出：全量备份（`store` 收集所有 `ys-*` / `quiz-history` / `yonsei-study-data` 原始数据），带 `format` 版本号，兼容旧字段
- 导入：识别全量备份 → 二次确认 → 覆盖后整页刷新恢复；兼容旧格式备份
- 清除所有记录：二次确认（防止误点），清掉两本书全部数据后刷新

### 👤 用户体验变化
- 修改前：学延世2 时，延世1 的搜索词搜不到、收藏和易错本"消失"；设置页总词汇只有 629 而非 1192；导出备份不带测验历史/打卡，换设备要重新学。
- 修改后：两本书是一个整体——一个搜索框搜全部、一个收藏页管全部、一个易错本记全部；备份一个文件 = 完整搬家。

---

## 📅 2026-08-12 00:36 · v1.15.13

### 📝 一句话总结
延世韩国语1 第10课（휴일 假日）56 词例句全手写（아프다 沿用原例句）：假日/假期名词、计划/时间名词、节日/活动名词、旅行/交通/行程名词、动词、形容词、副词全补齐每词 4 句，紧扣本课四大语法点「-아/어 보다」「-(으)ㄴ 적이 있다/없다」「-네요」「-(으)면 좋겠다」，放假→定计划→订票→出发→观光→买纪念品→返程全套场景张口就来。**至此延世韩国语1 全部 10 课例句手写完成。**

### 🎯 本次更新目标
- 为什么修改？第10课除 아프다 外 56 词此前全走生成器，dump 审核确认：假日/假期名词全部掉进「物件」模板（「새 휴일을 샀어요」「여름방학을 선물로 받았어요」）；旅行名词全废（「새 표를 샀어요」「체크인을 어떻게 사용해요」「여행사를 선물로 받았어요」）；词组被整词名词化（「계획을 세우다를 자주 합니다」「(사진을) 찍다를 열심히 했습니다」）；动词全中「처음 ...했습니다 때」语法病；形容词主语错（「요즘 날씨가 복잡합니다」「여기 분위기가 필요합니다」）；副词语义错（「제일 책을 읽어요」「너무 운동을 해요」）。
- 解决了哪些问题？56 词全部手写、每词 4 句，全部换回假期/旅行主题 + 本课语法：-아/어 보다（연휴에 제주도에 가 봤어요 / 겨울방학에 스키를 타 봤어요）、-(으)ㄴ 적이 있다/없다（한국을 방문한 적이 있어요）、-네요（여기 관광객이 정말 많네요 / 공항이 정말 크네요）、-(으)면 좋겠다（방학이 빨리 왔으면 좋겠어요 / 설날 연휴가 길면 좋겠어요）。
- 目标体验：学完第10课就能用韩语安排假期——定计划、订票订房、出发返程、观光购物、感叹所见。

### ✨ 本次更新
**词汇例句（第10课 56 词，全手写）**
- 假日/假期名词 7 词：휴일/공휴일/연휴/방학/봄방학/여름방학/겨울방학 —— 愿望表达（방학이 빨리 왔으면 좋겠어요 / 설날 연휴가 길면 좋겠어요）
- 计划/时间名词 5 词：계획/계획을 세우다/일정/동안/초 —— 假期计划链（여행 계획을 세웠어요 / 학기 초에 계획을 세웠어요）
- 节日/活动名词 5 词：설날/졸업식/구경/방문/활동 —— 春节·毕业·参观（설날에 떡국을 먹어요 / 한국을 방문한 적이 있어요）
- 旅行名词 14 词：휴가/관광/관광지/관광객/가이드/여행사/비자/숙박/표/짐/기념품/체크인/체크아웃/준비물 —— 行程链（표를 예매해요 → 체크인해요 → 기념품을 사요 → 체크아웃해요）
- 交通/行程名词 7 词：공항/왕복/편도/출발/도착/학기/고향/방법 —— 出行（왕복이 편도보다 비싸요 / 공항에 2시간 일찍 가야 해요），공항 生成器合格句保留
- 动词 8 词：끝나다/쉬다/(사진을) 찍다/가지다/예약하다/이용하다/예매하다/환전하다 —— 旅途动作（여기서 사진을 찍어도 돼요 / 공항에서 환전할 수 있어요）
- 形容词 3 词：복잡하다/편하다/필요하다 —— 拥挤·方便·需要（연휴에는 공항이 복잡해요 / 한국에 가려면 비자가 필요해요）
- 副词 5 词：그러니까/아니면/제일/주로/너무 —— 因果·选择·程度（그러니까 표를 빨리 예매해요 / 비행기로 갈 거예요, 아니면 기차로 갈 거예요）
- 其他 2 词：인터넷/가지다 —— 网络操作（인터넷으로 표를 예매했어요）

### 👤 用户体验变化
- 修改前：第10课单词点开例句经常是生成器病句（「새 휴일을 샀어요」「계획을 세우다를 자주 합니다」「처음 예약했습니다 때 힘들었어요」「제일 책을 읽어요」），学不到本课语法。
- 修改后：每词 4 句都是真实的假期/旅行对话，-아/어 보다、-네요、-(으)면 좋겠다张口就来。

---

## 📅 2026-08-12 00:30 · v1.15.12

### 📝 一句话总结
延世韩国语1 第9课（약속 约定）47 词例句全手写：约定核心名词/词组、时间名词、约定动词、认识/状态动词、形容词、副词全补齐每词 4 句，紧扣本课四大语法点「-기로 하다」「-(으)ㄹ게요」「-잖아요」「约定搭配链」，定约→定时间地点→守约/违约/改约→迟到道歉→没关系全套场景张口就来。

### 🎯 本次更新目标
- 为什么修改？第9课 47 词此前全走生成器，dump 审核确认：名词/时间词掉进「物件」模板（「새 약속을 샀어요」「내일을 선물로 받았어요」「새 올해를 샀어요」「어제 고향을 잃어버렸어요」）；7 个词组被整词名词化 + 를（「약속을 지키다를 자주 합니다」「시간을 바꾸다를 열심히 했습니다」「못 오다를 좋아합니다」）；动词模板空泛且语法错（「처음 알았습니다 때 힘들었어요」「시간이 취소하면 연락 주세요」主语颠倒、「천천히 모르세요」）；形容词全错（「이것은 미안합니다」「뭐가 제일 가능합니다」）。
- 解决了哪些问题？47 词全部手写、每词 4 句，全部换回约定/时间主题 + 本课语法：-기로 하다（내일 2시에 만나기로 했어요 / 여행을 가기로 결정했어요）、-(으)ㄹ게요 承诺（약속을 꼭 지킬게요 / 약속을 잡으면 바로 알려 줄게요）、-잖아요 反问（우리 오늘 만나기로 했잖아요）、约定搭配链（약속을 잡다 → 시간/장소를 정하다 → 지키다/어기다/바꾸다/취소하다 → 늦다/미안하다/늦어서 죄송합니다 → 괜찮아요）。
- 目标体验：学完第9课就能用韩语定约、改约、守约、道歉、安慰，时间词配「-에 时间点 + -기로」张口就来。

### ✨ 本次更新
**词汇例句（第9课 47 词，全手写）**
- 约定核心名词/词组 7 词：약속/약속 장소/약속 시간/시간을 바꾸다/약속을 지키다/약속을 어기다/약속을 잡다 —— 定约全流程（약속을 먼저 잡을게요 / 약속 시간을 정해요 / 약속을 꼭 지키세요）
- 时间名词 8 词：내일/오늘/내년/작년/올해/지난 달/이번 달/다음 달 —— -기로 하다 配时间点（내일 2시에 만나기로 했어요 / 다음 달에 한국에 가기로 했어요 / 내년에 결혼하기로 했어요）
- 约定动词 12 词：약속하다/어기다/지키다/만나다/기다리다/사과하다/결정하다/취소하다/변경하다/연락하다/시간을 정하다/장소를 정하다 —— 迟到→道歉→没关系对话链（약속에 늦어서 미안해요 / 약속을 어긴 것에 대해 사과할게요 / 괜찮아요, 다음에 만나요）
- 认识/状态动词 6 词：생각하다/알다/모르다/기억하다/참석하다/당기다 —— 知道/不知道/记得/提前（약속 시간을 몰라요 / 약속을 꼭 기억할게요 / 약속 시간을 30분 당겼어요）
- 形容词 4 词：늦다/미안하다/괜찮다/가능하다 —— 迟到道歉安慰（조금 늦을 것 같아요 / 내일 만나는 게 가능해요）
- 副词 6 词：함께/같이/다시/꼭/먼저/잠시 —— 一起/再次/一定/先（다음에 꼭 만나요 / 잠시 후에 다시 전화할게요）
- 其他 4 词：다음/고향/못 오다/늦어서 죄송합니다 —— 下次/故乡/不能来/迟到道歉（내일 못 오면 미리 알려 주세요）

### 👤 用户体验变化
- 修改前：第9课单词点开例句经常是生成器病句（「새 약속을 샀어요」「약속을 지키다를 자주 합니다」「처음 알았습니다 때 힘들었어요」「이것은 미안합니다」），学不到本课语法。
- 修改后：每词 4 句都是真实的定约/守约/改约对话，-기로 하다、-(으)ㄹ게요、迟到道歉张口就来。

---

## 📅 2026-08-12 00:26 · v1.15.11

### 📝 一句话总结
延世韩国语1 第8课（전화 打电话）54 词例句全手写：时间/频率副词、电话/科技名词、动作动词、状态形容词全补齐每词 4 句，紧扣本课四大语法点「-(으)ㄹ 거예요」「-아/어야 되다/하다 + -으면 안 되다」「-는데」「电话常用语」，打电话、发短信、订外卖、用手机操作场景张口就来。

### 🎯 本次更新目标
- 为什么修改？第8课 54 词此前全走生成器，dump 审核确认：时间副词掉进错误模板（「잠깐만 책을 읽어요」「후가 있어요?」）；频率/原因副词全错（아마 缺将来时、왜 缺问号）；电话/科技名词掉进「物件」模板（「전화번호를 선물로 받았어요」「메시지를 빌릴 수 있을까요」）；动词模板空泛（「시간이 알리면 연락 주세요」主宾颠倒、「처음 어겼습니다」时态错误）。
- 解决了哪些问题？54 词全部手写、每词 4 句，全部换回打电话/手机/科技主题 + 本课语法：将来 -(으)ㄹ 거예요（이따가 전화할게요 / 아마 늦을 거예요）、必须与禁止 -아/어야 하다 + -으면 안 되다（약속을 지켜야 해요 / 여기서 전화하면 안 돼요）、背景 -는데（전화했는데 안 받았어요）、电话常用语（여보세요 / 잠깐만요, 바꿔 드릴게요 / 잘못 걸었습니다 / 통화 중이에요）。
- 目标体验：学完第8课就能用韩语打电话、发短信、订外卖、注册登录、操作手机软件。

### ✨ 本次更新
**词汇例句（第8课 54 词，全手写）**
- 时间副词 5 词：이따가/잠깐만/일찍/아직/후 —— 电话时间表达（이따가 전화할게요 / 잠깐만요, 바꿔 드릴게요 / 10분 후에 다시 연락할게요）
- 频率/原因副词 4 词：항상/아마/왜/다 —— 频率·推测·疑问（항상 이 시간에 전화해요 / 아마 늦을 거예요 / 왜 문자를 안 보냈어요）
- 电话/科技名词 21 词：전화번호/국제전화/핸드폰/문자/메시지/벨/주소/잘못/주말/할인/배달/부탁/답장/이메일/연락/화면/앱/인터넷/계정/비밀번호/로그인 —— 全部落回本课场景（잘못 걸었습니다 / 배달 음식을 주문했어요 / 비밀번호를 바꿨어요）
- 动词 22 词：울리다/걸다/받다/보내다/바꾸다/켜다/끄다/확인하다/남기다/주문하다/통화하다/끊다/약속하다/어기다/지키다/전화하다/연락하다/알리다/검색하다/올리다/저장하다/삭제하다 —— 通话全流程 + 手机操作链（전화를 걸었는데 안 받았어요 / 핸드폰을 켜고 로그인해서 검색하고 저장해요）
- 形容词 2 词：심심하다/늦다 —— 状态表达（심심해서 전화했어요 / 약속에 늦으면 안 돼요）

### 👤 用户体验变化
- 修改前：第8课单词点开例句经常是生成器病句（「잠깐만 책을 읽어요」「전화번호를 선물로 받았어요」「처음 어겼습니다」「시간이 알리면 연락 주세요」），学不到本课语法。
- 修改后：每词 4 句都是真实的打电话/用手机对话，将来时、必须/禁止、电话常用语张口就来。

---

## 📅 2026-08-12 00:18 · v1.15.10

### 📝 一句话总结
延世韩国语1 第7课（교통 交通·问路）54 词例句全手写：方向代词/场所/交通/动词/副词五类全补齐每词 4 句，紧扣「-(으)러 가다」「-에서 -까지 + 얼마나 걸리다」「-(으)면」「-(으)세요」四大语法点，问路、乘车、换乘、迷路、迟到场景张口就来。

### 🎯 本次更新目标
- 为什么修改？第7课除 타다/바쁘다 外 52 词此前全走生成器，dump 审核确认：方向/地点名词掉进「物件」模板（「새 출구를 샀어요」「왼쪽을 잃어버렸어요」）；交通动词空泛（「주말마다 걸립니다」「천천히 걸리세요」）；副词 곧/얼마나 全错（「곧 한국어를 공부해요」「얼마나 한 번 해 보세요」）；依存名词 호/층/호선 特判正确但句数不足（1~3 句）。
- 解决了哪些问题？52 词全部手写、每词 4 句，全部换回交通/问路主题 + 本课语法：-에서 -까지 + 얼마나 걸리다（여기서 지하철역까지 얼마나 걸려요）、-(으)면（왼쪽으로 가면 은행이 있어요）、-(으)세요（2호선으로 갈아타세요）、-(으)러 가다（고속버스를 타러 터미널에 가요）。
- 目标体验：学完第7课就能用韩语问路、坐车、换乘、指路。

### ✨ 本次更新
**词汇例句（第7课 54 词，全手写）**
- 方向代词 3 词：이쪽/그쪽/저쪽 —— 生成器合格句保留、统一 4 句，强化 -보다 对比（이쪽이 저쪽보다 더 가까워요）
- 依存名词 3 词：호/층/호선 —— 补齐到 4 句（몇 호에 살아요 / 5층으로 올라가세요 / 4호선을 타고 가면 빨라요）
- 场所 7 词：아파트/호텔/미술관/대사관/기숙사/정류장/지하철역 —— 交通场景（정류장까지 걸어서 5분 걸려요）
- 地点/方向 17 词：길/출구/입구/왼쪽/오른쪽/횡단보도/육교/지하도/동·서·남·북문/사거리/신호등/직진/좌·우회전 —— 问路语法（사거리에서 우회전하면 편의점이 있어요 / 신호등을 보고 길을 건너요）
- 交通工具 6 词：시내버스/고속버스/기차/자전거/비행기/배 —— -으로/로 + -보다（기차가 비행기보다 느려요 / 자전거로 학교에 가요）
- 动词 8 词：찾다/건너다/나오다/걸리다/내리다/갈아타다/도착하다/출발하다 —— 乘车全流程（다음 정류장에서 내리세요 / 사당에서 4호선으로 갈아타세요）
- 形/副词 5 词：늦다/빨리/곧/얼마나/자리 —— 迟到·直走·距离（약속에 5분 늦을 것 같아요 / 이 길로 곧 가세요）
- 其他 3 词：약속/실례합니다/교통카드 —— 约定·问路敬语·刷卡（교통카드로 버스를 타요 / 실례합니다, 지하철역이 어디예요?）

### 👤 用户体验变化
- 修改前：第7课单词例句常是生成器病句（「새 출구를 샀어요」「주말마다 걸립니다」「곧 한국어를 공부해요」），学不到本课语法。
- 修改后：每词 4 句都是真实问路/乘车对话，问路命令、乘车流程、换乘、迷路、迟到张口就来。

---

## 📅 2026-08-11 23:50 · v1.15.9

### 📝 一句话总结
延世韩国语1 第6课（쇼핑 购物）52 词例句全手写：场所/食物/衣服/颜色/量词五类词全部补齐每词 4 句，紧扣本课四大语法点「-보다 比较」「颜色表达」「-으로/로」「-아/어요」，量词配具体名词（꽃 한 송이·구두 한 켤레·커피 한 잔），비싸다 沿用原例句。

### 🎯 本次更新目标
- 为什么修改？第6课除 비싸다 外 51 词此前全走生成器，dump 审核确认：食物模板把蛋糕当「一碗」、葡萄「有点辣」；物件模板出现「丢了裤子」「买了新的红色」；形容词模板错位（「最近天气很结实」）且漏掉第6课「-보다」比较语法；量词全是空模板无具体搭配；词组 입어 보다 被整词名词化加 를。
- 解决了哪些问题？51 词全部手写、每词 4 句，全部换回购物主题 + 本课语法：比价（이 바지가 저 바지보다 더 비싸요）、颜色（빨간색 치마가 정말 예뻐요 / 무슨 색을 좋아해요）、方式（바나나랑 우유로 스무디를 만들어요）、口语体（-아/어요 贯穿全课）。
- 目标体验：学完第6课就能用韩语逛店——挑颜色、比价格、砍价、试穿、买水果。

### ✨ 本次更新
**词汇例句（第6课 52 词，全手写）**
- 场所名词 5 词：꽃집/빵집/편의점/문구점/옷 가게 —— 购物场景（꽃집에서 꽃다발을 샀어요 / 오른쪽으로 가면 옷 가게가 있어요）
- 食物名词 6 词：케이크/빵/과일/포도/귤/바나나 —— 正确量词搭配（케이크 한 조각·포도 한 송이·귤 세 개）
- 衣服 10 词：바지/치마/셔츠/티셔츠/코트/스웨터/구두/슬리퍼/한복/넥타이 —— 试穿·大小·颜色（바지를 한번 입어 봐도 돼요?）
- 颜色 6 词：색깔/빨간색/파란색/노란색/하얀색/검은색 —— 颜色表达语法（빨간색 치마가 정말 예뻐요 / 파란색이 노란색보다 더 시원해 보여요）
- 礼物/花束 2 词：선물/꽃다발 —— -으로/로 用法（선물로 뭐가 좋을까요?）
- 形容词 10 词：길다/짧다/싸다/무겁다/가볍다/가깝다/멀다/튼튼하다/두껍다/얇다 —— 全部含 -보다 比较句（이 가방이 저 가방보다 무거워요 / 이 종이가 저 종이보다 얇아요）
- 量词 9 词：송이/개/명/장/권/병/잔/켤레/대 —— 配具体名词（꽃 한 송이·구두 한 켤레·책 두 권·커피 한 잔·컴퓨터 한 대·카드 한 장으로 계산했어요）
- 词组/动词 3 词：입어 보다/깎다/구경하다 —— 试穿·砍价·闲逛（조금만 깎아 주세요 / 저는 돈이 없어도 구경만 해요）

### 👤 用户体验变化
- 修改前：第6课单词点开例句经常是生成器病句（「케이크 한 그릇」「포도가 매워요」「바지를 잃어버렸어요」「시간이 구경하면 연락 주세요」），学不到本课语法。
- 修改后：每词 4 句都是真实的购物对话，量词有具体名词搭配，比价/颜色/砍价张口就来。

---

## 📅 2026-08-11 23:42 · v1.15.8

### 📝 一句话总结
新增例句回归校验脚本 scripts/check-examples.js（每词4句/无裸자모/助词防错，自动发现延世2遗留49个3句词）；语法卡片例句自动高亮语法标记（-에/-부터/-까지/-고 词尾安全点亮，不误伤 학교/포도）；生成器顶部标注已确认失效的词类，防未来误补硬编码。

### 🎯 本次更新目标
- 为什么修改？逐课手写例句已到第5课，「每词 4 句」是全站硬标准，但缺少一键回归校验——靠肉眼核对迟早漏。同时语法页公式（-에）与例句用法（7시에）之间缺一眼可见的对应，初学者容易发怵。
- 解决了哪些问题？① 新增 `node scripts/check-examples.js`：一次性检查全部教材的单词例句与语法例句（每词恰 4 句、kr/cn 非空、无裸자모、无重复句、无「词组名词化」(이를 닦다를)、无「어제/오늘/내일+에」），首次运行即暴露延世2 第1/7/8/9/10课 49 个词组词仍为 3 句的遗留问题（脚本内置遗留清单，如实报告不判失败，待扩展）。② 语法例句高亮：从语法卡 pattern/formula 提取 -X 标记，按「韩文词末尾」边界匹配，例句中的 -에/-부터/-까지/-고 等自动加亮。③ 生成器失效注释：时间/星期/三餐/词组等已确认语义错误的词类在 generateExample 顶部标注。
- 目标体验：语法公式 → 例句用法一眼对上；改例句后跑一遍脚本即可防回归。

### ✨ 本次更新
**功能优化**
- 语法例句高亮：-에（7시에 / 월요일에 학교에）、-부터 -까지、-고（먹고）、-을/를、-에서 等词尾标记在例句中自动加亮；含 / 的多选变体（아/어요、은/는）与无字面标记的语法点（数词、日期）不参与，避免误伤
- 高亮带词尾边界保护：이에요 的 -에 不亮、학교/포도 的 고 不亮；点读仍走 data-speak 原始文本，不受影响

**工程化**
- 新增 scripts/check-examples.js 回归校验脚本（无 DOM、纯 Node，退出码 0/1 便于 CI）
- 内置延世2 遗留 49 个 3 句词清单（第1/7/8/9/10课词组/惯用语），计入报告但暂不判失败
- utils.js generateExample 顶部标注已确认失效的词类，明确「主题课必须整课手写」的结论

### 👤 用户体验变化
- 修改前：语法页例句是纯文本，公式（-에）与例句里的实际用法（7시에）要靠自己对照；改例句后没有自动校验，4 句标准靠肉眼。
- 修改后：语法例句里的语法点一眼高亮，公式→例句对应关系直观；回归校验一条命令完成。

### 💭 设计思考
- 高亮标记只取「-前缀 + 纯韩文」的标记，并过滤含 / 或 ~ 的多选变体——因为 아/어요、이/가 这类单字变体做高亮必然误伤（요 到处都是）。宁可漏掉（数词、日期卡不高亮），也不要点错，符合「降低思考成本」。
- 词尾边界用「后不接韩文」判断：에 在 학교에 亮、在 이에요 里不亮；고 在 먹고 亮、在 학교/포도 里不亮。对学习者来说，高亮的就是「这个词的语法尾巴」。
- 49 个遗留 3 句词是旧标准时期写的，不是错误，所以脚本单独列「已知遗留」不判失败——脚本的意义是防新增回归，不是对历史翻旧账。

### ⚠️ 开发记录
- scripts/check-examples.js 用 Node VM 加载 data-books.js + 全部 data-*.js（正则排除 data-books.js 避免重复声明）；语法检查在沙箱内执行，避免跨脚本访问 let/const 绑定。
- 高亮逻辑已用第5课语法例句实测：-에（7시에/월요일에/학교에）、-부터 -까지、-고（먹고）、-을/를（운동을）全部落对，无误伤。

### 📌 下一步建议
- 把延世2 遗留 49 个 3 句词组词逐个补到 4 句（可按第7课电话/约定 → 第8课生病 → 第10课搬家分次做），补完即从脚本遗留清单移除
- 继续逐课审核例句（第6课起，主题：购物/花店/服装店）
- 校验脚本可再接 git pre-commit，改例句即自动拦截

---

## 📅 2026-08-11 23:23 · v1.15.7

### 📝 一句话总结
第5课（하루 생활 一天的生活）48 词全部手写例句，逐词 4 句、紧扣本课语法点：时间词统一 -에（且 어제/오늘/내일 不加 -에）、-부터 -까지、-고 连接、固有数词（한 시/두 시）+ 汉字数词（분/월/일）、日期表达（며칠）；星期 7 词、오전/오후、지난주/이번 주/다음 주 等时间名词全部落对场景，词组 이를 닦다/옷을 입다 用对结构，动词按一天作息配对。

### 🎯 本次更新目标
- 为什么修改？第5课是「一天的生活」主题课，生成器对时间类名词是重灾区：全部 22 个时间/星期/여행/기분 词掉进「物件」模板（새 지금을 샀어요 买了新的现在、어제 오늘을 잃어버렸어요 昨天把今天弄丢了、월요일을 찾았어요 找到了星期一、여행을 잃어버렸어요 把旅行弄丢了）；아침/점심/저녁 被当食物套吃饭模板（아침 한 그릇 더 주세요 再来一碗早上）；动词产生语法病句（처음 일어났습니다 때 힘들었어요、시간이 일어나면 연락 주세요 让时间起床）和荒谬搭配（같이 세수합시다 一起洗脸吧、지난 주말에 친구와 샤워했습니다 和朋友一起洗澡、주말마다 출근합니다 每周末上班）；词组被名词化（이를 닦다를 자주 합니다 经常"刷牙"）。
- 解决了哪些问题？42 个生成词全部 4 句手写，6 个历史手写词保持原样。时间词落在真实作息场景，-에/-부터 -까지/-고/数词/日期 五大语法点在例句里全数落地。
- 目标体验：第5课例句自然可跟读，学完就能用韩语说「几点做什么、从周一到周五、几点到几点」这类日常表达。

### ✨ 本次更新
**功能优化**
- 第5课 48 词全部手写例句（各 4 句）：时间/日期名词 17（지금/시/분/시간/반/오전/오후/오늘/어제/내일/그저께/모레/년/월/일/지난주/이번 주/다음 주）+ 星期 7 词 + 一天作息动词 10 + 三餐 3 + 여행/기분 2 + 词组 2（이를 닦다/옷을 입다）+ 历史 6 词保持原样（쉬다/자다/기다리다/쉽다/어렵다/예쁘다）
- 例句呼应本课语法点：-에（아침 6시에 일어나요、월요일에 학교에 가요，어제/오늘/내일 后不加 -에）、-부터 -까지（오전부터 오후까지 학교에 있어요、월요일부터 금요일까지 회사에 가요）、-고（일어나고 세수하고 아침을 먹어요、운동하고 샤워해요）、固有数词（지금 한 시입니다、약속이 두 시에 있어요）、日期表达（오늘은 며칠이에요?、생일이 5월 10일이에요）
- 依存名词改进：시/분/년/월/일 由生成器「3 句且不突出本词」升级为 4 句且每句突出本词（분 用 5분/10분/1분、년 用 3년/5년/몇 년）

### 👤 用户体验变化
- 修改前：第5课 42 个词走生成器，例句含「买了新的现在」「昨天把今天弄丢了」「星期一找到了」「时间太贵了」「让时间起床」「一起洗脸吧」「和朋友一起洗澡」「每周末上班」「经常'刷牙'」等病句，아침/점심/저녁 时间用法完全没体现，星期词全是「找到/丢失」。
- 修改后：48 词全部自然手写例句，时间词落在作息场景（几点起床、几点上班、几点下班），星期/周次词落在日程安排，三餐词兼顾「时间+饭」双义，动词按「起床→洗脸→刷牙→上班→下班→洗澡→睡觉」一天作息串联，跟读体验一致且更连贯。

### 💭 设计思考
- 第5课把生成器的语义盲区进一步钉死：时间/星期名词全掉「物件」模板（새 지금을 샀어요），因为生成器只认「物件」和「食物」两类名词，没有任何时间类模板。词组类（이를 닦다）被当成名词加 를，说明词组词型在生成器里根本没正确建模。结论延续第3/4课：主题课必须整课手写。
- 时间表达的语法陷阱正好在第5课语法区覆盖，手写时特意对照：어제/오늘/내일 后不加 -에（어제 열 시에 잤어요，加的是"10시에"不是"어제에"）；시 用固有数词（한 시/두 시），분/월/일 用汉字数词；日期顺序 년-월-일-요일；며칠 是固定写法。

### ⚠️ 开发记录
- 本次仍是整块替换第5课 vocab（连结尾 `]` 后的逗号一起替换，避免 `],,` 语法错误），Node 沙箱加载校验通过。
- 校验脚本验证：第5课 48 词 + 全站所有手写词 ex 数量均为 4，无语法错误。

### 📌 下一步建议
- 继续逐课审核例句（第6课起，主题：购物/花店/服装店）
- 新增 `scripts/check-examples.js` 校验脚本（每词 4 句、无裸자모、助词正确）防回归
- 时间类生成器模板可考虑补一个「时间名词」分类兜底，减少未来主题课踩坑

---

## 📅 2026-08-11 23:06 · v1.15.6

### 📝 一句话总结
第4课（음식 饮食·餐厅）47 词全部手写例句，逐词 4 句、贴合点餐/吃饭/评价食物场景：饮品词统一 마시다/한 잔，味道词全落在食物上，量词改对（한 접시/한 줄）；并顺带把全站遗留的 11 个旧标准 5 句手写词统一裁为 4 句。

### 🎯 本次更新目标
- 为什么修改？第4课是「饮食·餐厅」主题课，恰好是生成器的语义重灾区：味道形容词被套「气氛/天气」模板（여기 분위기가 맵습니다 这里气氛很辣、요즘 날씨가 짭니다 最近天气很咸）、情绪形容词荒谬（화나면 정말 좋겠어요 要是生气就好了）、副词被当谓语套动词模板（아주 운동을 해요 非常运动、우리 별로 갑시다 我们不太去吧）、饮品被当饭（어제 처음 물을 먹어 봤어요 昨天第一次吃水、녹차가 조금 매워요 绿茶有点辣、커피 한 그릇 더 주세요 再来一碗咖啡）、可乐/冰淇淋/年糕掉进「物件」模板（콜라를 찾았어요 找到可乐了、아이스크림을 어떻게 사용해요 冰淇淋怎么用）。
- 解决了哪些问题？47 词全部 4 句手写：饮品统一 마시다/한 잔/한 병，固体食物用 한 접시/한 줄，味道词全落食物，情绪词落心情，副词进真实语境，动词配对环境正确。
- 目标体验：第4课例句自然、可放心跟读，点餐场景学的句子马上能用。

### ✨ 本次更新
**功能优化**
- 第4课 47 词全部手写例句（各 4 句）：场所名词 2 + 食物/饮品名词 22 + 味道形容词 6 + 情绪形容词 3 + 动词 7 + 副词 6 + 맛있다 由 5 句裁为 4 句
- 例句呼应本课语法点：-에서（식당에서 밥을 먹었어요）、주다（소주 한 병 주세요 / 추천해 주세요）、-이/가 되다（요리사가 되고 싶어요）、-을/를
- 顺带统一历史遗留：맛있다/쉬다/자다/기다리다/쉽다/어렵다/예쁘다/비싸다/타다/바쁘다/아프다 共 11 个旧标准 5 句手写词全部裁为 4 句（全站「每词 4 句」标准就此齐整）

### 👤 用户体验变化
- 修改前：第4课 46 个词走生成器，例句含「这里气氛很辣」「最近天气很咸」「要是生气就好了」「昨天第一次吃水」「找到可乐了」「冰淇淋怎么用」等病句，饮品和食物分不清「吃/喝」「杯/碗」。
- 修改后：47 词全部自然手写例句，饮品/味道/情绪/副词/动词各归其位，贴合饮食主题与课文场景；全站所有手写词统一为 4 句，跟读体验一致。

### 💭 设计思考
- 第4课进一步暴露生成器的语义盲区：名词里「饮品 vs 食物 vs 物件」靠 foodSet 硬编码归类，漏一个就掉进错误模板；味道形容词被当普通形容词套「气氛/天气」；副词本质是修饰语却被当谓语套动词模板。结论与第3课一致：主题课必须整课手写，生成器只留作「语法正确」的兜底。
- 饮食课的词性陷阱：饮品要 마시다/한 잔，固体食物用 한 접시/한 줄，味道词必须落在食物上（这汤有点咸、咖啡太苦），情绪词落在心情上（收到礼物所以高兴）。全课例句统一按此处理，避免「吃绿茶」「一碗烤肉」这类母语者一眼就觉得怪的错误。

### ⚠️ 开发记录
- 一次性脚本注入例句时注意：替换数组要把结尾 `]` 后的逗号一起吞掉，否则会产生 `],,` 语法错误（本次整块替换第4课 vocab 已按此处理，Node 加载校验通过）。
- 校验脚本验证：第4课 47 词 + 全站所有手写词 ex 数量均为 4，无语法错误。

### 📌 下一步建议
- 继续逐课审核例句（第5课起；目前全站手写词已统一为 4 句标准，无历史遗留）
- 新增 `scripts/check-examples.js` 校验脚本（每词 4 句、无裸자모、助词正确）防回归
- 第4课例句已覆盖 -에서/주다/-이/가 되다，可考虑在语法页加「本课例句中的语法点标注」，帮跟读时对照理解

---

## 📅 2026-08-11 22:41 · v1.15.5

### 📝 一句话总结
第3课（가족 家人）60 词全部手写例句：逐词 4 句、贴合敬语语法（-시-/계시다/드시다/말씀하시다）与课文场景，55 个原生成器词全部替换为手写。

### 🎯 本次更新目标
- 为什么修改？第3课是「家人 + 敬语」主题课，恰好是自动生成器的重灾区：家人称呼被套普通人物模板（아들은 제 꿈이에요 儿子是我的梦想、할아버지가 되고 싶어요 想成为爷爷）、活动名词被当物品（새 공부를 샀어요 买了新的学习、운동을 빌릴 수 있을까요 能借一下运动吗）、敬语动词全错（저는 매일 드십니다 把敬语用在自己身上、처음 말씀하셨습니다 때 语法病句）。这课是敬语核心课，例句教错敬语用法比缺失更伤。
- 解决了哪些问题？60 词全部 4 句手写：家人称呼有真实语境、敬语动词全部用正确敬语（-께서/진지）、活动名词搭配合理动词。
- 目标体验：第3课例句自然、可放心跟读，敬语用法学得对。

### ✨ 本次更新
**功能优化**
- 第3课 60 词全部手写例句（各 4 句）：家人称呼 25 词 + 敬语动词 3 词 + 动词/物品名词/形容词/副词 32 词
- 手写词标准统一：사다/배우다/좋아하다/재미있다/따뜻하다 由 5 句裁为 4 句（删去 -ㄹ지/-려면/-으면서도 等偏难句式）
- 例句呼应本课语法点：-의（할머니의 생신）、-도（저도）、-하고（가족하고）、-을/를 좋아하다、敬语 -시-

### 👤 用户体验变化
- 修改前：第3课 55 个词走生成器，例句含「爸爸是我的梦想」「能借一下伯母吗」「最近天气很窄」「我每天吃/喝（敬语）」等病句。
- 修改后：60 词全部自然手写例句，敬语用法正确，贴合家人主题与课文场景。

### 💭 设计思考
- 生成器的极限暴露：按「词性」分发模板无法感知语义，遇到家人称呼/敬语/活动名词这类「语义强、词性无差别」的词必然荒谬。生成器保留作「语法正确的兜底」，第3课这类主题课必须整课手写。
- 敬语动词是本课语法核心，例句全部用正确敬语（선생님께서 교실에 계세요 / 진지 드셨어요 / 말씀하세요），并顺带覆盖名词敬语（진지）。

### ⚠️ 开发记录
- 一次性脚本注入例句时注意：数组结束 `]` 后原有逗号，替换时需一并吞掉，否则产生 `],,` 语法错误。
- 校验发现历史遗留：第4/5/6/7/10 课共 11 个手写词仍是旧标准 5 句（맛있다/쉬다/자다/기다리다/쉽다/어렵다/예쁘다/비싸다/타다/바쁘다/아프다），待统一为 4 句。

### 📌 下一步建议
- 把遗留的 11 个 5 句词统一裁为 4 句
- 新增 `scripts/check-examples.js` 校验脚本（每词 4 句、无裸자모、助词正确）防回归
- 继续逐课审核例句（第4课起）

## 📅 2026-08-11 22:21 · v1.15.4

### 📝 一句话总结
修复自动例句生成器（utils.js 活用系统）：无收音动词的 -ㅂ니다/-ㄹ 수/-ㄹ 겁니다/-ㅂ시다 全部拼错（크ㅂ니다→큽니다）、名词助词硬编码（학생는→학생은）、过去式 ㅆ 终声错位（배웑습니다→배웠습니다）等 4 大类，全站自动例句一次性变语法正确。

### 🎯 本次更新目标
- 为什么修改？例句审核时发现自动生成器产出的韩语存在大量拼写错误，影响面覆盖延世1 第3课起 + 延世2 全部未手写例句（两本教材共 1100 词靠生成器兜底）。错误分 4 类：①无收音词把 ㅂ/ㄹ 拼成独立字符（크ㅂ니다、가ㄹ 수）②名词助词不分收音硬编码（학생는、책를、밥가）③过去式 ㅆ 终声索引错（用了 ㄵ 的索引，产出 배웑습니다、왅습니다 这类错字）④ㄹ/ㄷ 불규칙 动词缺处理（알다→아세요、걷다→걸어요）。
- 解决了哪些问题？学习者翻到没手写的词时，看到的不再是错误韩语；错误会误导记忆，比例句缺失更伤学习。
- 目标体验：自动例句"语法正确、句式合理"（及格线），手写例句继续逐课替换（优秀线）。

### ✨ 本次更新
**Bug 修复**
- 新增 `mergeFinal()`：把 ㅂ/ㄹ 正确合成进音节（가다→갑니다、가다→갈 수 있습니다），ㄹ받침 先 ㄹ탈락（알다→압니다、알 수 있습니다）
- 重构 `aeoStem()/bipStem()/pastStem()`：统一 -아/어 连接与过去式（오다→와서、되다→돼서、배우다→배워서、맵다→매워서），修复原 aSeoForm 的单音节乱码（오다→유서 类）
- 修 `pastForm()`：ㅆ 终声索引 5→20，修正所有无收音动词过去式；补 ㅜ→ㅝ、ㅚ→ㅙ 合并
- 不规则动词表补全：서두르/다르/누르/오르 + ㄷ불규칙 묻/싣
- 名词模板助词全部改用 `attach()`：人物/食物/物品类的 는/가/를/랑 按收音自动选 은/이/을/이랑
- `condForm`（알다→알면）、`pleaseForm`（알다→아세요、걷다→걸으세요）补 ㄹ/ㄷ불규칙
- 不涉及手写例句（第1/2课 27 词等），零影响

### 👤 用户体验变化
- 修改前：约 1100 个走生成器的词，例句含 크ㅂ니다、학생는、배웑습니다 这类拼写错误，误导记忆。
- 修改后：全部自动例句语法正确（갑니다、학생은、배웠습니다、갈 수 있습니다），可放心朗读跟读。

### 💭 设计思考
- 生成器定位为"及格线兜底"：保证每个词都有例句可看且语法正确；"优秀线"靠逐课手写例句达成。这次修复是低成本的全局收益（改一个文件，好一片词），而非逐词手写（1100 词 × 4 句 = 4400 句，非短期能完成）。
- 活用规则抽成公共函数（aeoStem/bipStem/pastStem）而非零散 if，未来加词不用重复修。

### ⚠️ 开发记录
- 原代码把 과거 ㅆ 的终声索引写成 5（ㄵ），导致所有无收音动词过去式产出错字，靠自动检查脚本（裸자모 检测）抓到——原 jamo 检测只查"独立字符"，这种"合成但错误"的字靠人工抽查 dump 发现。后续应把 과거/연결 检查加入校验脚本。
- 名词助词检测需注意误报：复合名词（바닷가/숟가락/신랑）和连接词尾（왔는데/없는）会误判为名词+助词，判断时需人工排除。

### 📌 下一步建议
- 新增 `scripts/check-examples.js` 校验脚本：每词例句 4 句、自动例句无裸자모、无名词助词拼错，防回归
- 继续逐课手写例句（第3课起），把生成器"及格"逐步升级为"优秀"
- 生成器句子较模板化，可后续为动词/形容词增加更多自然句式，减少机械感

## 📅 2026-08-11 22:00 · v1.15.3

### 📝 一句话总结
全站例句统一为每词 4 句：第1课 12 个词各删 1 句（5→4），第2课 15 个词补齐手写例句（形容词 7 个 + 位置名词 7 个 + 좋다 修正），全部 4 句标准。

### 🎯 本次更新目标
- 为什么修改？例句审核发现每词 5 句对闪卡学习负担偏重，统一为 4 句更聚焦；第2课形容词/位置名词此前走自动生成器、质量差（语法错误如"크ㅂ니다"），需手写覆盖。
- 解决了哪些问题？①5 句改为 4 句，降低翻卡信息量；②第2课 좋다 逗号误用（→ 句号）、删除语义重复句；③形容词/位置名词补齐贴合第2课语法（에 있다）的例句。
- 目标体验：例句负担适中，每句都贴合课文语境。

### ✨ 本次更新
**功能优化**
- 第1课：12 个动词/形容词（있다/가다/오다/만나다/먹다/마시다/보다/듣다/읽다/쓰다/공부하다/인사하다）各删 1 句，5→4 句
- 第2课：新增 없다/많다/적다/크다/작다/나쁘다 手写例句各 4 句；新增 위치명사 앞/뒤/옆/위/아래/안/밖 手写例句各 4 句；있다 复用第1课例句；좋다 修正标点并删冗余句

### 👤 用户体验变化
- 修改前：例句 5 句偏多，部分自动生成例句有语法错误。
- 修改后：第1、2课例句全部 4 句、全部手写、语法正确，贴合课文主题。

### 💭 设计思考
- 4 句是"语境输入量"与"翻卡负担"的平衡点，作为全站例句数量标准。
- 位置名词用「N에 있다」句式（책이 책상 위에 있어요）贴合第2课语法点，让例句本身即复习语法。

### 📌 下一步建议
- 继续第3课起逐课审核（进行中）
- 生成器修复见 v1.15.4

## 📅 2026-08-11 21:40 · v1.15.2

### 📝 一句话总结
《延世韩国语1》第1课例句质量审核优化：修正 6 处翻译/拼写/生硬表达，补齐 오다/만나다/인사하다 三个动词的例句（各 5 句）。

### 🎯 本次更新目标
- 为什么修改？例句是闪卡学习的重要语境输入，但第1课部分例句存在中文时态不准、量词分写不规范、表达偏书面等问题，且 오다（来）/ 만나다（见面）/ 인사하다（打招呼）三个高频动词此前完全没有例句，卡片例句区空白。
- 解决了哪些问题？
  1. 中文翻译不准确（如"没时间去不了"时态错误 → "因为没时间，没能去"）
  2. 量词分写不规范（한잔 → 한 잔）
  3. 三个高频动词卡片例句区空白，与其他动词体验不一致
- 目标体验：第1课每个动词翻卡后都有自然、准确的 5 条例句，语境完整。

### ✨ 本次更新
**功能优化**
- 修正 6 处例句：있다/가다/마시다/보다/쓰다/공부하다 的中文翻译或韩语表达
- 补齐 3 个动词例句（오다/만나다/인사하다 各 5 句，贴合第1课"自我介绍"主题，含 반가워요 / 선생님께 / 어서 오세요 等高频表达）

### 👤 用户体验变化
- 修改前：오다/만나다/인사하다 翻卡没有例句；个别例句中文翻译有偏差。
- 修改后：第1课 11 个动词全部有 5 条自然例句，中文准确。

### 💭 设计思考
- 难度策略：例句允许使用延世1 全书（初级全程）语法，不强压到第1课——动词在只允许第1课语法时几乎造不出自然例句，保持自然度和语境价值优先（方案A）。
- 例句统一：每词 5 句，语法点分散（现在时/过去时/请求/疑问/惯用语），避免模板化。

### 📌 下一步建议
- 按单元逐课审核第2课起的例句质量（进行中）
- 可考虑新增数据校验脚本（动词必有例句、例句数统一），避免再出现例句空白

## 📅 2026-08-11 21:16 · v1.15.1

### 📝 一句话总结
补全《延世韩国语2》49 个词组/问候语/惯用语词的例句——这些词此前闪卡上没有例句区。

### 🎯 本次更新目标
- 为什么修改？延世2 上线后复查数据发现，43 个"动词短语"（전화를 걸다 打电话、약속을 지키다 守约、빨래를 하다 洗衣服等）+ 4 个问候语 + 여보세요 + 웬일이에요 共 49 个词在闪卡上没有例句——示例句生成器只认识延世1 用到的 11 种词性，遇到"动词短语""惯用语"这两个新标签就返回空。
- 解决了哪些问题？
  1. 词组型单词（打电话/守约/发烧/拍照）是全书最高频实用口语，没有例句学不踏实
  2. 卡面例句区空白，学习体验不完整
- 目标体验：这 49 个词翻卡后都能看到贴合课文语境的 3 个例句。

### ✨ 本次更新
**功能优化**
- data-yonsei2.js 为 49 个词补写手写例句（每个 3 句，围绕课文场景）：
  - 제1과 问候语：처음 뵙겠습니다 / 말씀 많이 들었습니다 / 잘 부탁합니다 / 오래간만입니다
  - 제7과 电话：여보세요 / 전화를 걸다 / 약속을 지키다 等 16 个
  - 제8과 医院：열이 나다 / 기침을 하다 / 병이 낫다 等 16 个
  - 제9과 旅行：사진을 찍다 / 기억에 남다 / 기대가 되다
  - 제10과 家务：이사를 하다 / 빨래를 하다 / 설거지를 하다 等 11 个
- 校验：49 词全部有例句、文件语法正确、数据结构完整（629 词 + 80 语法无变动）

### 👤 用户体验变化
- 修改前：学 전화를 걸다（打电话）等词，卡片上例句区是空的
- 修改后：翻卡即可看到 3 个自然例句，边学边看用法，与延世1 体验一致

### 💭 设计思考
- 为什么采用手写例句而不是自动生成？词组型单词（전화를 걸다 已带宾语助词）没法用通用模板自动造句，手写才能贴合课文语境；延世1 里 `词组`、`쉬다` 等也是手写例句的既有模式，保持一致。

### ⚠️ 开发记录
- 遇到的问题：第一次插入例句时，文件原有的 CRLF 换行与插入内容（LF）混在一起、条目间还出现多余空行。
- 解决：先把文件恢复干净，改写插入脚本——统一换行为 LF、按行精确定位替换，完成后校验 CR/LF 数量与文件结尾，格式与仓库一致。

### 📌 下一步建议
- 延世1 里也有 1 个遗留词（늦어서 죄송합니다）没有例句，可顺手补上
- 实际学习时如发现释义/例句与课文语境不符，随时改 data-yonsei2.js
- 远期：可考虑为"动词短语"这类新词性在示例句生成器里加兜底逻辑，避免以后加教材再踩"例句为空"的坑

## 📅 2026-08-11 20:47 · v1.15.0

### 📝 一句话总结
《延世韩国语2》全部 10 课正式上线：629 个单词、80 个语法点，书架里延世2 从"敬请期待"变成可学习的教材。

### 🎯 本次更新目标
- 为什么修改？延世1 数据已稳定，学习需要进阶教材。阶段1~3 搭好的多教材架构，等的就是第二本真内容入库。
- 解决了哪些问题？
  1. 书架里延世2 是"敬请期待"占位，点不进去
  2. 10 课内容（词汇/语法）此前只存在于提取草稿里，没有进网站
- 目标体验：打开首页，延世2 卡片可直接点进课程列表，闪卡学习 / 测验 / 收藏 / 搜索全部和延世1 一样可用。

### ✨ 本次更新
**新增功能**
- 新增教材数据 `data-yonsei2.js`：延世韩国语2 全 10 课（소개 / 한국 음식 / 시장 / 초대 / 교통 / 공공기관 / 전화 / 병원 / 여행 / 집안일）
  - 词汇 629 个（每课 57~78 个，按主题分组，含重要度★与词性）
  - 语法 80 个（每课 8 个，含公式、讲解、例句）
  - 词汇/语法对照教材逐课人工核对；每课结尾"阅读"的高频词也已补入词汇表
- 延世2 从 PLANNED_BOOKS 移除，书架自动显示为可学教材（延世3 仍为"敬请期待"）
- 加载顺序：data-books → data-yonsei1 → data-yonsei2 → utils → …；sw.js 缓存号 v38→v39

### 👤 用户体验变化
- 修改前：书架第二格是"延世韩国语2 · 敬请期待"，想学只能翻 PDF / 草稿。
- 修改后：点延世2 卡片直接进课程列表，体验与延世1 完全一致。
- 学习进度、测验记录、上次所学按书各自保存（阶段3 已就绪）：切到延世2 学完再切回延世1，两边进度都在，不串书。

### 💭 设计思考
- 为什么采用当前方案？延世2 直接沿用延世1 的 VOCAB/GRAMMAR/LESSONS 数据结构注册，零新代码、零新页面，维护成本最低。
- 数据怎么来的？教材 PDF 文字层逐课提取 → 中文释义人工编写 → 存疑条目（⚠️）逐条对照实体书确认 → 已确认后由脚本解析草稿生成数据文件。
- 遵循原则：这次只"填充数据"，不新增功能模块，符合做减法。

### ⚠️ 开发记录
- 遇到的问题：教材每课结尾的"阅读"容易被漏掉——第6/7课结尾阅读补词已处理；部分课草稿标题的"共N词"与实际表格数不一致，以表格实内容为准（每课 57~78 词）。
- 解决：逐页检查补漏；数据文件由脚本从草稿生成，并做结构校验（80 条语法全部有例句、词汇无缺字段）。

### 📌 下一步建议
- 延世2 内容已就绪，先实际学习一段时间，遇到释义/重要度★不合理的地方随时改 data-yonsei2.js
- 延世3 仍是占位，视学习进度决定何时开始提取
- 远期：学习统计、AI 学习助手等仍在产品规划里，不急

## 📅 2026-08-11 18:27 · v1.14.0

### 📝 一句话总结
学习进度、测验记录、上次所学三块数据改为按教材各自保存——切教材零丢失，不再弹"确定切换吗？"确认框。

### 🎯 本次更新目标
- 为什么修改？阶段1/2 让多教材架构立起来了，但学习数据还是全站一份：切教材会清空学习进度，测验记录不区分是哪本书的。以后真的同时学延世1 和延世2 时，数据会混、会丢。
- 解决了哪些问题？
  1. 有未完成学习进度时切换教材，进度直接清空丢失
  2. 测验记录 / 今日分析分不清是哪本书的
  3. 上次学到第几课全站一个，换书就串
- 目标体验：随便切书，每本书"学到哪了"都原样记得；测验历史只看当前书，不混淆。

### ✨ 本次更新
**功能优化**
- 学习进度快照改为按书保存（`ys-study-progress` 变成为 `{ 书号: 快照 }` 的映射）：
  - 切教材不再清空进度，切走再切回原样恢复，换书零丢失
  - 首页"继续学习"按钮按当前书读取，各书互不干扰
- 测验记录每条写入所属书号；测验历史、今日分析、错词统计只看当前书（老记录自动算默认书）
- 上次所学课程按书隔离：每本书记各自的 `ys-last-lesson-<书号>`
- 删除切换教材的确认弹窗——既然切换不再丢进度，确认框就没有存在必要了，更少打断

### 👤 用户体验变化
- 修改前：学一半想看看另一本，系统警告"会清空进度"，甚至真的会丢
- 修改后：点另一本直接切走，回来还是原来的位置，一点不慌
- 测验历史里不再混入别的书的数据，看的是哪本就显示哪本的记录

### 💭 设计思考
- 为什么按书存而不是全站一份？多教材的核心就是"每本书独立"，数据跟书走才符合直觉；后续加更多教材不用再改结构。
- 为什么删确认框？确认框存在的唯一理由是"切换会丢数据"，现在数据不丢了，弹窗就是纯打扰，符合"做减法"。
- 为什么每日打卡保持全局？"今天学了没、连续几天"是整体学习行为，不该被某本书割裂；细到"今天学了哪本"等延世2 加入后需要时再细分。

### ⚠️ 开发记录
- 冒烟 23 项断言全过：两本书快照互不串、切走切回零丢失、旧单快照正确迁移到默认书、测验历史只显示当前书、切书不弹 confirm。
- 老数据迁移幂等且安全：旧学习快照 / 旧测验记录 / 旧"上次所学"首次打开时自动归入默认书延世1，不丢任何历史。

### 📌 下一步建议
- 阶段4：加入延世韩国语2 数据（需要用户提供教材内容），书架的延世2 自动变成可学教材
- 学习快照加"进行中"标记：首页能看到哪本书学到一半，一键继续
- 测验历史每条加书名小标签（如 연세1），跨书看历史不混淆
- 全站 LESSONS/VOCAB/GRAMMAR 引用渐进替换为 getLessons()/getVocab()/getGrammar()

## 📅 2026-08-11 18:09 · v1.13.0

### 📝 一句话总结
书架上的每本教材卡片都加上掌握进度（如「已掌握 45/167」+ 细进度条），首页扫一眼就知道每本书学到哪了。

### 🎯 本次更新目标
- 为什么修改？书架 v1.12.0 只回答了"有哪些书"，没回答"我学到哪了"。首页应该是一眼能看清整体学习状态的入口。
- 解决了哪些问题？
  1. 想知道一本教材的掌握进度，得点进课程列表一科一科看，成本高
  2. 以后同时学多本教材时，没法快速对比各本书学到哪
- 目标体验：打开首页，每本书的卡上一行字 + 一根细条，扫一眼就知道"这本书学了百分之多少"。

### ✨ 本次更新
**新增功能**
- 已加入教材的卡片新增进度显示：
  - 文字：`延世韩国语1 · 10课 · 已掌握 45/167`
  - 视觉：标题下方一根 4px 细进度条，宽度 = 掌握比例，随学习实时刷新（学习完直接回首页即更新）
- 进度按"每本书自己的书号"统计——未来加入第二本教材后，各书的进度互不串扰

### 👤 用户体验变化
- 修改前：书架上只有书名和"几课 · 几个词"，不知道掌握情况
- 修改后：卡片上直接看到已掌握 X/N 和进度条，学习状态一目了然，不用点进去数
- 学习流程更顺：学完今天的词回到首页，进度条立刻前进，正反馈即时可见
- 当前教材的区分改为「蓝边框 + ✓ 学习中」标识，去掉整卡淡蓝底——用户反馈满卡变蓝太突兀，用边框勾选更克制、不抢内容

### 💭 设计思考
- 为什么加在书卡里而不是单独页面？书架是首页主内容，进度是"学习状态"的第一信息，放这里最符合"扫一眼"。
- 为什么文字 + 细条都要？数字精确（45/167），细条直观（一眼看比例），互补；细条沿用课程列表的 4px 风格，不抢视觉。
- 进度按书号算：SRS 数据 key 是「书号|课号|单词」，统计时必须用"这本书自己的书号"拼 key，否则非当前书的进度会串到当前书上（测试专门验证了这点）。

### ⚠️ 开发记录
- 关键坑：统计非当前教材进度时，不能复用全局 wk()（它拼的是当前书号），必须用 b.bookId 直接拼 key。测试模拟了两本书并特意"串错 key"，确认不会误统计。
- 冒烟 10 项断言全过：未学显示 0/N、学 4 个后显示 4/N、进度条宽度=比例、非当前书只算自己 key、updateHomeStats 后刷新、占位卡无进度。

### 📌 下一步建议
- 阶段3：测验历史、学习进度快照、上次所学按书隔离（保存时写入 bookId）
- 阶段4：加入延世韩国语2 数据，书架自动把延世2 从"敬请期待"变成可学教材（进度随之显示）
- 后续：把全站 LESSONS/VOCAB/GRAMMAR 引用渐进替换为 getLessons()/getVocab()/getGrammar()

## 📅 2026-08-11 17:38 · v1.12.0

### 📝 一句话总结
首页课程列表改为「我的教材」书架：直接列出延世韩国语 1/2/3（1 可学，2/3 占位），点书进课程列表；点书名切换教材的方式同步上线。

### 🎯 本次更新目标
- 为什么修改？阶段1 已完成多教材地基，但用户还看不到、也切不了教材。这次把首页改造成书架，让整个延世系列的"学习路径"一眼可见。
- 解决了哪些问题？
  1. 首页只有延世1 的课程列表，看不出后面还有延世2/3，学习没有延续感
  2. 没有统一入口切换教材，只能靠数据文件硬切
  3. 换教材缺少保护：有未完成的学习进度时，直接切会静默丢失
- 目标体验：学完延世1 心里清楚"下一本是延世2"；以后延世2 数据加入，点一下就能换书继续学。

### ✨ 本次更新
**新增功能**
- 首页「课程列表」→「我的教材」书架：列出已加入教材 + 规划中的教材
  - 已加入教材（延世1）：显示韩文名 + 中文名 + 课数/词数，当前教材高亮「✓ 学习中」
  - 规划中教材（延世2/3）：🔒 占位显示「敬请期待」，点一下提示即将加入
- 点已加入教材进入该书的课程列表；点其他已加入教材则切换教材并进入它的课程
- 切换教材时：全站书名、课程列表、进度条、主按钮副标题、学习/测验数据全部跟随新教材刷新
- 保护：有未完成学习进度时先弹确认，确认后才切换并清空旧书进度

### 👤 用户体验变化
- 修改前：首页直接是延世1 的课程列表，教材选择无入口
- 修改后：首页 = 书架，一眼看到整个延世系列；点书进课程，学习路径更清晰
- 切换后立即看到课程列表和进度都变成新教材的，反馈即时

### 💭 设计思考
- 为什么首页课程列表改成书架？用户希望"首页的课程列表直接变成书列表，点书再进课程列表"——书是学习的第一层级，课程是第二层级，层级更清楚。
- 为什么 2/3 要占位（而非隐藏）？用户明确要求"书的内容先不填、只占位置"——把延世系列完整列出来，学习有延续目标感；数据加入后占位自然变可学，无需改页面。
- 占位为什么加去重保护？占位书加入 BOOKS 后若忘记从 PLANNED_BOOKS 移除会重复显示，渲染时自动跳过已注册的占位书，杜绝重复。
- 切换保护为什么用确认？换书会清空进行中的学习会话，直接切会损失进度；有进度才问、无进度直接切，降低打扰。

### ⚠️ 开发记录
- 阶段1 已备好的 setCurrentBook() 这次直接复用，切换只需"更新状态 → 重绑数据 → 清会话 → 刷新全站"。
- 移除了初版的「点书名弹教材面板」交互（openBookPicker/closeBookPicker/selectBook 及对应 HTML/CSS），改为首页书架直显，避免入口藏在弹窗里。
- 测试：node 冒烟 33 项断言全过——书架渲染（1 真书 + 2 占位）、点当前书进课程、点占位书弹 toast、占位书不可切换、确认弹窗拦截（取消不切 / 确认才切）、模拟延世2 加入后书架去重与切换刷新。

### 📌 下一步建议
- 阶段3：测验历史、学习进度快照、上次所学按书隔离（保存时写入 bookId）
- 阶段4：加入延世韩国语2 数据（新建 data-yonsei2.js），书架自动把延世2 从"敬请期待"变成可学教材
- 后续：把全站 LESSONS/VOCAB/GRAMMAR 引用渐进替换为 getLessons()/getVocab()/getGrammar()

## 📅 2026-08-11 17:09 · v1.11.0

### 📝 一句话总结
为支持多教材做准备：数据层升级为「教材注册表 + 当前教材」架构（阶段1 打地基），页面体验完全不变。

### 🎯 本次更新目标
- 为什么修改？原架构只有延世1 一套 VOCAB/GRAMMAR/LESSONS 全局数据，直接加第二本教材（延世2）会导致结构混乱。先打地基，后面加教材才安全、不碰已有数据。
- 解决了哪些问题？
  1. 数据只有一份、写死在 data.js，未来无法并存多套教材
  2. 没有统一的切教材入口，学习/测验会话、进度快照可能跨书串数据
  3. 老数据的 localStorage 记录没有"所属教材"信息，换书后无法区分归属
- 本阶段只做内部升级，不新增任何用户可见功能，页面与学习流程与 v1.10.15 完全一致。

### ✨ 本次更新
**代码重构（多教材架构 · 阶段1 打地基）**
- 新增 data-books.js：教材注册表 BOOKS + 全局状态 APP_STATE（当前教材）+ 应用配置 APP_CONFIG + getCurrentBook / getBook / bindBookGlobals
- data.js 更名为 data-yonsei1.js：数据改名 YONSEI1_VOCAB / GRAMMAR / LESSONS，末尾 registerBook 注册为延世1 教材并 bindBookGlobals
- 全局 LESSONS / VOCAB / GRAMMAR 改为可重绑变量，始终指向当前教材（全站 45 处原有引用零改动）
- index.html：加载顺序改为 data-books.js → data-yonsei1.js → …；启动脚本恢复上次选择的教材并重绑数据；wk / gk / 书名改读 getCurrentBook()
- app.js：新增 setCurrentBook()（切书入口）、clearBookSessionState()（切书状态清理协议）、migrateMultiBook()（数据迁移，幂等）；migrateBookIdKeys 前缀改用默认书号
- sw.js：FILES 更新为 data-books.js + data-yonsei1.js，缓存号 hangul-v34 → v35

### 👤 用户体验变化
- 修改前 / 修改后：页面、交互、学习流程**没有任何变化**。本阶段是"装修打地基"，用户不会感知任何不同。
- 学习数据完全保留：进度、收藏、掌握、SRS、打卡、测验历史原样不动；迁移只做"补全所属教材标记 + 备份"，不删、不改任何已有数据。

### 💭 设计思考
- 为什么用「普通变量重绑」而不是 defineProperty getter？getter 会让全局词法绑定遮蔽 window 属性，产生隐藏 bug；普通变量 + bindBookGlobals() 在启动/切书时重绑，简单可靠，未来可渐进迁移到显式 getLessons() / getVocab() / getGrammar()。
- 为什么阶段1不动任何页面？架构升级要分阶段验证：先保证"地基稳、数据安全"，再谈"首页展示多教材"。一次大改风险高，小步走每步可测。
- 遵循了哪些原则？做减法（不加用户可见功能）、渐进式重构（不动 45 处原有引用）、数据安全优先（迁移幂等、保留备份、不删任何数据）。

### ⚠️ 开发记录
- 测试：用 node 模拟浏览器环境加载全部模块，19 项断言全过——迁移幂等、切书防护、会话清理、数据重绑均验证通过。
- 一个坑：data.js 重命名后，已安装的 PWA / 桌面应用缓存里仍是旧文件清单，必须同步 bump 缓存号 + 更新 FILES，否则拿不到 data-books.js 会 404（已处理）。

### 📌 下一步建议
- 阶段2：首页加「我的教材」入口（等用户确认阶段1稳定后再开始）
- 阶段3：学习进度快照、测验历史、上次所学按书隔离（保存时写入 bookId）
- 阶段4：加入延世韩国语2 数据（新建 data-yonsei2.js）
- 后续：把全站 LESSONS/VOCAB/GRAMMAR 引用渐进替换为 getLessons() / getVocab() / getGrammar()，彻底去全局依赖

## 📅 2026-08-11 16:24 · v1.10.15

### 📝 一句话总结
修复三个真实存在的小 Bug：收藏页取消收藏语法无效、首页进单词列表历史记录重复、搜索结果进学习不关搜索浮层。

### 🎯 本次更新目标
- 为什么修改？架构审计（2026-08-11）发现这三个问题一直在影响使用体验，且属于"现在修了只有好处、不会改变使用方式"的小修复。
- 解决了哪些问题？
  1. 收藏页点击星星取消收藏语法，实际删除的是错误的 key，操作无效（语法收藏 key 漏了 `bookId` 前缀，与全站 `gk()` 不一致）
  2. 首页点某课进单词列表，`navigateTo` 被调用两次，历史栈多压一条，累积后返回逻辑失真
  3. 搜索结果点「▶ 学习」进入学习模式，搜索浮层未关闭，盖在学习页上方

### ✨ 本次更新
**Bug 修复**
- 收藏页语法收藏开关改用 `gk(lessonNum, grammarIdx)` 统一 key，与课程页/收藏判断一致，取消收藏语法现在真正生效
- 首页课表点击改为只调用 `openLesson()`（其内部已负责跳转），去掉外层重复的 `navigateTo('page-words')`
- 搜索结果「▶ 学习」先 `closeSearch()` 再进入学习模式

### 👤 用户体验变化
- 修改前：在收藏页点掉语法收藏的星星没反应；首页进单词列表返回时历史偶尔错乱；搜索后进学习，浮层挡在学习页上方
- 修改后：三处操作都符合直觉——取消收藏立即生效、返回路径干净、学习时浮层已关闭

### 💭 设计思考
- 收藏页语法 key 与全站不一致属于"代码笔误"，修正后统一走 `gk()`，为将来多教材（bookId 前缀扩展）扫除隐患
- 三个修复均为最小改动，不改变任何现有使用方式

### 📌 下一步建议
- 教材地基改造（v1.11.x 方向）：数据层引入「教材」维度，首页课表区改教材列表，详见架构审计报告
- 双历史栈（JS 栈 + 浏览器历史）失同步问题属于架构级，建议随教材改造一并处理

---

## 📅 2026-08-11 01:59 · v1.10.14

### 📝 一句话总结
每日记录每天一个框的边框从深灰改到最浅的浅灰（与总览卡边框同色），框更淡、更安静。

### 🎯 本次更新目标
- 为什么修改？用户反馈 v1.10.13 每天的框颜色太深太重，抢了上方总览卡的风头，要求换很浅的色。

### ✨ 本次更新
**UI·UX 优化**
- 每日记录盒子边框由 `--text-subtle`（中灰）改为 `--border`（最浅灰），与总览卡边框同色

### 👤 用户体验变化
- 修改前：每天一个较明显的灰框，视觉偏重
- 修改后：极浅灰框，存在但不抢眼，页面更安静

### 💭 设计思考
- 用与总览卡相同的 `--border`，全站边框语言统一；主次靠「白底填色卡 vs 透明描边框」区分，不靠边框深浅

---

## 📅 2026-08-11 01:52 · v1.10.13

### 📝 一句话总结
学习记录页每日记录再微调：去掉「📅 每日记录」分区标题，每天改为一个透明浅灰边框的小盒子（背景不变色），一天的紧凑信息收在盒子里。

### 🎯 本次更新目标
- 为什么修改？v1.10.12 把每日记录做成无边框流水，用户希望每天有独立框来分组、但背景保持透明；同时「每日记录」标题与月份标题的📅图标重复、略显多余，一并去掉。
- 解决了哪些问题？每天的信息有清晰边界，扫读时不会粘在一起；标题去重。
- 希望提升哪些体验？每天一个框分组感更强，透明背景不抢上方总览卡的风头。

### ✨ 本次更新
**UI·UX 优化**
- 去掉「📅 每日记录」分区标题（与月份标题「📅 2026年8月」图标重复）
- 每日记录由无边框流水改为「每天一个透明小盒子」：浅灰边框、透明背景（不变色）、圆角，紧凑内容收在盒内
- 词条标签（点听发音）、折叠展开、「·」连接的统计行等交互全部保留

### 👤 用户体验变化
- 修改前：每日记录是无边框流水，靠分隔线区分；上方有「📅 每日记录」小标题
- 修改后：每天一个透明浅灰框，信息边界清楚；标题去掉，页面更简洁
- 背景透明不变色，总览卡（白底填色卡）与每日记录（透明描边框）靠「填色 vs 描边」区分主次

### 💭 设计思考
- 为什么每天一个框而不是整个区域一个大框？用户明确希望每天有独立分组；透明背景让框不喧宾夺主
- 为什么去掉标题？「每日记录」与月份「📅」图标重复，且总览卡紧邻其上，标题属冗余信息，做减法

### ⚠️ 开发记录
- v1.10.12 已提交推送后继续微调，故单独成 v1.10.13 小版本
- 每天边框色比总览卡略明显（`--text-subtle`），保证"框"看得清；若觉得太重可改回更淡的 `--border`

### 📌 下一步建议
- 每天边框若视觉偏重，可换成更淡的 `--border`
- 总览卡底部「本月掌握 · 累计测验」是累计数据，与今日信息混放，可考虑后续拆分

---

## 📅 2026-08-11 01:30 · v1.10.12

### 📝 一句话总结
学习记录页主次重新划分：今日总览回到原来的白底浅灰边配色（不做蓝色主角），每日历史改为紧凑的无边框流水列表（日期行 + 「·」连接的统计行 + 词条标签），两区之间新增「📅 每日记录」分区标题——页面从"一串相似的卡片"变成"一张总览卡 + 一条历史流水"。

### 🎯 本次更新目标
- 为什么修改？v1.10.10 起总览是淡蓝底蓝框、每日记录是白卡片，两者同形同感，一眼扫过去主次不分明；希望一眼分清"今天的快照"和"每天的历史"。
- 解决了哪些问题？主次不清——总览与历史不再靠颜色深浅区分，而是靠"唯一一张带框卡片 vs 无边框列表"的形态区分，历史不再和总览抢视觉。
- 希望提升哪些体验？进页面第一眼是总览，往下是安静的历史，层级清楚、扫读成本低。

### ✨ 本次更新
**UI·UX 优化**
- 今日总览卡背景恢复为 `--surface` 白底 + `--border` 浅灰细边框（回到 v1.10.9 之前的原配色，去掉 v1.10.10 引入的淡蓝底蓝框）
- 每日历史记录由"每天一张白卡片"改为"紧凑流水式无边框列表"：每条 = 日期行（是今天则加「· 今天」）+ 「·」连接的统计行（学习了/掌握了/测验，有哪项显示哪项）+ 掌握词/错词标签行；内容缩进在日期行下方，条目间用极浅分隔线
- 「今天」标识从蓝色小胶囊改为日期行旁的「· 今天」纯文字，与日期融为一体、不抢视觉
- 历史列表最上方新增分区小标题「📅 每日记录」（灰色小字，层级在月份标题之上、页面大标题之下），把总览与历史之间画出一道清晰边界

### 👤 用户体验变化
- 修改前：总览（淡蓝卡）和历史（白卡）都带框、同形，扫过去像一串相似的卡片
- 修改后：总览是页面上唯一一张带边框的卡片，历史是紧凑的无边框流水，中间还有分区标题——主次一眼分明
- 每天的记录一行扫完（统计 + 词条标签），不再一行一个数字地上下找
- 点词条听发音、掌握/错词折叠展开等交互全部保留

### 💭 设计思考
- 为什么总览不做蓝色主角？用户希望回到原本的白底浅灰边配色；在历史列表改为无边框之后，白卡天然成为页面上唯一的"卡片"，靠形态而非颜色就能区分主次，更克制、更符合"做减法"原则
- 为什么历史改紧凑流水式？主次对比最强——一张卡 vs 一条流水；且每天的信息用「·」连成一行（有哪项连哪项），比多行散列更省眼力、扫读更快
- 分区标题放在列表最上方（两区交界处），视觉停顿最自然

### ⚠️ 开发记录
- 中途方向微调：先按用户意向把总览卡做成淡蓝实底主角卡，随后用户确认更想要原先的白底浅灰边配色——主次改用"形态区分"实现，颜色不动
- `--primary-faint` 变量恢复 0.05 原值，总览卡不再使用

### 📌 下一步建议
- 「📅 每日记录」与月份标题「📅 2026年8月」都是日历图标，可考虑给分区标题换图标或去掉，避免 emoji 重复
- 总览卡底部"本月掌握 · 累计测验"是累计数据，与今日信息混在一起，可考虑淡出或拆分
- 每日列表改无边框后，可观察是否需要更明显的时间分隔来增强日期扫读感

---

## 📅 2026-08-11 01:17 · v1.10.11

### 📝 一句话总结
顶部今日总览的「今日掌握」和「今日错词」词条也能点击播放韩语发音了，与下方每日卡片完全一致；总览从此不只能看、也能点。

### 🎯 本次更新目标
- 为什么修改？v1.10.10 给每日卡片词条加了「点击播放发音」，但顶部今日总览的掌握词 / 错词只是纯展示，点击没反应——同一个页面两处词条，体验不一致。
- 解决了哪些问题？总览的词条现在点一下就能听到对应单词的韩语发音，随时快速复习今天学过的词。
- 希望提升哪些体验？总览词条与每日卡片统一为同一套点击逻辑和交互反馈，学习记录页所有词条都能点发音。

### ✨ 本次更新
**UI·UX 优化**
- 顶部今日总览的「今日掌握」词条（浅蓝标签）和「今日错词」词条（珊瑚色标签）改为与每日卡片同一套 `sdc-chip` 标签：可点击播放该词韩语发音，带手型光标和按压缩放反馈
- 点击逻辑抽成通用函数 `bindSpeakChips(el)`，顶部总览与每日列表共用，事件委托每个容器只绑定一次

### 👤 用户体验变化
- 修改前：顶部总览的掌握词 / 错词只是灰色文字块，点了没反应
- 修改后：点一下总览里任意词条（掌握 / 错词）直接播放对应单词的韩语发音，与每日卡片体验一致

### 💭 设计思考
- 为什么不单独写一份总览逻辑？总览词条与每日卡片词条本质是同一件事——「点词条听发音」，共用一套样式（`sdc-chip`）和一套点击逻辑（`bindSpeakChips`），以后维护只改一处
- 事件委托：用 `closest('.sdc-chip')` 只对词条响应，点击总览空白处不误触发

---

## 📅 2026-08-11 01:12 · v1.10.10

### 📝 一句话总结
学习记录页每日列表从紧凑三列改为「每天一张卡片」，与顶部今日总览同风格，小屏下不再换行错位；点卡片上的词直接播放发音；顶部今日总览淡蓝底+蓝框与下方统一灰边卡片层级分明。

### 🎯 本次更新目标
- 为什么修改？原每日列表是「日期｜掌握｜测验」三列小字，手机上测验列太窄，测验结果是 100% 这类较长文本会换行、把行撑开，看起来对不齐；信息密度也低，看不到"学习了几个词"和具体掌握的词。
- 解决了哪些问题？改为竖排卡片后每行信息完整（学习/掌握/测验/错词都展示），长文本天然放得下，不再换行错位；顶部今日测验文字颜色与其他行统一，不再显得浅。
- 希望提升哪些体验？记录页一眼看到每天的完整学习足迹，视觉与顶部今日总览统一，干净整齐。

### ✨ 本次更新
**UI·UX 优化**
- 每日记录列表：从三列紧凑布局改为「每天一张卡片」，每张卡片显示：日期 + 学习单词数 + 掌握词数（附当天掌握的具体词，浅蓝标签）+ 测验正确率 + 当天测验错词（珊瑚色标签）；没有的数据不显示对应行
- 卡片内容不带 emoji，措辞与顶部一致；今天的卡片右上角加「今天」小标签，边框与其它卡片统一（靠标签区分当天）
- 掌握词 / 错词各最多显示 8 个，更多词折叠在浅灰倒三角 ▾ 后，点击展开查看全部（再点收起），卡片不会因词多而变高
- **点击卡片上的任意词条直接播放该词韩语发音**：快速复习读音，词条有手型光标和按压缩放反馈
- 当天错词按天从测验记录取，与顶部「今日错词」同一套逻辑（抽出通用函数复用）
- 顶部今日总览「今日测验」文字颜色从浅灰改为与正文一致
- 顶部今日总览大框：淡蓝底色（5%）+ 蓝色边框，作为主框突出
- 下方每日卡片统一浅灰边框，每一张都有清晰的边框线（此前非今天卡片边框太浅、几乎看不见）

### 👤 用户体验变化
- 修改前：每日列表三列小字，手机上 100% 测验结果会换行，行与行对不齐；看不到"学习了几个词"和具体掌握的词
- 修改后：每天一张卡片，信息完整、手机上不换行、行行整齐；一眼看清每天的单词数、掌握词、正确率和错词；词多的日子折叠成「▾ 展开」，点开才看全部，卡片保持紧凑；点任意词条直接播放韩语发音，词条有手型光标和按压缩放反馈；顶部总览淡蓝底+蓝框与下方灰边卡片区分，今天靠「今天」标签标识

### 💭 设计思考
- 为什么用卡片？竖排卡片天然按内容高度排布，不存在横向挤占，小屏放长文本不会错位；同时与顶部今日总览卡片形成统一设计语言
- 层级：顶部今日总览用淡蓝底+蓝框作主框，下方每日卡片统一浅灰边框，一眼分清「主框」与「历史列表」
- 措辞取舍：每日记录写「错词」，不加「今日」（每张卡片代表具体某一天，"今日"只属于顶部今日总览）
- 复用：抽出 `dayQuizErrors(dayKey)`，顶部「今日错词」与每日卡片共用同一套按天取错词的逻辑，避免重复代码

---

## 📅 2026-08-10 · v1.10.9

### 📝 一句话总结
学习记录页每日列表改为「日期｜掌握｜测验」三列固定对齐（各归各列），设置页版本号去掉副名，主屏图标下小字固定显示「每日韩语」。

### ✨ 本次更新
- 学习记录页每日列表：固定三列左对齐（日期｜掌握｜测验），每一天各列起点一致，掌握对齐掌握、测验对齐测验；去掉原「复习 N 词」，只学过词、没有掌握也没有测验的日子不再出现在列表
- 设置页版本号：从「Daily Hangul (每日韩语) · v」改为「Daily Hangul · v」，版本号行只留主名
- 主屏图标下的小字：应用名统一为「每日韩语」，无论系统读哪个字段都显示中文；「Daily Hangul」保留在浏览器标签页标题与版本行

### 👤 用户体验变化
- 学习记录页每日列表更整齐，一眼对齐「掌握」与「测验」
- 手机/桌面主屏图标下的字变为「每日韩语」（已添加的图标需删除后重新添加一次才生效）

---

## 📅 2026-08-10 · v1.10.8

### 📝 一句话总结
单词/语法编号升级为「书ID+课号+词」结构（为多教材铺地基并自动迁移现有数据），单词列表页顶部补上短书名，全站书名改为读配置。

### 🎯 本次更新目标
- 为什么修改？当前编号是「课号+词」（如 `3|가족`），只适用于一本书；将来加第二本教材时两本书课号会撞车，学习进度会互相串。
- 解决了哪些问题？给所有单词/语法编号加上书 ID 前缀（如 `yonsei1|3|가족`），并自动迁移现有学习数据（掌握进度/收藏/语法收藏/语法掌握），让未来加书零风险、零迁移。
- 希望提升哪些体验？为多教材扩展铺平道路；同时补全单词列表页缺失的书名，界面信息更完整、更一致。

### ✨ 本次更新
**功能基础（对用户几乎不可见）**
- 单词唯一标识 `wk()` 升级为 `书ID|课号|词`，语法标识新增统一函数 `gk()`（`书ID|课号-序号`）
- 启动时自动迁移旧编号→新编号（幂等，只跑一次），迁移前自动备份一份原始数据到 `yonsei-study-data-backup`
- 导入旧格式备份同样自动迁移（与启动迁移逻辑一致），避免导入后进度显示"丢失"
- 首页/课程页/学习弹窗的书名从"写死"改为读配置 `APP_CONFIG`，显示不变

**UI·UX**
- 单词列表页顶部标题从「제1과 - 소개」改为「연세1 · 제1과 - 소개」（补齐全站唯一缺书名的地方）
- 设置页版本行从「연세 한국어 1 · v1.10.7」改为「Daily Hangul (每日韩语) · v1.10.8」（版本属于应用而非教材，书名不再混进版本号）
- 品牌名统一为「Daily Hangul」主名、「每日韩语」辅名：浏览器标签 / 安装应用名改为 Daily Hangul，手机主屏小字保留每日韩语

### 👤 用户体验变化
- 单词列表页顶部多显示一个短书名（연세1）；设置页版本行改为「Daily Hangul (每日韩语) · 版本号」；浏览器标签页显示 Daily Hangul；其余页面显示与之前完全一致
- 已安装的应用需重新添加到主屏，主屏小字才会显示新名字（浏览器标签与设置页即时生效）
- 学习进度、收藏、掌握状态全部保留（数据已自动迁移，无需任何手动操作）

### 💭 设计思考
- 书 ID 命名规范：yonsei1（延世1）、yonsei2（延世2）……为将来加教材预留
- 迁移幂等：检测到已是新格式就跳过，不会重复执行；先备份再迁移，异常可一键还原
- 遵循原则：为长期扩展铺地基，一次小改动避免将来大迁移；做减法，本次只动数据层与一处标题

---

## 📅 2026-08-10 17:07 · v1.10.7

### 📝 一句话总结
学习记录页每日历史记录改为「日期固定列 + 内容对齐」布局，修复内容因正确率长短而参差不齐的问题。

### 🎯 本次更新目标
- 为什么修改？记录行是两端对齐（日期靠左、内容靠右），带「测验正确率」的行文字更长、往左顶得更远，不带的行短，导致内容列左边缘参差不齐。
- 解决了哪些问题？所有行的日期与内容从固定位置起笔，正确率长短不再影响对齐。
- 希望提升哪些体验？记录页一眼扫过去整齐自然。

### ✨ 本次更新
**UI·UX 优化**
- 每日记录列表 `.stats-day`：flex 两端对齐 → 网格两列（日期固定 96px + 内容 1fr）
- 内容列加 `min-width: 0`，防止长文本撑破布局

### 👤 用户体验变化
- 修改前：每行右侧内容左边缘随文字长短参差不齐，带正确率的行更明显
- 修改后：日期、内容全部从固定位置起笔，行行对齐

### 💭 设计思考
- 备选方案：把正确率单独拆成最右一列（表格化）。不采用——这是学习日志而非数据表格，正确率是内容的一部分，拆列会碎片化、增加阅读成本
- 遵循原则：做减法、降低认知成本、信息层级清晰

### ⚠️ 开发记录
- 纯 CSS 改动（style.css），未触及 JS 渲染逻辑；版本三同步 1.10.7 + 缓存 hangul-v25→v26

## 📅 2026-08-10 02:04 · v1.10.6

### 📝 一句话总结
代码重构收官：index.html 主脚本全部拆出，形成 app.js / study.js / stats.js / quiz.js 四个独立模块，index.html 从 3597 行降到 648 行；页面行为完全不变。

### 🎯 本次更新目标
- 为什么修改？v1.10.5 拆出测验后，index.html 仍有约 2200 行主脚本。按 CLAUDE.md「渐进式重构」完成剩余拆分。
- 解决了哪些问题？把 3000+ 行内嵌脚本按职责拆成四个文件：核心层（导航/首页/设置/数据）、学习层（闪卡/收藏/搜索/易错本）、统计层（学习记录）、测验层。每文件几百到一千行，可整本阅读，新功能按模块入对应文件。

### ✨ 本次更新
**代码重构**
- 新建 app.js（958 行）：全局状态、页面导航、首页与单词列表、设置、数据持久化、打卡/每日日志
- 新建 study.js（1052 行）：闪卡学习、发音、收藏页、搜索、易错本、复习池、键盘与滑动手势
- 新建 stats.js（202 行）：学习记录页、今日分析
- 沿用 quiz.js（750 行）：测验模块（v1.10.5 已拆）
- index.html 脚本区：data.js → utils.js → app.js → study.js → stats.js → quiz.js → 启动脚本
- 初始化与「刷新恢复位置」逻辑归并到末尾启动脚本（确保模块均已加载）
- sw.js 缓存加入 app/study/stats.js（v24→v25），data.js 版本号同步 1.10.6

### 👤 用户体验变化
- 修改前 / 修改后：无用户可见变化（纯代码重构），所有功能行为一致，离线秒开不受影响。

### 💭 设计思考
- 按「层」划分：核心 → 学习 → 统计 → 测验，依赖方向单一（app 最早加载，quiz 最后），避免循环依赖。
- 加载顺序硬约束：quiz.js 的 showPage 覆盖需先有原函数（app.js 先加载）；启动逻辑调用各模块函数，放最后。
- 遵循原则：渐进式重构、小步修改、可维护性优先。

### ⚠️ 开发记录
- 状态变量统一放 app.js 顶部（starred/srs/studyWords 等），各文件运行时共享全局，沿用 v1.10.5 已验证的模式。
- 校验手段：96 个函数逐一核对各归其位（0 缺失 0 重复）、逐行比对内容、全部语法检查、HTML onclick 引用核对。

### 📌 下一步建议
- CLAUDE.md 增加规则：新功能代码必须写入独立 JS 文件，禁止内联进 index.html
- 将约 150 处内联样式提炼为 style.css 的 class
- 抽取重复 3~4 份的 word-item 卡片渲染为 renderWordItem()
- 清理死代码（srsDueCount、starTabData 等）

---

## 📅 2026-08-10 01:46 · v1.10.5

### 📝 一句话总结
代码重构第一步：把测验模块从 index.html 拆为独立文件 quiz.js，index.html 从 3597 行降到 2854 行，页面行为完全不变。

### 🎯 本次更新目标
- 为什么修改？index.html 长期是"大杂烩"（HTML + 全部 JS 揉在一起，3597 行），新功能只能往里加，半年后难维护；按 CLAUDE.md「渐进式重构」原则开始拆分。
- 解决了哪些问题？以边界最清晰的测验模块做第一个试点，验证"纯搬家不改逻辑"的流程，为后续拆分学习/统计/设置模块铺路。

### ✨ 本次更新
**代码重构**
- 测验模块（29 个函数 + 状态）迁入独立 quiz.js
- 脚本加载顺序调整为：data.js → utils.js → 主脚本 → quiz.js → 启动块
- 「刷新恢复位置」逻辑移到末尾，确保 quiz.js 加载后再执行
- sw.js 缓存加入 quiz.js（v23→v24），data.js 版本号同步 1.10.5

### 👤 用户体验变化
- 修改前 / 修改后：无用户可见变化（纯代码重构），测验与各功能行为一致；sw.js 依旧缓存全部资源，离线秒开不受影响。

### 💭 设计思考
- 先做"搬文件不改逻辑"，避免重构引入行为差异；确认无变化后再迁移内联样式、继续拆模块。
- 加载顺序：quiz.js 内覆盖 showPage 需捕获原函数，故放主脚本后；启动块依赖测验函数，故放最末。
- 遵循原则：渐进式重构、小步修改、可维护性优先。

### ⚠️ 开发记录
- 坑：最初想放主脚本之前，发现 showPage 覆盖逻辑会因捕获不到原函数而失效；改为放主脚本之后，并把 initHash 启动块同步挪到末尾。
- 经验：拆文件时，"文件一加载就执行的代码"必须跟着它依赖的函数走；函数声明靠运行时解析，天然跨文件安全。

### 📌 下一步建议
- 继续渐进式拆分：学习 / 统计 / 设置模块独立成文件
- 将 151 处内联样式提炼为 style.css 的 class（利于深色模式与统一修改）
- 抽取重复 3~4 份的 word-item 卡片渲染为 renderWordItem()
- CLAUDE.md 增加规则：新功能代码必须写入独立 JS 文件，不得内联进 index.html
- 清理死代码（srsDueCount、starTabData 等）

---

## 📅 2026-08-10 01:33 · v1.10.4

### 📝 一句话总结
一次"信息层级与一致性"整理：语法 Tab 不再残留单词过滤栏、首页进度条数字与条形口径统一、搜索入口补到三个列表页；顺带统一退出测验的文案与行为、学完弹窗加返回首页、测验进度条满格、emoji 数组去重、开放学习文本长按复制。

### 🎯 本次更新目标
- **为什么修改？** ① 单词页「全部/未掌握/已掌握」过滤栏在语法 Tab 下仍显示，点了还会跳回单词页，用户会误以为语法也有掌握筛选；② 首页进度条标签（已掌握 X/Y）和条形（已掌握+学习中）口径不一致，数字 35% 条却 50%；③ 搜索是全局功能，入口却只有首页角落一个 🔍；④ 退出测验出现「会计入测验记录 / 进度将丢失」前后矛盾的文案与行为；⑤ 全局禁选文本，想长按复制韩文去查词做不到。
- **解决了哪些问题？** 语法页界面干净、不误导；进度条诚实且与数字一致；凡是能看到词列表的地方都能搜索；任何方式退出测验，行为与文案统一；核心学习文本可复制。
- **希望提升哪些体验？** 层级更清楚、数字不打架、全局功能找得到、退出不矛盾、查词更方便。

### ✨ 本次更新
- **UI·UX 优化（层级）**：语法 Tab 隐藏「全部/未掌握/已掌握」过滤栏（此前残留显示、点击还会跳回单词页）；首页进度条口径改为只算「已掌握」，与每课进度条及「已掌握 X/Y」数字完全一致。
- **功能优化（搜索入口）**：单词列表页、收藏页、易错本页标题栏都补上 🔍 搜索按钮（样式早已存在），搜索从"只有首页能进"变成"列表页全局可进"。
- **功能修复（退出测验一致性）**：抽出统一逻辑 `exitQuizWithConfirm()`，「✕ 退出」「底部导航测验」「测验中切换页面」三条路径统一——文案都是「已答的 X 题会计入测验记录」，且都持久化已答部分；同时删除从未被调用的死代码 `quizNavGuard`。
- **UI·UX 优化（学完收尾）**：学完弹窗由单一「完成」改为「🏠 返回首页」（主）+「留在本页」（副），返回前自动停止发音，不再被困在学习页手动按返回。
- **UI·UX 优化（测验进度条）**：进度按「已到第几题」计算，最后一道题满格 100%（此前最后一题只到 90%）。
- **代码重构（emoji 去重）**：首页随机 emoji 数组原写了两份（回到首页/启动各一份），并成一份 `HOME_EMOJIS` 常量，内容逐字符一致、显示零变化，以后加 emoji 只改一处。
- **功能优化（文本可选中）**：单词/例句/语法/卡片的学习文本开放长按选中（方便复制去查词），按钮等交互控件保持不可选中，防快速点按误选。

### 👤 用户体验变化
- 修改前：语法页还挂着单词过滤栏；进度条数字与条形口径打架；搜索只有首页能进；「✕ 退出」说会计入、「底部导航退出」却说进度丢失；学完只能留在学习页手动按返回；测验最后一题进度条不到头；学习内容长按不能复制。
- 修改后：层级清楚、数字一致、搜索全局可进、退出行为与文案统一、学完一键回首页、进度条满格、核心文本可长按复制。

### 💭 设计思考
- **进度条为什么只算已掌握？** 「掌握进度」只应体现掌握——「学习中」的词本就不该推动掌握进度；且每课进度条本来就是只算已掌握，顶部条统一口径后整页数字与图形才完全对应。
- **为什么三个退出路径统一持久化已答部分？** 同样是"退出测验"，没有理由让「✕ 退出」保留已答、「切页退出」丢弃——数据应绑定在行为（答了题）上，而不是绑定在退出方式上。统一后任何退出都不白做。
- **搜索为什么补到三个列表页？** 功能是全局的，入口就不能是局部的；凡是能看词列表的地方，用户就可能有"查个词"的需求。
- **遵循原则**：信息层级清晰、降低用户思考成本、做减法（删死代码）。

### 📌 下一步建议
- 搜索从别的页面进入后，「▶ 学习」的返回目的地目前统一回首页，可考虑带回"从哪进来回哪去"。
- 单词列表页每张卡片仍有多达 4 个可点区（发音/收藏/掌握/学习），后续可评估弱化次要操作、突出发音这一主操作。
- `updateHomeStats` 中的 `learningCount` 等统计变量已无实际用途，后续清理时可一并移除。

---

## 📅 2026-08-10 01:06 · v1.10.3

### 📝 一句话总结
学习词数从"进入整组词规模"改为"当天实际看过的去重单词数"——数字不再虚高、也不因手机退出方式漏记；sw.js 应用入口改「网络优先」，版本更新刷新一次即生效。

### 🎯 本次更新目标
- **为什么修改？** ① v1.10.2 按"进入的整组词规模"累计学习词数：一组词进一次加一次、到期词池动辄上百、跨组重复计，数字明显虚高，与"今天实际学了几个词"的直觉不符；② sw.js 仍对所有请求缓存优先，应用入口旧版会一直显示、刷新也难以更新到新版。
- **解决了哪些问题？** ① 学习词数改为逐词记录、当天去重——翻到一张卡记一个词，反复看不重复计，等于"今天真正看过的词数"；② 导航请求（index.html）改为网络优先，每次打开先取最新版、断网才回退缓存。
- **希望提升哪些体验？** 打开记录页看到的学习词数就是实际学过的词；版本更新后刷新一次即可看到新版，不再"为什么还是旧的"。

### ✨ 本次更新
- **功能修复（学习词数去重）**：新增 `markWordStudied` 逐词记录 + 每日日志 `studiedKeys` 去重字段；`showCard` 翻卡即记、反复看不重复；移除 `startStudyFromContext` 按整组规模累计。
- **代码修复（sw.js 网络优先）**：导航请求（index.html）由缓存优先改为网络优先，断网才回退缓存；CSS/JS/data.js 等资源保持缓存优先（秒开不受影响）；缓存号 hangul-v21→**v22**，强制已安装 PWA 清理一次旧缓存。

### 👤 用户体验变化
- 修改前：学习记录页"今天学习 X 个单词"按进入的整组词累计，数字虚高（反复进出、跨组重复、到期池上百）；手机侧滑/系统返回退出还会漏记；应用入口缓存优先，版本更新后刷新仍可能显示旧版。
- 修改后：数字 = 今天实际看过的去重单词数，怎么退出都不丢、也不会虚高；版本更新刷新一次即生效。

### 💭 设计思考
- **为什么逐词记录而不是按"组规模"计？** ① 记录应绑定在行为发生的那一刻（看到一张卡），而不是依赖某个出口——退出路径（返回箭头/侧滑/系统返回/直接关页面）无法穷尽，挂在任何一处都有漏记风险；② "组规模"不是用户心智里的"学了多少"——到期池动辄上百、反复进出翻倍、跨组重复计，观感失真。逐词去重后，数字才等于真正学过的词。
- **为什么只把导航请求改网络优先？** 应用入口（index.html）必须始终最新，值得每次等一次网络；CSS/JS/data.js 这类大资源缓存优先才能秒开。两类分开，速度与"总是新版"兼得。

### ⚠️ 开发记录
- 去重计数沿用 `markMastered` 同一套模式（日志内 `*Keys` 数组 + 计数），每日日志结构新增 `studiedKeys`，旧数据兼容（缺省 `|| []`）。

### 📌 下一步建议
- 打卡框已积累 6 项指标（连续打卡/累计掌握/今天学习/今天掌握/今日测验/今日错词），若后续再增加，建议把低频数据（如"本月掌握·累计测验"）下移或折叠，保持首屏一眼能看懂。
- 首页今日小结"学了 X 词"与记录页已同口径（去重），可顺手核实两处数字一致。
- 若未来想统计"连续多天分别学了哪些词"，`studiedKeys` 已可直接复用。

---

## 📅 2026-08-10 00:31 · v1.10.2

### 📝 一句话总结
移除 Google Fonts（826KB 渲染阻塞 CSS + 900 个字体子集、国内被墙），改用各设备自带的系统字体——页面秒开、不再依赖任何境外网络。另：学习词数改为"进入学习即记录"（修复手机侧滑/系统返回退出漏记），学习记录页打卡框新增「今天学习了 X 个单词」。

### 🎯 本次更新目标
- **为什么修改？** 每次打开站点都白屏几十秒甚至更久：`<head>` 里的 Google Fonts 样式表是**渲染阻塞**的，而它在真实浏览器下是一份 **826KB 的 CSS、引用 900 个字体子集文件**，且 `fonts.gstatic.com` 在国内被墙/极慢，页面必须等它超时才能画第一帧。
- **解决了哪些问题？** ① 首屏渲染不再被境外网络卡住；② v1.10.1 的 SW 缓存策略改版（v20 缓存优先）会清空旧缓存，之前攒下的字体缓存被删、字体又永远不会被写入新缓存，导致"每次打开都慢"从偶发变成必然——本次一并根治；③ 移除唯一的外部依赖后，站点彻底自足，不受 GFW 波动影响。
- **希望提升哪些体验？** 打开即见，离线秒开，学习流程从"等加载"变成"直接开始"。
- **为什么修改（学习词数）？** 学习词数此前只在点左上角返回箭头退出时才记录，手机上用侧滑/系统返回退出会直接漏记，导致学习记录页"今日学习词数"缺失或偏小——同一份代码电脑有、手机没有。
- **解决了哪些问题？** 记录时机改为进入学习即记录，与退出方式完全解绑；打卡框新增始终显示的「今天学习了 X 个单词」，不再因条件隐藏让人误以为功能不存在。
- **希望提升哪些体验？** 学习记录页如实反映当天学了多少词，怎么退出都不丢数据。

### ✨ 本次更新
- **性能优化**：删除 Google Fonts 全部引用（2 个 `preconnect` + 1 个 `rel="stylesheet"`），首屏不再有任何渲染阻塞的外部请求。全站外部依赖只剩发音按钮触发的 Google TTS（用户点击时才请求）。
- **UI·UX 优化**：字体栈改为跨平台系统字体——
  - `--font-display`（标题/韩文主展示）：`Apple SD Gothic Neo → Malgun Gothic → Noto Sans KR → PingFang SC → Microsoft YaHei → sans-serif`
  - `--font-body`（正文/中文）：`PingFang SC → Microsoft YaHei → Apple SD Gothic Neo → Malgun Gothic → Noto Sans KR → sans-serif`
  - Android 系统自带的就是 Noto Sans KR 本体，iOS/macOS 用 Apple SD Gothic Neo、Windows 用 Malgun Gothic，观感差异极小。
- **代码修复**：`sw.js` 缓存优先的回退逻辑修正——跨域/资源请求失败时不再返回 `index.html`（会把 HTML 当 CSS/JS 解析），改为**仅导航请求回退首页**、其余资源失败就失败。
- **版本号同步**：`APP_CONFIG.version` 1.10.1→1.10.2；`sw.js` 缓存 hangul-v20→**v21**，确保已安装 PWA / 桌面应用能刷新缓存。
- **功能修复**：学习词数统计时机从「退出学习」提前到「进入学习」——此前只有点左上角返回箭头退出才会记录，手机上用侧滑/系统返回退出会漏记，导致学习记录页的"今日学习词数"缺失或偏小（打卡不受影响，因为打卡在看卡片时就记录）。
- **UI·UX 优化**：学习记录页打卡框新增一行「📖 今天学习了 X 个单词」（始终显示、与"今天掌握"同款字号颜色），替换原先靠后、字小偏暗、仅在有学习时才出现的「✍️ 今日复习」行。

### 👤 用户体验变化
- 修改前：打开页面先白屏等待几十秒（Google 字体渲染阻塞 + 被墙超时），装过 PWA 后偶发快、经常慢。
- 修改后：首屏立即渲染，无任何境外网络依赖，装过 PWA 后彻底秒开离线可用。
- 学习流程：进入站点第一件事从"等页面"变成"开始学习"。
- 学习记录：学习词数改为进入学习即记录，手机侧滑/系统返回退出不再漏记；打卡框顶部新增「今天学习了 X 个单词」，打开记录页第一眼就能看到今天学了几个词。

### 💭 设计思考
- **为什么用系统字体而不是保留 Noto？** 每个平台的系统韩文字体本身就是 Google 在用的那套（Android 直接就是 Noto Sans KR），学习场景下跨设备观感差异可以忽略；而保留 Noto 的三条路各有硬伤——国内镜像站（fonts.loli.net 等）是社区维护、随时可能挂掉；自托管 KR 变体字体需额外 ~2.8MB 首屏体积且加大仓库；异步加载在国内网络下字体请求全失败、等于白等。做减法：一个每天都在用的学习工具，加载速度 > 字体像素级一致。
- **遵循原则**：降低用户思考成本、做减法、长期可维护。
- **为什么把"记录"放在进入学习而不是退出？** 数据记录应绑定在行为发生的那一刻，而不是依赖某个出口——退出路径（返回箭头/侧滑/系统返回/直接关页面）无法穷尽，挂在任何一处都有漏记风险。"进入学习"是用户明确的"开始学"信号，语义最稳，也天然覆盖所有退出方式。

### ⚠️ 开发记录
- **排查经验（值得保留）**：用 `curl` 测 Google Fonts 时，默认 UA 只拿到 **1.8KB** 的 CSS、0 个 woff2，看起来"字体没问题"；换真实 Chrome UA 后同一地址返回 **826KB**、引用 **900** 个字体子集。**测外部资源的性能必须带真实浏览器 UA，否则结论完全失真。** 排查中还实测了 Noto Sans KR 单字重全部分片约 3MB、国内镜像站响应 2.9–4.7s，为方案取舍提供了真实数据。
- **排查经验（学习词数漏记）**：同一份代码电脑有显示、手机没有，第一反应先查缓存/版本（版本确实一致），真正根因是**移动端侧滑/系统返回走 popstate 绕过了 `exitStudy`**——只有"退出生效"的数据会在手机上漏记。判断"数据挂在哪一事件上"，比反复刷新缓存能更快定位这类跨设备差异。

### 📌 下一步建议
- 首页/各页可继续核实中文与韩文混排下系统字体的行高表现，如个别元素出现挤压可微调 `line-height`。
- 发音 TTS（translate.google.com）在国内同样不可靠，未来可考虑替换为可用的国内 TTS 或预置发音音频。
- 若未来想要统一排版，可评估自托管 Noto Sans KR **变体字体**（1 个文件 ~2.8MB 覆盖全部字重），届时按 OFL 许可证附上许可证文本即可。
- 打卡框已积累 6 项指标（连续打卡/累计掌握/今天学习/今天掌握/今日测验/今日错词），若后续再增加，建议把低频数据（如"本月掌握·累计测验"）下移或折叠，保持首屏一眼能看懂。
- "今天学习了 X 个单词"统计的是进入的整组词数，若想更精准可改为实际翻看过的卡片数（需给每张卡加"已看"标记）。

---

## 📅 2026-08-09 02:40 · v1.10.1

### 📝 一句话总结
把"一天"的日界线从凌晨 4:00 提前到凌晨 2:00——凌晨 0:00–1:59 算前一天，2:00 起算新的一天。

### 🎯 本次更新目标
- **为什么修改？** 凌晨 4 点前还在学习的情况很少，4:00 分界过晚，导致"昨天深夜学的内容"长期挂在旧一天上，记录观感延迟。
- **解决了哪些问题？** 日界线更贴近实际作息，打卡 / 每日记录"翻篇"更符合直觉。
- **希望提升哪些体验？** 打开记录页时，"今天"从凌晨 2 点开始，前一天的记录更早收尾。

### ✨ 本次更新
- **功能优化**：`getStudyDay` 日界线 4:00 → 2:00（0:00–1:59 仍算前一天，"熬夜不断卡"逻辑保留）。
- **UI·UX 优化（解释小字 `.ui-hint`）**：统一左对齐（此前未设对齐、会继承父级居中）、行高 1.4、**上下边距等宽（4px）**，整体不占地方；记录页的打卡/日界线说明移到**"学习记录"标题正下方**（与标题对齐），文案改为"每天学习就算打卡 · 次日 2:00 前为前一天"；测验模式的说明小字移到**模式按钮上方**，且精确居中于"测验/记录"页签与模式按钮两排之间（上下各 4px）；单词页"学习当前筛选的 X 个词"移到**开始学习按钮上方**并去掉居中；易错本说明文字改等宽边距；首页进度条"学习记录 ›"小字由右对齐改为**左对齐**。
- **版本号同步**：`APP_CONFIG.version` 1.10.0→1.10.1；[sw.js](sw.js) 缓存 hangul-v18→**v20**，fetch 策略改为**缓存优先**（命中缓存立即返回、秒开，后台静默更新本站资源）。

### 👤 用户体验变化
- 修改前：凌晨 0:00–3:59 的学习都算前一天，前一天记录拖到早上 4 点才翻篇；解释小字在部分页面呈居中、间距偏大。
- 修改后：凌晨 2:00 起算新的一天，记录和打卡更贴近作息；解释小字统一左对齐、更紧凑，记录页标注置于卡片顶部并写明"02:00 翻篇"。

### 💭 设计思考
- 保留"凌晨熬夜不断卡"的机制，只是把边界从 4:00 收紧到 2:00，兼顾记录及时性与夜猫子友好。

---

## 📅 2026-08-09 01:36 · v1.10.0

### 📝 一句话总结
每日记录从"学过几个"改为"真正掌握了几个（已掌握）"——记录学习**成果**，而不是学习**动作**。

### 🎯 本次更新目标
- **为什么修改？** 原记录页记的是 `words`（今天翻牌学过的词数），但"学过"不等于"学会"。用户真正想知道的是"今天掌握了几个词、测验正确率多少"。
- **解决了哪些问题？** ① 记录口径从"动作"改为"成果"（已掌握 = SRS lv≥4）；② 逐日流水账带满屏"没有学习"空行，噪音太大；③ 测验历史在记录页和测验页重复渲染两遍。
- **希望提升哪些体验？** 打开记录页一眼看到"今天学会了什么、测验怎么样"，历史干净可回看。

### ✨ 本次更新
- **新增功能（成果记录）**：① 每日日志新增"当天新掌握词数 + 具体词"（`mastered`/`masteredKeys`），词达到已掌握（SRS lv≥4）的那天被记入；② 今日总览卡重构——连续打卡 + 累计已掌握（X/总词数）+ 今日掌握词 chips + 今日测验正确率 + 今日复习量 + 今日错词 chips + 本月掌握；③ 已掌握词与错词统一为小标签（chip）样式，主色 / 珊瑚色区分"收获 / 待补"。
- **功能优化（记录列表）**：每日列表改为**只显示有学习记录的日子**，按自然月分组、倒序（最新在前），彻底移除"没有学习"空行和逐天 while 循环；每行按优先级展示"掌握 X 词 > 测验 Y/Z（P%）> 复习 X 词"。
- **功能优化（消除重复）**：记录页移除整份测验历史渲染（保留在测验页"记录"tab）；今日错词仍从测验记录取，只展示不点击。
- **代码重构**：新增 `markMastered` / `unmarkMastered` 单一入口写入、清除"掌握日期戳"，接入学习页掌握按钮、单词列表徽标、批量标记、测验降级四处；删除废弃的 `cumulativeWords` 与 `.stats-week-label` / `.stats-info.empty` 样式。
- **版本号同步**：`APP_CONFIG.version` 1.9.1→1.10.0；[sw.js](sw.js) 缓存 hangul-v17→v18。

### 👤 用户体验变化
- 修改前：记录页显示"今天学了 15 个词"（翻牌就算），夹着一堆"没有学习"的灰色空行，测验历史重复出现。
- 修改后：记录页主打"今天掌握了 3 个词"，能直接看到是哪几个词；测验正确率、今日复习量、今日错词并列；历史只剩真实学习过的日子、按月排列。
- 学习流程：打开记录页即知"学会了什么、哪里还差"；空态时引导"去学习"。

### 💭 设计思考
- **为什么以"已掌握"为度量？** "已掌握"（SRS lv≥4）是会被测验检验的状态（答错降级），是真正可验证的成果；"学过"只是过程。符合"记录成果而非动作"的产品逻辑。
- **为什么"今日复习量"仍保留？** 掌握是稀疏成果（多数天 0-3 词），若只讲掌握，用户会误以为自己白学。复习量作为背景信息，避免本末倒置。
- **为什么 chips 不可点击？** 用户明确不需要"点词复习"，纯展示、控交互成本。
- **为什么记录页移除测验历史？** 测验历史在测验页"记录"tab 已有完整版本，重复渲染违背"一处维护、减少重复信息"。

### ⚠️ 开发记录
- **历史空窗**：改版前已掌握的词没有"掌握日期"，无法回溯进每日记录（累计数字不受影响），是本次不可逆的数据限制。
- **降级后重新掌握**：词被测验降级后再次掌握，会作为新的"成果事件"再记一次，属预期设计（日志是事件账本，不回改）。
- **边界 bug 修复**：今日总览的空状态最初只判断"今天 + 连续打卡"，会让老用户（有历史但今天空）误显示"还没有学习记录"；已改为检测任意历史足迹（日志或 srs）。
- **流程文档更新**：本次合入 [CLAUDE.md](CLAUDE.md) 新增的「新功能 / 大改动评审机制」（最高优先级，13 步评审）——纯流程文档变更，无代码。后续新功能将先走评审、确认后再开发。

### 📌 下一步建议
1. 首页今日摘要（`renderTodaySummary`）仍显示"学了 X 词"，与记录页"掌握"口径不一致，建议统一。
2. 等真实数据积累 1-2 周后，可加"最近 14 天掌握数"轻量趋势。
3. 可考虑给"已掌握"加半自动判定（复习到次数自动升级），但那是交互改动、与本次记录无关，另行立项。
4. srs 条目的 `masteredAt` 字段可为未来的"周回顾 / 复习计划"直接复用。

---

## 📅 2026-08-06 16:46 · v1.9.1

### 📝 一句话总结
全站补上"解释小字"让每个入口都能被看懂，并加入统一的视觉反馈（按压微缩 / 状态切换弹一下 / 答错震动），界面"活"起来。

### 🎯 本次更新目标
- **为什么修改？** ① 很多功能用户不知道能点、不知道为什么——比如学习记录入口、易错本原理、听写怎么玩；② 点击没有任何即时反馈（手机上尤其明显），操作"死气沉沉"。
- **解决了哪些问题？** ① 关键位置加了浅色小字说明，可发现性提升；② 全站统一按压手感 + 状态切换有"弹一下" + 答错会"抖一下"。
- **希望提升哪些体验？** 打开就知道点哪里、按下去就有反应。

### ✨ 本次更新
- **新增功能（视觉反馈）**：① 全站按压微缩——按钮/选项卡/词卡/列表项按下瞬间缩到 94% 弹回；② 状态切换弹一下——收藏星标、语法星标/掌握、闪卡学习页掌握按钮点成功时放大回弹；③ 测验答错时选错的选项左右抖动。
- **UI·UX 优化（解释小字）**：新增统一 `.ui-hint` 样式（11px 浅灰，不占地方），加到六处——首页进度条「学习记录 ›」、易错本原理说明、测验模式栏随选中模式换说明（听音/听写等）、学习记录页连续打卡说明、单词页「开始学习」筛选说明、今日小结「查看详情 ›」（复用此前未接线的 `goToQuizAnalysis`，点击跳测验今日分析）。
- **功能优化**：单词页顶部「开始学习」按钮改为**学习当前筛选的词**（此前学整课），与说明文字一致、更符合直觉。
- **版本号同步**：`APP_CONFIG.version` 1.9.0→1.9.1；[sw.js](sw.js) 缓存 hangul-v16→v17。

### 👤 用户体验变化
- 修改前：学习记录入口没有提示、易错本不解释来源、听写不知道怎么玩；点击没有即时反馈。
- 修改后：关键入口有浅色小字说明；按按钮有按压手感，收藏/掌握有回弹，答错会抖动，界面有"生命感"。

### 💭 设计思考
- **为什么解释小字用"无背景纯文字"？** 和一次性"首次提示"（蓝底横幅）区分：常驻说明就该淡、不抢注意，`11px + text-dim` 浅灰最小侵入。
- **为什么说明放控制下方？** "先选模式、再看它干什么"符合阅读顺序，且随选择响应。
- **为什么视觉反馈分三层而不是全用震动？** 真震动 iOS 不支持且易烦躁；用 CSS 动画（微缩/回弹/抖动）跨端一致、可精确控制强度。按压微缩全站统一，回弹只给状态切换，抖动只给答错——克制不浮夸。
- **为什么「开始学习」改学筛选词？** 用户筛"未掌握"却学到整课是反直觉的；改成学筛选结果后，"开始学习"按钮与说明、与用户意图三者一致。

### ⚠️ 开发记录
- 视觉反馈全部用 CSS 动画 + 一个 `popEl()` 工具函数，无第三方依赖；排除闪卡（翻转动画）和发音按钮（呼吸动画）避免 transform 冲突。
- 「开始学习」行为改动是本版唯一的行为变化，已如实记录，如需回退说一声。

### 📌 下一步建议
- 触觉（真震动）仍可作进阶选项，安卓上答错加 `navigator.vibrate`（iOS 自动忽略）。
- 首页课程列表与「课程」tab 重复的减法，可继续考虑。
- 听写宽容可做成设置开关。
- 云同步（远期）。

---

## 📅 2026-08-06 16:23 · v1.9.0

### 📝 一句话总结
新增「学习记录」页（每日学习日志 + 按周/月归组的打卡列表 + 测验记录合并）；同时打磨了测验和听写体验：上一题锁定、听写宽容判分、已掌握降级提示、测验范围可只测已掌握。

### 🎯 本次更新目标
- **为什么修改？** ① 学了没积累感——每天的学习成果用完即弃，打开网站看不到"坚持"；② 测验能回改分数、听写音近字直接判错、已掌握词被悄悄降级却不解释，都是"使用不舒服"的小问题。
- **解决了哪些问题？** ① 每天学了什么按日期沉淀，还能按周/月回看；② 上一题改不了分、听写更宽容、降级有提示、可只测已掌握词巩固。
- **希望提升哪些体验？** 长期坚持有反馈、操作更放心、概念更清楚。

### ✨ 本次更新
- **新增功能**：「学习记录」页——首页点「已掌握 X/N」进度条进入；坚持总览（连续打卡 / 今天 / 累计）+ **每日打卡列表**（从第一天到今天，每天一行，按 月 → 周 归组，没学显示"没有学习"，今天高亮）+ 测验记录（复用现有渲染）。数据存 localStorage（`ys-daily-log`），关掉再开不丢；首次使用显示空状态鼓励。
- **功能优化**：测验「上一题」回看已答的题变为**锁定回顾态**（选项禁用、显示正误，改不了分）；听写判分加**宽容模式**（编辑距离 ≤1 且词长 ≥3 算对，显示「很接近 · 正确写法」）；测验范围新增「✅ 已掌握」。
- **UI·UX 优化**：已掌握词在测验答错 / 闪卡评「忘记」而降级时，给出明确提示（测验结果卡 + toast）；今日分析的按时段统计改为「上午/下午/晚上答对 X/Y · N%」更清楚。
- **版本号同步**：`APP_CONFIG.version` 1.8.1→1.9.0；[sw.js](sw.js) 缓存 hangul-v15→v16。

### 👤 用户体验变化
- 修改前：学了没积累感，刷新/关掉就清零；测验能回改分数；听写音近字直接判错；已掌握词悄悄降级进易错本让人困惑。
- 修改后：每天的学习都沉淀成可见记录；测验上一题只能回看；听写更宽容；降级有解释；可以只测已掌握词巩固。

### 💭 设计思考
- **为什么学习记录页从首页进度条进入？** 不加底部导航、不挤首页，进度条本就可点击，顺手的直觉。
- **为什么测验历史合并进记录页？** 避免两个"记录"入口；记录页 = 学习总览 + 测验明细，测验页「记录」tab 保留作快捷入口（两扇门进同一间屋，内容不重复）。
- **为什么听写宽容设"词长 ≥3"才生效？** 短词（1~2 字）改一格就变义（네/내、사과/사고），必须全对防误导。
- **为什么「上一题」用锁定回顾而不是禁用？** 回看题目是需求，但不能改分，锁定回顾两全。
- **为什么已掌握降级保留并提示（方案B）而不是硬锁（方案A）？** 保留"答错该重学"的学习科学性，同时用提示消除"打勾了怎么还在易错本"的困惑。

### ⚠️ 开发记录
- 测验历史渲染 `renderQuizHistory` / `renderTodayAnalysis` 参数化（containerId），学习记录页与测验页共用一份代码，避免复制。
- 学习记录页是「学习统计」的地基——以后打卡可视化、每日目标、周报都能长在 `ys-daily-log` 上。
- 每日日志**从现在开始记录**，此前的学习没有历史明细可回溯。

### 📌 下一步建议
- 首页课程列表与底部「课程」tab 重复的减法（用户此前选择不改，仍建议以后考虑）。
- 学习统计下一步：打卡可视化日历、每日目标。
- 听写宽容可做成设置开关（严格 / 宽容）供进阶用户选择。
- 云同步（远期）。

---

## 📅 2026-08-06 15:34 · v1.8.1

### 📝 一句话总结
修复单词列表点「掌握」徽标后整页跳回顶部——现在列表原地刷新，视线不丢。

### 🎯 本次更新目标
- **为什么修改？** 点「掌握」时词卡会刷新列表（已掌握的词滑到底部 / 按筛选消失），但刷新走的是 `openLesson → navigateTo → showPage`，`showPage` 固定 `scrollTo(0)`，导致整页跳回顶部，用户丢失阅读位置。
- **解决了哪些问题？** 点击掌握后列表在原地刷新，滚动位置保持。
- **希望提升哪些体验？** 连续标记多个词时不再反复被拉回顶部，浏览不打断。

### ✨ 本次更新
- **Bug 修复**：掌握徽标点击处理器在刷新前记住 `window.scrollY`，刷新后用 `scrollTo(top: savedY, behavior: 'auto')` 还原，抵消 `showPage` 的平滑滚动到顶。一处改动覆盖课程 / 收藏 / 易错本 / 搜索四个列表。
- **版本号同步**：`APP_CONFIG.version` 1.8.0→1.8.1；[sw.js](sw.js) 缓存 hangul-v14→v15。

### 👤 用户体验变化
- 修改前：往下滚动后点「掌握」，整页立刻滚回顶部，位置丢失。
- 修改后：点「掌握」后词滑到底部，但视线停在原地，继续标记下一个词很流畅。

### 💭 设计思考
- **为什么用"记住位置再还原"而不是改 openLesson？** `showPage` 的 scrollTo(0) 是"切换页面回到顶部"的通用行为，不该为这个场景破坏；在徽标处理器里局部还原，改动最小、覆盖最广。
- **为什么还原用 behavior:'auto'？** 抵消 showPage 的平滑滚动，避免两个滚动动画打架。

### ⚠️ 开发记录
- 根因链路：徽标点击 → `openLesson()` 重渲染 → 末尾 `navigateTo('page-words')` → `showPage()` → `window.scrollTo({top:0,smooth})`。刷新本应是"就地"的，却被当成了"重新进入页面"。

### 📌 下一步建议
- 以后新增"就地刷新"类操作时，注意避开 `openLesson`/`showPage` 的导航+滚动副作用，可考虑给这些函数加 `inPlace` 参数统一处理。

---

## 📅 2026-08-04 01:04 · v1.8.0

### 📝 一句话总结
新增「✍️ 听写」测验模式（听韩语打出单词），测验中途退出保留记录；完成全站响应式适配，词卡在手机/平板/电脑上统一保持双列。

### 🎯 本次更新目标
- **为什么修改？** ① 之前测验只有"再认"型选择题（韩→中 / 中→韩 / 听音），缺少"回忆"型练习——听写是记忆强度更高的一档，是"感觉学习方法太少"的核心解法；② 测验中途退出成绩全丢，答过的题不记录；③ 桌面端是一条窄竖条、手机窄屏词卡拥挤，缺少适配层。
- **解决了哪些问题？** ① 有了从"认得出"到"写得出"的练习入口；② 半途退出的测验也有迹可循；③ 页面在手机 / 平板 / 电脑上各自适配，词卡列表任何设备都保持双列。
- **希望提升哪些体验？** 听写让记忆更扎实；退出不白答；任何设备打开都不再"奇怪"。

### ✨ 本次更新
- **新增功能**：✍️ 听写模式——自动播放韩语发音、显示中文意思（半听写），用户输入韩文判分；判分带归一化容错（去首尾空格 / 全角转半角）。
- **功能优化**：测验中途退出会保存已答部分的记录，历史里标记「未完成」；完整完成和半途退出共用 `persistQuizRecord()`，错词照常进易错本。
- **UI·UX 优化**：测验设置区布局重排——模式按钮独占一行（4 个等宽）、题数独立成行带「每轮题数」标签；搜索弹窗结果从单列改为双列，与课程 / 收藏 / 易错本一致。
- **响应式适配**：平板 / 电脑（≥768px）内容列加宽到 760px、测验页 480px、底部导航对齐（保留）；词列表任何宽度都保持双列（移除窄屏单列规则）；横屏闪卡缩小规则经试用后移除，闪卡恢复原样。
- **Bug 修复**：听写题「再听一遍」文字撑破 44px 圆形按钮，改回纯图标样式并加小字提示；「제X과」单元标签在网格里被拉成整列宽，加 `justify-self: start` 贴合文字。
- **版本号同步**：`APP_CONFIG.version` 1.7.0→1.8.0；[sw.js](sw.js) 缓存 hangul-v10→v14。

### 👤 用户体验变化
- 修改前：测验只有选择题，没有"写出韩语"的练习；中途退出成绩全丢；桌面端像放大的手机页；词卡布局在不同设备不一致。
- 修改后：多一个"听韩文→写韩文"的回忆练习；半途退出也能在记录里看到这次答了多少（带「未完成」标记）；桌面舒展、手机词卡双列、搜索也双列、标签贴合文字。

### 💭 设计思考
- **为什么做"半听写"（给中文提示）？** 纯听写对初学者太挫败——"听得出但不确定是哪个词"。给中文后，练习核心变成"中→韩回忆 + 听音校准"，贴合延世1 学习阶段。
- **为什么中途退出按"已答题数"记录而不是总题数？** 退出的题根本没答，用总数记会让分数假性偏低；按已答数记才反映真实表现。
- **为什么听写用打字而不是拼块？** 听写的本质是自己写出来；韩文拆字拼块适合以后做成独立的拼字小游戏，放进听写反而干扰"回忆词形"。
- **为什么词列表始终双列？** 用户明确要求"任何地方都是双列"——双列是既定浏览习惯，单列会让每屏可看的词变少，违背"快速浏览"定位。窄屏拥挤更适合压缩内边距而非改列数。
- **为什么标签加 justify-self: start？** 网格子项默认 `justify-self: stretch` 会把标签拉满整格，加 `start` 后贴合内容宽度，是网格布局的标准解法。

### ⚠️ 开发记录
- 中途退出记录：从 `showQuizResult` 抽出 `persistQuizRecord(score, total, partial)`，完成和半途退出共用，避免逻辑复制。
- 响应式验证：用无头 Edge 实测多个宽度，确认无横向溢出、桌面加宽与底部导航对齐。注意无头浏览器对 <400px 窗口有最小宽度钳制，**窄屏手机与横屏效果需真机 / 设备模拟确认**——本次词卡列数问题正是由此留到真机才发现。
- 本次一个 session 的全部改动最终合并为 v1.8.0 一次发布，git 历史同步压成一个提交。教训：改动应攒齐、确认后再一次性发布，避免零散小版本。

### 📌 下一步建议
- 听写判分加"编辑距离 ≤1 算对"的宽容模式（设置里可开关），照顾 ㅐ/ㅔ、ㄱ/ㅋ 这类音近手误。
- 学习统计沉淀：每日学习日志目前是 sessionStorage，跨天即失，可持久化并做连续打卡可视化。
- 首页信息密度可借机做减法——课程列表与底部「课程」tab 重复，可考虑挪走。
- 词卡标签类元素可统一封装，避免以后新增元素忘记处理网格拉伸。
- 云同步（远期）。

---

## 📅 2026-08-03 01:45 · v1.7.0

### 📝 一句话总结
学完收尾提示：学完一批词后弹出本次学习小结（共学 N 词 · 忘记/困难/顺利），有完成感。

### 🎯 本次更新目标
- **为什么修改？** 之前学完一批词没有任何反馈，翻到最后一张就默默结束，缺少完成感和对当天学习的小总结。
- **解决了哪些问题？** ① 学习有明确的"结束"时刻；② 一眼看到本次表现（忘记几个、进易错本几个）；③ 轻量学习统计雏形。

### ✨ 本次更新
- **新增功能**：学完收尾弹窗——闪卡翻到最后一张时弹出，显示「本次共学 N 个词 · 忘记 X · 困难 Y · 顺利 Z」。
- 忘记的词 >0 时提示「已进易错本，很快会再复习」。
- 每次进入学习/恢复学习时重置统计；评分实时累计。

### 👤 用户体验变化
- 修改前：学完一批词默默结束，没有反馈。
- 修改后：翻到最后一张弹出小结，点「完成」关闭，学习有收尾、有完成感。

### 💭 设计思考
- **为什么用弹窗而不是 toast？** toast 一闪而过，收尾提示需要用户看到自己的表现，弹窗最合适，且只点一下就能关。
- **为什么记录"忘记/困难/顺利"？** 对应闪卡四个评分按钮，让用户直观看到本次哪些词不熟，也为将来的学习统计打基础。
- **为什么在所有学习场景都生效？** 课程学习、今日复习、易错本复习都是"学一批"，统一收尾体验，不做区分。

### ⚠️ 开发记录
- 评分统计 hook 在 `srsRate`（utils.js）里加一行 `recordSessionRate(quality)`，用 `typeof` 守卫避免加载顺序问题；统计定义在 index.html，运行时必然已定义。

### 📌 下一步建议
- 想法 3：首页连续打卡天数。
- 收尾弹窗加"再复习忘记的词"按钮（把 lv=0 的到期词直接拉回闪卡）。
- 轻量学习统计（每日/累计学习词数）雏形可以基于本次会话统计扩展。

---

## 📅 2026-08-03 01:39 · v1.6.1

### 📝 一句话总结
修复首页「今日复习」按钮文字过长导致主按钮被撑高。

### 🎯 本次更新目标
- **为什么修改？** 「今日复习 12 个词」+「到期复习 · 防止遗忘」在手机上宽度不足换行，把主按钮框撑高了，破坏首页布局。
- **解决了哪些问题？** 按钮文字缩短到一行，高度与其他状态一致。

### ✨ 本次更新
- **Bug 修复**：按钮文字改为「今日复习 / N 个词到期」，数量移到副标题，主标题保持简短。

### 👤 用户体验变化
- 修改前：首页主按钮变高，和旁边两个小按钮不对齐。
- 修改后：按钮高度与其他状态完全一致，数量信息仍在。

---

## 📅 2026-08-03 01:35 · v1.6.0

### 📝 一句话总结
首页主按钮变成"今天学什么"的智能入口：有 SRS 到期词时一键进入今日复习；例句点击反馈从淡紫改为淡蓝。

### 🎯 本次更新目标
- **为什么修改？** ① 之前删除独立复习模式后，"到期复习"没有主动入口——打开首页不知道该复习什么；② 例句点击时的淡紫背景与整体配色不搭。
- **解决了哪些问题？** ① 到期词有了最直接的入口，打开首页就知道今天该干嘛；② 例句点击反馈融入主色调。

### ✨ 本次更新
- **新增功能**：首页主按钮智能切换——有到期词时显示「今日复习 N 个词」（图标 🔁），点击直接进入到期词闪卡复习；无到期词时维持「继续学习 / 开始学习」。
- **优先级**：今日复习 > 继续学习 > 开始学习。
- **UI·UX 优化**：例句点击背景从淡紫灰（`#f0ecf2`）改为淡蓝（`--primary-light`），与页面主色一致。

### 👤 用户体验变化
- 修改前：到期词需要自己想起来去复习，首页按钮只能继续学习/开始学习。
- 修改后：打开首页第一眼看到「今天要复习 N 个词」，一键进入；复习完自动按 SRS 排下次时间。

### 💭 设计思考
- **为什么复习优先于继续学习？** 符合 SRS/Anki 理念——到期卡片是当天最高优先级的学习动作，学新课次之（可从课程页进入）。
- **为什么不算纯新词？** 新词没有 SRS 记录，算进"复习"会让第一天就显示"复习 300 词"的乌龙。
- **为什么用主色蓝替代淡紫？** 与 `--primary` 设计语言统一，深色模式自动适配。

### ⚠️ 开发记录
- 复习优先级会覆盖"继续学习"的进度存档（复习会话写同一个进度槽）。已评估为可接受——课程可随时重进，且这是"复习优先"的必然结果。

### 📌 下一步建议
- 想法 2：学完一课后的"收尾提示"（今天学了 N 词，其中 X 词进易错本）。
- 想法 3：连续打卡天数展示。
- 到期词当天没打开时的轻量提醒（页面内 Notification API 版）。
- 云同步（远期，届时可一并规划真正的推送）。

---

## 📅 2026-08-03 01:23 · v1.5.2

### 📝 一句话总结
修复发音按钮呼吸反馈不生效 + 发音按钮换为主色蓝。

### 🎯 本次更新目标
- **为什么修改？** ① v1.5.1 的呼吸反馈因发音按钮缺少 `id` 一直没生效；② 发音按钮原本粉橙渐变与整体风格不协调。
- **解决了哪些问题？** 反馈动画真正生效；按钮配色融入网站主色调。

### ✨ 本次更新
- **Bug 修复**：发音按钮补上 `id="ctrl-speak"`，呼吸反馈动画恢复生效。
- **UI·UX 优化**：发音按钮从粉橙渐变改为网站主色蓝（`--primary`），深色模式自动适配。

### 👤 用户体验变化
- 修改前：发音时按钮没有动画；按钮是扎眼的粉橙渐变。
- 修改后：发音时按钮蓝色呼吸；按钮与整体配色统一。

### 💭 设计思考
- **为什么用主色蓝？** 蓝色是网站主题色，通篇使用；发音按钮作为高频操作与其保持一致最协调。保留圆形形状，仅换色，改动最小。
- **为什么是补 id 而不是改 JS？** 其他按钮（收藏/掌握）都有 `id`，唯独发音按钮漏了；补齐 id 即可让现有 `flashSpeakBtn()` 生效，不动 JS 逻辑。

---

## 📅 2026-08-03 01:02 · v1.5.1

### 📝 一句话总结
发音按钮统一读韩语 + 自动发音时按钮呼吸反馈，明确"谁在读"。

### 🎯 本次更新目标
- **为什么修改？** 中文优先模式下"发音"按钮会播中文，但正面是中文提示，播中文没意义；且自动发音时用户不知道声音从哪来。
- **解决了哪些问题？** ① 发音按钮在中文优先下也能听到韩语单词；② 自动/手动发音时右下角按钮有呼吸动画，声音来源一目了然。

### ✨ 本次更新
- **功能修正**：闪卡页"发音"按钮始终播放韩语单词（不再跟随卡片方向播中文）。
- **UI·UX 优化**：自动发音或手动点击发音时，"发音"按钮做 0.9s 呼吸动画（放大 + 光晕），播完自然熄灭。
- **版本号同步**：设置页显示版本从 1.4.2 更新为 1.5.1。
- **规范更新**：CLAUDE.md 与 CHANGELOG_RULES.md 新增规则——每次发布须同步更新 `APP_CONFIG.version` 与 `sw.js` 缓存版本号 `CACHE`。
- **Bug 修复**：Service Worker 缓存名 v5→v6，解决桌面软件/安装版一直显示旧版本（旧缓存不更新）的问题。

### 👤 用户体验变化
- 修改前：中文优先模式下按发音听到的是中文；自动发音时没有声音来源提示。
- 修改后：任何时候按发音都听到韩语；正在发音时按钮会"呼吸"一下，一眼知道是它在读。

### 💭 设计思考
- **为什么按钮始终读韩语？** 这是韩语学习站，"发音"的语义就是"这个单词怎么读"，即韩语。中文优先只是让正面显示中文当提示，不影响按钮含义。
- **为什么用固定时长动画而非等音频播完？** 保持 `speak()` 通用函数零改动，避免影响其他调用方（例句、测验等）；0.9s 的呼吸足够传达"正在读"。

---

## 📅 2026-08-03 00:51 · v1.5.0

### 📝 一句话总结
闪卡学习页自动播放发音：点进卡片即读韩语，切上一张/下一张自动跟着读。

### 🎯 本次更新目标
- **为什么修改？** 学闪卡时每个单词都要手动点一次"发音"按钮，切一张卡点一次，操作重复、打断学习节奏。希望让"听"自然融入切卡的流程，而不是一个额外动作。
- **解决了哪些问题？** 去掉每次切卡多点一下发音的重复操作，听力练习更连贯。
- **希望提升哪些体验？** 点进卡片立刻听到读音，切卡即换音，跟着读词记忆更深。

### ✨ 本次更新
- **新增功能**：闪卡学习页自动播放当前单词的韩语发音。
- **生效范围**：点进学习页的第一张、点"上一张/下一张"按钮、左右滑动切卡，均自动发音。
- **中文优先模式不自动播**：`cn-first` 下卡片正面是中文，跳过自动发音，避免未回忆就提前泄露韩语答案。
- **复用现有 `speak()` 链路**：自动取消上一次语音，快速切卡不会出现声音重叠；Google TTS 不通时自动降级原生语音。

### 👤 用户体验变化
- 修改前：每进入一张新卡片都要手动点右下角"发音"按钮。
- 修改后：进入卡片自动发音，几乎不用再点发音按钮。
- 减少了一次点击，学习流程更顺、更接近"打开就学"。

### 💭 设计思考
- **为什么只在 `showCard()` 加三行？** `showCard()` 是进入学习 / 切卡 / 恢复进度的唯一渲染入口，一处改动覆盖所有切卡场景，不需要动其他逻辑。
- **为什么中文优先不播？** 遵循闪卡"先回忆 → 再验证"的核心原则：正面是中文提示时自动播韩语等于提前给答案，练习效果打折扣。
- **为什么不做成设置开关？** 遵循"做减法"原则，先作为默认行为；若后续有用户不想要，再加开关不迟。

### ⚠️ 开发记录
- 自动播放发生在用户点击进入 / 切卡的交互链路上，符合浏览器自动播放策略，无需额外处理；`speak()` 顶部已有取消旧语音逻辑，声音重叠无需担心。

### 📌 下一步建议
- 是否让例句也在卡片翻面后自动朗读（跟着句子练听读）。
- 自动播放时给底部"发音"按钮一个高亮/呼吸反馈，让用户知道音是它播的。
- 测验页题干是否也跟随自动朗读，统一"打开即听"的体验。
- 如果自动发音有人不想要，再考虑在设置页补一个开关（目前先不加）。

---

## 📅 2026-08-01 23:24 · v1.4.4

### 📝 一句话总结
全面修复页面刷新后动态内容丢失：测验/收藏/易错本/设置/单词列表/闪卡复习/首页今日小结。

### 🎯 本次更新目标
- **为什么修改？** 拆分为多文件后，手机浏览器通过 GitHub Pages 访问时，刷新任意页面都会导致 JS 动态渲染的内容丢失，严重破坏体验。
- **解决了哪些问题？** 所有 7 个有动态内容的页面，刷新后自动恢复渲染。
- **希望提升哪些体验？** 用户可以放心刷新或分享链接，页面内容不丢失。

### ✨ 本次更新
- **收藏页**：修复函数名错误（`renderStarred` → `showStarred`），刷新后正常显示。
- **单词列表页**：新增上次查看课程记忆（`ys-last-lesson`），刷新后自动恢复到该课。
- **闪卡复习页**：刷新后调用 `resumeStudy()` 恢复复习进度。
- **测验页**：刷新后重新渲染单元列表和测验记录。
- **易错本页**：刷新后重新渲染薄弱词列表。
- **设置页**：刷新后重新填充统计数据。
- **首页**：刷新后调用 `renderTodaySummary()` 显示今日学习小结。

### 👤 用户体验变化
- 所有页面的分享链接和刷新操作均正常恢复内容。

---

## 📅 2026-07-27 · v1.4.3

### 📝 一句话总结
首页收藏按钮加数量标识：直观显示当前收藏总数，减少学习者的记忆负担。

### 🎯 本次更新目标
- **为什么修改？** 之前收藏功能没有数量反馈——用户不知道自己收藏了多少单词/语法，也不容易发现收藏功能的存在。
- **解决了哪些问题？** ① 收藏数量一目了然；② 给收藏功能一个轻量的"存在感"，但不喧宾夺主。
- **希望提升哪些体验？** 降低思考成本——无需点进收藏页面就能知道有多少收藏内容。

### ✨ 本次更新
- **首页收藏按钮新增数字标识**：`starred.size + grammarStarred.size` 实时显示在"收藏"按钮后，浅金色小字 + 浅色背景，风格克制不抢眼。
- **数量为 0 时自动隐藏**：没有收藏时不显示数字，保持界面简洁。
- **实时更新**：收藏/取消收藏单词或语法时立即反映到首页数字。

### 👤 用户体验变化
- 首页"收藏"按钮后多了浅金色小数字，一眼知道有多少收藏。
- 不影响任何已有操作流程。

### 💭 设计思考
- **为什么不用大红大紫的角标？** 收藏不是一个紧急或重要通知，不需要抢占注意力。浅金色与收藏按钮的 `#d4a550` 主题色一致，轻描淡写。
- **为什么 0 要隐藏而不是显示 "0"？** 遵循"做减法"原则——没东西就不要占位置。用户不需要知道"没有收藏"这个信息。
- **本次遵循的产品设计原则？** ⑤ 降低思考成本——不用点进去就知道数量。

---

## 📅 2026-07-26 02:21 · v1.4.2

### 📝 一句话总结
单文件拆分 + 深色模式 CSS 去重：5785 行巨石 → 5 个文件，CSS 减少 57 行重复规则。

### 🎯 本次更新目标
- **为什么修改？** 5785 行单文件维护成本越来越高——改数据怕碰 JS，改样式怕碰 HTML，每次定位都要上下翻几十屏。
- **解决了哪些问题？** ① 代码定位效率；② 修改安全性（改一个文件不影响其他）；③ 为后续功能扩展（AI 助手、学习统计等）打好基础。
- **希望提升哪些体验？** 开发体验——改数据只开 data.js，改样式只开 style.css，不再在巨型文件中迷失。

### ✨ 本次更新

#### 代码重构
- **拆分数据层 → `data.js`**（1312 行）：`VOCAB` + `GRAMMAR` + `LESSONS` + `APP_CONFIG`，纯数据零逻辑。
- **拆分样式 → `style.css`**（864 行）：全部 CSS 含浅色/深色两套变量 + 系统自动模式。
- **拆分工具函数 → `utils.js`**（549 行）：SRS 间隔重复算法 + 韩语动词变形（18 种形态）+ 例句生成 + 随机取样。
- **index.html 瘦身至 3067 行**：仅保留 HTML 结构 + 页面逻辑，加载顺序 `style.css → data.js → utils.js → 主脚本`。
- **Service Worker 更新**：`hangul-v4 → v5`，缓存列表新增 `style.css`、`data.js`、`utils.js`。
- **`masteryCount()` 移至 data.js 之后**：该函数依赖 `VOCAB`，确保加载顺序正确。
- **深色模式 CSS 去重**（-57 行）：`auto` 模式改由 JS 层解析——head 内联脚本 + `applyTheme()` 直接根据系统偏好设置 `data-theme="dark"`，CSS 不再需要 `@media (prefers-color-scheme: dark)` 和 `:root:not([data-theme])` 两套重复选择器。删除全部 7 个 `@media` 重复块，CSS 从 864 → 807 行。
- **版本号修正**：`APP_CONFIG.version` 从 `1.3.0` → `1.4.2`。

### 👤 用户体验变化
- **用户无感知变化**。拆分是纯工程优化，不影响任何页面功能。
- **双击 HTML 依然可以正常打开**，不依赖构建工具或服务器。
- **PWA 离线可用**：SW 缓存列表已更新。

### 💭 设计思考
- **为什么用普通 `<script src>` 而不是 ES Module？** 确保 `file://` 协议下双击 HTML 也能正常加载。ES Module 在 `file://` 下会被 Chrome CORS 策略阻止。
- **为什么拆分顺序是数据 → 样式 → 工具？** 数据层最安全（纯数据零逻辑），先拆风险最低的；工具函数次之但要仔细验证依赖；样式完全独立随时可拆。
- **为什么第一步不拆更多？** 渐进式——每步都可部署可测试，出了问题立刻知道是哪一步。
- **本次遵循的产品设计原则？** ⑤ 降低思考成本——开发者的思考成本也在考虑范围内。代码可维护性直接影响产品迭代速度。

### ⚠️ 开发记录
- **加载顺序验证**：`masteryCount` 原本定义在 data.js 之前的脚本块中（L1395），虽然函数体延迟求值不报错，但为了一致性移到了 data.js 之后。
- **style.css 中的深色模式无需修改**：`@media` 和 `:root[data-theme]` 选择器不依赖文件位置，抽出后完全正常。
- **utils.js 引用的全局变量**：`srs`、`starred`、`studyIndex`、`studyWords` 等均在主脚本中定义，utils.js 函数只在调用时访问，定义时不需要存在。
- **CSS 去重的关键**：原来的 `@media auto` 方案需要 CSS 为 `[data-theme="dark"]` 和 `@media auto` 各写一套相同的规则。改为 JS 在 `auto` 模式下主动检查系统偏好、直接设置 `data-theme="dark"` 或移除属性，CSS 只需一套 `[data-theme="dark"]` 规则，`_themeMedia` 监听器在系统切换时重新判定。

### 📌 下一步建议
- **可考虑将 HTML 页面模板也拆出**：7 个 `<div id="page-*">` 共约 400 行，可独立为 `pages.html` 用 fetch 加载（但需注意这会破坏双击打开，需要权衡）。

## 📅 2026-07-26 01:23 · v1.4.1

### 📝 一句话总结
学习卡片细节打磨：修复滚动条 + 卡片放大 + Hero 浮动动画 + Hero emoji 库扩充。

### 🎯 本次更新目标
- **为什么修改？** 卡片页面出现滚动条、滑动提示看不见、emoji 太老套、Hero 浮动动画从未生效。
- **解决了哪些问题？** ① 卡片页纵向滚动条；② slide-hint 被挤到屏幕外；③ Hero emoji 太少太单调；④ `@keyframes hero-float` 缺失导致动画从未生效。

### ✨ 本次更新

#### Bug 修复
- **卡片页滚动条修复**：`flashcard-area` 的 `min-height: 100vh` 与 `slide-hint` 叠加超过视口高度，改为 `calc(100vh - 30px)`。
- **Hero 浮动动画补全**：`.home-hero-emoji` 一直引用了 `hero-float` 动画但 `@keyframes` 定义不存在，本次补上（±6px / 3s / 缓动）。

#### UI·UX 优化
- **卡片尺寸微调**：`max-width: 300px → 340px`。
- **卡片提示文字**：改为"空白处左右滑动切卡 🫶🏻"，`position: fixed` 固定显示在卡片下方。
- **Hero emoji 库扩充**：从 25 个 emoji 扩充到 77 个，涵盖食物/动物/天气/心情等更丰富的表情。

### 👤 用户体验变化
- **修改前**：卡片页有莫名的纵向滚动条；卡片下方提示看不见；Hero emoji 永远是那 25 个。
- **修改后**：卡片页不再滚动；提示文字清晰可见；每次回到首页都有新鲜的 emoji 迎接。

### ⚠️ 开发记录
- **`hero-float` 一直没生效**：CSS 中引用了未定义的 `@keyframes`，浏览器静默忽略不报错。

### 📌 下一步建议
- **浮动动画可扩展到空状态页面**：收藏/易错本为空时的图标加浮动，让"空"不那么冷清。
- **hero-float 可提取为通用 class**：`.animate-float`，任何需要微妙提示的图标都可以复用。

## 📅 2026-07-25 17:40 · v1.3.0

### 📝 一句话总结
产品整理优化：连续学习天数 + 今日小结 + 测验分析 + 卡片手势重构 + 版本号统一管理。

### 🎯 本次更新目标
- **为什么修改？** 学习完成后首页没有任何反馈，用户无法感知"今天的任务完成了"；卡片手势不够直觉——上下滑看例句和左右滑切卡混在一起；版本号硬编码在代码中容易遗忘更新。
- **解决了哪些问题？** ① 学习闭环缺失 → 今日小结卡片；② 用户不知道连续学了多少天 → streak 显示；③ 测验成绩没有跨维度分析 → 按单元/按时段聚合；④ 卡片手势冲突 → 卡片上只翻面+滚动，空白处才切卡；⑤ 版本号每次发版忘记改 → 统一到 `APP_CONFIG`。
- **希望提升哪些体验？** 学习完有成就感，连续天数有动力，卡片操作更直觉，版本号不再过时。

### ✨ 本次更新

#### 新功能
- **连续学习天数**：凌晨4点分界，进入学习或测验才计数（仅打开首页不算）。日期行末尾显示 `🔥 连续 X 天`（仅 ≥2 天时显示）。Streak 中断自动重置。
- **今日小结卡片**：学习/测验结束后回到首页，进度条下方出现小结卡片。聚合当日学习单词数 + 测验轮数/正确率。关闭后显示一行极小入口文字，可重新打开。点击"查看详细"跳转测验记录页。`sessionStorage` 存储，当天刷新不丢，第二天自动清空。
- **测验记录今日分析**：记录页新增 `📊 今日分析` 区块，按单元（错题分布）和按时段（上午/下午/晚上正确率）聚合展示。

#### Bug 修复
- **测验页多余 `</div>`**：删除 L1134-1135 两行无对应开标签的闭合标签。
- **测验结果页底部导航缺失**：`showQuizResult()` 末尾恢复 `showNav()`，答题中 `hideNav()`。

#### UI·UX 优化
- **卡片手势重构**：触摸事件从 `#flashcard` 移到 `.flashcard-area`。卡片上完全放行（tap 翻面 + 上下滚看例句），空白处左右滑动切上一张/下一张。
- **卡片尺寸缩小**：`max-width: 360px → 300px`，`aspect-ratio: 3/4.2 → 3/4`，两侧空白区更多，滑动切卡更好操作。
- **卡片提示语分拆**：卡片上 → `轻点卡片翻面 🫶🏻`（卡片底部内侧），卡片下方 → `← 空白处左右滑动切卡 →`（灰色小字）。
- **听音模式提示**：题干区底部加 `点击 🔊 可重播发音`。
- **首页鼓励语刷新**：每次回到首页随机更换鼓励语，不再只有首次加载时取一次。

#### 代码清理
- **版本号统一管理**：新增 `APP_CONFIG` 全局对象（`textbook` + `version`），设置页版本号改为 JS 动态填充。发版只需改一处。
- **手势代码精简**：从 35 行减到 25 行，逻辑更清晰。
- **全局防水平溢出**：`html, body` 加 `overflow-x: hidden; position: relative`。

### 👤 用户体验变化
- **修改前**：学完/测完回到首页 → 什么反馈都没有 → 好像什么都没发生。
- **修改后**：回到首页 → 小结卡片展示当日收获 → 关闭后留一个小入口 → 第二天自动消失。
- **修改前**：不知道连续学了几天 → 没有动力锚点。
- **修改后**：日期行 `🔥 连续 X 天` → 每天打开有期待。
- **修改前**：卡片上滑动 → 方向判断错误就跳走了 → 上下滚例句变成翻面。
- **修改后**：卡片上随意滑，不会误跳上一张/下一张。空白处才是切卡区。
- **学习流程是否更加自然？** 是。学习 → 回来 → 看到收获 → 形成闭环。
- **是否减少了思考成本或点击次数？** 卡片手势不再需要判断滑动方向，降低了操作负担。

### 💭 设计思考
- **为什么小结不显示聚合正确率百分比？** 上午 2/10 + 下午 18/20 合并成 67%，这个数字没有人经历过。所以只显示数量，不显示百分比。百分比放在测验记录页的按单元分析里。
- **为什么 streak 要凌晨 4 点分界？** 半夜 23:50 和 00:10 应该是同一天。Duolingo 和 Anki 都这么做。
- **为什么连续天数小字放在日期行而不是独立一行？** Hero 卡片已有 emoji+问候+鼓励语+日期四层信息，再加一层会压扁鼓励语。日期行本身权重最低，是最自然的合并位置。
- **为什么卡片手势不在卡片上也区分方向？** 卡片背面有例句需要上下滚动。如果卡片上的上下滑也被拦截为翻面，用户永远看不到后面的例句。所以卡片上完全放行，翻面靠 tap，滚动靠系统。
- **本次遵循的产品设计原则？** ① 学习优先——小结卡片让学习形成闭环；② 做减法——小结不是数据堆积，只保留最有意义的数字；③ 降低思考成本——卡片手势不再需要判断方向。

### ⚠️ 开发记录
- **水平滚动条排查**：第二个卡片出现水平滚动，最终根因是 `html, body` 缺少 `overflow-x: hidden; position: relative`。与单词长度无关。
- **slide-hint 的布局**：最初放在 `flashcard-area` 内部 `flex-direction: column`，后来移出为独立元素避免影响卡片居中。
- **手势重构的边界条件**：卡片上的 `touchend` 需要先 `return` 放行，不能落入后续的切卡逻辑。`e.target.closest('.flashcard')` 在卡片及其所有子元素上都能正确匹配。

### 📌 下一步建议
- **课程列表可考虑折叠**：10 课全展开在手机上有一定滚动距离，未来可默认折叠为手风琴。
- **Streak 断档缓冲**：连续 N 天后中断一天，可以不直接清零而是显示"差点忘了"，给用户一个心理缓冲。
- **测验记录按单元分析可更丰富**：当前只展示错题分布，未来可展示每课正确率趋势图。
- **听音模式可独立为题型**：目前听音和韩→中共享同一套选项生成逻辑，未来可让听音模式支持"拼写输入"等进阶题型。

## 📅 2026-07-25 16:12 · v1.2.0

### 📝 一句话总结
搜索改浮层 + 修复听音错题Bug + 删除复习模式 + 首页做减法，入口从 4 减至 1。

### 🎯 本次更新目标
- **为什么修改？** 复习模式与课程/收藏/易错本高度重叠（5个tab里4个是已有功能），违反"一个页面一个任务"原则。首页"1主+3辅"设计仍有冗余——用户在底部导航能找到测验和收藏，首页不需要重复。
- **解决了哪些问题？** ① 复习页面的存在让用户困惑："复习和课程有什么区别？" ② 首页和底部导航两套入口依然有交集（复习→易错本/收藏，测验→测验）；③ 收藏在首页有入口但在课程页看单词时想跳收藏没有路径；④ 测验答错的已掌握词不会出现在易错本。
- **希望提升哪些体验？** 首页打开只剩一个按钮，零决策成本。搜索不跳页面，搜完继续原地浏览。

### ✨ 本次更新

#### 新功能
- **搜索改为浮层弹窗**：从独立全屏页面改为半透明浮层，在当前页面上方弹出搜索框+结果。点击背景/✕/ESC关闭，回到原地。课程页和单词页头部🔍按钮触发。
- **收藏入口移至课程页头部**：课程列表页头部新增⭐按钮，有单词的地方就能进收藏。

#### Bug 修复
- **已掌握词答错不进易错本**：`showQuizResult()`中已掌握词（`lv>=4`）答错时只递增`badCount`但未降级`lv`，而`isWeak()`条件是`lv<4`，导致已掌握词永远被过滤。修复：答错时加`if (d.lv >= 4) d.lv = 3`，三种测验模式均受益。

#### 功能删除
- **删除复习模式**：`page-review`页面+CSS+JS全部移除（约230行）。该页5个tab中薄弱词/收藏/全部/未掌握分别对应易错本/收藏页/课程页已有功能，仅"待复习"为独有。保留底层共用函数（`buildReviewPool`/`startStudyPool`/`_isDue`/`_isUnmastered`）供易错本"复习薄弱词"和易错本"复习全部"按钮继续使用。
- **删除首页次要入口**：复习、测验、收藏三张卡片整行移除。测验从底部导航进入，收藏从课程页头部进入。

#### UI·UX 优化
- **首页从"1主+3辅"简化为"1主"**：只剩问候卡片+进度条+开始学习按钮。视觉层级从三层变为两层，用户打开后不需要任何选择。
- **易错本新增单元标签**：每张错词卡片上在词性标签旁边加`제X과`紫色标签，一眼知道来源。
- **标签`white-space: nowrap`**：词性和单元标签加防断行，不再出现竖长条。

#### 代码清理
- 删除废弃CSS：`.secondary-actions`/`.secondary-action`相关样式
- 删除废弃JS：`reviewType`/`selectedLessons`/`switchReviewTab`/`reviewQuick`/`toggleReviewLesson`/`doReview`/`renderReviewLessons`/`showReviewSetup`/`startReview`/`home-review-desc`DOM更新
- 删除`PAGE_HASH`中`page-review`映射

### 👤 用户体验变化
- **修改前**：打开首页→4个区块→"复习还是测验？"→两个入口都能到同一个地方→困惑。
- **修改后**：打开首页→📖开始学习。想收藏去课程页⭐，想测验点底部导航，想复习……不需要复习了（该功能已解散到课程/收藏/易错本各自负责）。
- **修改前**：搜索→跳到全屏页面→搜完返回首页→迷失。**修改后**：搜索→浮层弹出→搜完关闭→还在原地。
- **修改前**：已掌握的单词在听音测验中答错→"已加入易错本"→去易错本看→没有。**修改后**：答错即降级为lv=3，真正进入易错本。
- **学习流程是否更加自然？** 是。首页不再试图扮演"全能入口"，回归"学习起点"的唯一定位。
- **是否减少了思考成本或点击次数？** 首页从4个入口减至1个，零决策。搜索从"跳转再返回"变为"浮层即走"。

### 💭 设计思考
- **为什么删除而不是改进复习模式？** 复习页面本质上是一个"万能筛选器"——选类型+选单元+开始学习。但每种类型都对应一个已有页面：薄弱词→易错本、收藏→收藏页、全部/未掌握→课程页过滤。唯一独有的是"待复习"（SRS到期词），但将其独立为一个页面代价太大（额外的页面、导航、代码）。产品设计原则第③条：一个页面只完成一个主要任务。
- **为什么首页去掉测验入口？** 测验在底部导航已有固定入口，首页再加就是原则第②条所说的"重复导致困惑"。而且测验不是每日必做的学习动作——它更像是阶段性检查——放底部导航作为"工具"比放首页作为"入口"更合适。
- **为什么收藏放在课程页而不是底部导航？** 底部导航5个位置已满（首页/课程/测验/易错本/设置），塞第6个会挤。课程页是"浏览单词"的场景，收藏是浏览的延续动作——"看到想收藏的→去收藏列表复习"——逻辑链路一致。而且课程页头部已有🔍，再加⭐形成对称的辅助工具栏。
- **易错本为什么用标签而不是手风琴/折叠？** 手风琴在实现中产生了grid布局的左右空白同步问题，且多处尝试flex覆盖仍然存在视觉bug。标签方案与搜索结果风格统一、零额外CSS、一行的改动。原则——简单、干净、不引入新复杂度。
- **本次遵循的产品设计原则？** ①学习优先——首页只剩一个主按钮；②做减法——删复习页、删首页3个入口、删约230行代码；③一个页面一个任务——复习功能回归各负责页面；④降低思考成本——首页不再有选择。

### ⚠️ 开发记录
- **手风琴布局问题**：易错本按单元折叠分组时，`word-item`的grid布局导致左右列高度共享——右边按钮撑高后左边空白也相应增大。尝试覆盖为flex布局但`align-items: center`导致上下空白对称分布。最终放弃手风琴方案，改用更简单的标签方案。
- **复习模式删除的依赖排查**：`buildReviewPool`/`startStudyPool`/`_isDue`/`_isUnmastered`被测验（薄弱词范围）、易错本（复习全部/按单元复习）、测验结果（复习错题）共用，删除前逐条确认引用链，确保只删复习页独有代码。
- **`setKeys`保留**：测验选择和导入导出数据均依赖此工具函数。

### 📌 下一步建议
- **首页Hero卡片可加入今日学习进度动画**：问候卡片目前是静态的，可以在用户完成学习/测验后刷新时有一个微小的emoji或颜色变化，增加"回来看看"的动机。
- **课程页⭐按钮可加未读红点**：当收藏列表有新增时可显示一个小红点提示。
- **易错本可加排序选项**：按单元、按错误次数、按最近错误时间排序，帮助用户优先复习最薄弱的词。

## 📅 2026-07-25 18:38 · v1.4.0

### 📝 一句话总结
首页韩系清新重设计：奶白淡青配色、Hero浮动动画、课程列表回归首页、5:2.5:2.5并排操作区。

### 🎯 本次更新目标
- **为什么修改？** 经过v1.2.0和v1.3.0的功能整理，首页只剩一个主按钮——视觉上太空太规矩。配色偏粉紫，不够韩系学习App风格。
- **解决了哪些问题？** ① 首页视觉密度——加入课程列表，一页完成"浏览→选课→学习"；② 配色从暖粉→奶白淡青，更清新克制；③ Hero加浮动动画增加陪伴感。
- **希望提升哪些体验？** 打开网站感受到韩系清新质感，不用跳转就能看到所有课程，学习路径最短。

### ✨ 本次更新

#### UI·UX 重设计
- **配色全面更新**：从暖粉紫（`#fef5f7`）→奶白淡青（`#eaf5f0`）。进度条渐变浅绿→天蓝，主按钮淡蓝渐变填充，Hero边框浅绿。
- **Hero 浮动动画**：emoji `hero-float` 3s ease-in-out，上下 4px，微小的活感。
- **操作行5:2.5:2.5并排**：继续学习（左半侧，5）+ 收藏/搜索（右侧并排两个小框，各2.5），一行内完成核心操作。
- **课程列表回归首页**：底部 `home-lesson-list`，每课显示编号/标题/词汇数/掌握进度条，点击进入单词学习。
- **进度条精简**：📚已掌握 + X/Y + 百分比 同行一条，框高减到 8px+5px进度条。
- **今日小结内联化**：`✨ 鼓励语 · 📖学了X词 · ✅测验8/10` 一条，不再有可关闭大卡片。

### 👤 用户体验变化
- **修改前**：一个主按钮孤零零，下面一大片空白。
- **修改后**：Hero→进度→CTA+收藏搜索→课程列表，页面充实有节奏。
- **修改前**：想看课程必须点底部tab跳转。**修改后**：首页直接滚到课程列表。
- **学习流程是否更加自然？** 首页→看课程→点进去→学，或直接点继续学习。

### 💭 设计思考
- **为什么奶白+淡青？** 粉紫色温度高，长时间学习容易疲劳。淡青偏中冷，安静克制，类似Duolingo的绿调、Anki的蓝灰调。
- **为什么5:2.5:2.5？** 继续学习占一半权重，收藏搜索各四分之一。三按钮等大但CTA更左更重。
- **浮动动画不花吗？** 3s/4px/缓入缓出，潜意识的"页面是活的"，用户不会注意到。

### 📌 下一步建议
- **底部导航精简**：课程列表已回归首页，课程tab是否仍需？
- **收藏加数量标识**：收藏个数可显示在按钮上，类似易错本的数字提示。

## 📅 2026-07-24 23:08 · v1.1.0

### 📝 一句话总结
9项系统性优化：修复学习入口Bug、重建全站导航、新增主题/继续学习/新手提示，全面提升学习流畅度。

### 🎯 本次更新目标
- **为什么修改？** 产品在日常使用中暴露出多个体验断点——学习按钮点了却从头开始、返回键逻辑混乱、缺少深色模式眼睛疲劳、中途退出后无法恢复进度等，这些积累的摩擦让用户无法"随手打开、立即学习"。
- **解决了哪些问题？** 共9项：学习入口Bug / 首页统计混乱 / 测验无自动发音 / 掌握按钮一次性的 / 全站返回逻辑碎片化 / 无主题切换 / 新手困惑 / 无学习进度保存 / 多项细节Bug。
- **希望提升哪些体验？** 更自然的导航、一键主题切换、学习进度不丢失、新手零门槛上手。

### ✨ 本次更新

#### Bug 修复
- **学习模式入口Bug（P0）**：从搜索/收藏/易错本/课程页面点击"学习"按钮，现在从点击的位置开始学习，而不是永远第1个。根本原因是旧代码只传了`lessonNum`，导致重新加载整课数据；现在通过`_studyContextList`保存上下文词表，用`wk()`键值精准定位。
- **全站返回按钮统一**：移除了所有硬编码的`showPage('page-home')`返回按钮，改为统一的页面栈机制（`goBack()`），无论从哪来都能正确回到上一页。
- **底部导航双重点击Bug**：底部导航按钮现在每次都重置页面栈（`navigateRoot`），避免连续切换标签后返回路径混乱。
- **CSS重复规则清理**：修复了深色模式`.home-hero`和`.greeting`规则重复定义的问题。

#### 新功能
- **主题模式设置**：设置页新增"跟随系统 / 浅色 / 深色"三选一。CSS变量从`@media`媒体查询改为`data-theme`属性控制，`<head>`内联脚本防闪烁。监听系统主题变化，切换到auto模式时自动跟随。
- **测验自动播放发音**：韩→中 / 听音模式进入每道题后自动播放一次，不再需要手动点击。听音模式文案从"点击播放发音"改为"🔊 正在播放..."。
- **「全部标记已掌握」智能切换**：当一课所有单词都掌握后，按钮自动变为"↩ 取消全部已掌握"（粉红色警告样式），点击可一键全部撤销。
- **继续学习**：在学习卡片切换/退出时自动保存进度（localStorage），首页快捷入口展示"继续学习"按钮。翻完最后一页自动清除记录。
- **新手操作提示**：单词列表页首次进入时显示可关闭的提示条（"💡 点击卡片听发音 · ⭐ 收藏 · ○ 标记掌握"），关闭后不再显示。

#### UI·UX 优化
- **首页统计精简**：删除了3张统计卡片（已掌握/学习中/收藏），合并为一条进度条，展示"已掌握：X / Y"和百分比。视觉重心从"数据"回归"学习"。
- **快捷入口卡片导航修正**：课程列表、测验、收藏、易错本等快捷入口现在统一走页面栈导航，不再绕开返回逻辑。

#### 代码重构
- **页面栈导航体系**：新增`navigateTo()` / `goBack()` / `navigateRoot()` 三个统一导航函数，替代散布在代码中各处的`showPage()`调用。`goBack()`调用`history.back()`保持浏览器历史同步。
- **`_studyContextList`全局变量**：作为学习模式的词表上下文，每个页面在渲染前设置它，学习模式不再依赖`VOCAB[lessonNum]`重新读取数据。
- **底部导航包装函数**：`navQuiz()` / `showWeakWordsNav()` / `showSettingsNav()` / `showStarredNav()` 包装原始函数，底层导航入口统一加栈重置。

### 👤 用户体验变化
- **修改前**：从收藏页点"学习"→直接跳到第1课第1个单词开始学；返回按钮永远回首页，想回收藏页得重新点三层。
- **修改后**：从收藏页点"学习"→从那个单词开始，顺序遍历收藏列表（未掌握优先）；点返回→回到收藏页。
- **修改前**：晚上手机自动变深色模式，网站却白得刺眼；想换回浅色还得改系统设置。
- **修改后**：设置页一键切换，也可以"跟随系统"自动同步。
- **修改前**：学了15个单词，锁屏回个消息，回来网站刷新了——从头开始。
- **修改后**：首页"继续学习"按钮，恢复到上次中断的位置。
- **学习流程是否更加自然？** 是。导航不再"跳来跳去"，而是像App一样有清晰的层级和回退路径。
- **是否减少了思考成本或点击次数？** 测验自动发音省去每次手动点击；首统简化让用户一眼看到进度而非数据表格。

### 💭 设计思考
- **为什么用`data-theme`属性而非class？** `data-theme`语义更清晰（"当前主题是什么"），且方便在`<head>`内联脚本中设置（`document.documentElement.setAttribute`），避免class优先级冲突。CSS中用`:root[data-theme="dark"]`选择器覆盖变量，精确可控。
- **为什么页面栈不用`history.state`管理？** `history.state`只能存取当前状态，无法回溯完整栈。独立`_pageStack`数组可以完整追踪路径，`goBack()`时`pop()`即可。同时调用`history.back()`保持浏览器URL前进/后退可用。
- **为什么保存学习进度用localStorage而非sessionStorage？** 学习中断可能是被迫的（来电、锁屏、浏览器被杀），sessionStorage在标签页关闭后丢失。localStorage让用户明天打开还能继续。
- **本次遵循的产品设计原则？** ① 学习优先——首页统计精简，视觉重心回归课程列表和"继续学习"；② 做减法——删除重复的3张统计卡片；③ 一个页面一个任务——页面栈确保导航层级清晰；④ 降低思考成本——测验自动发音、新手提示、智能按钮切换都是减少用户决策。

### ⚠️ 开发记录
- **页面栈与`popstate`的协调**：`goBack()`调用`history.back()`触发`popstate`，而`popstate`处理器会更新`_currentPage`。但`navigateRoot()`不走`history.back()`而是直接`showPage()`+`pushState`。两者的关键区别：`goBack`是"回退"（消费栈），`navigateRoot`是"跳转"（重置栈）。
- **CSS媒体查询→属性选择器迁移**：暗色主题规则需要同时支持"手动设置dark"和"auto模式下系统dark"，因此每条规则都要写两份：`:root[data-theme="dark"]`和`@media:root:not([data-theme]), :root[data-theme="auto"]`。这是必要的冗余，不是重复。
- **`_studyContextList`的数据一致性**：由于词表对象在`filter`/`map`过程中会丢失`lessonNum`属性，每个页面设置上下文时都必须手动补齐`lessonNum`字段，否则`wk()`生成键值时`lessonNum`为`undefined`。

### 📌 下一步建议
- **测验历史记录持久化**：当前测验结果只在本次会话可见，刷新后丢失。建议保存到localStorage，便于回顾进步。
- **语法模块交互优化**：当前语法条目只有"标记掌握"按钮，缺少例句展示和学习入口，体验与单词模块差距较大。
- **首页"今日复习"智能推荐**：根据SRS到期时间，首页提示"今天有X个词需要复习"，点击直达复习模式（与快捷入口区分开）。
- **学习卡片的SRS按钮可访问性**：当前四个按钮（忘记/困难/一般/简单）在深色模式下对比度偏低，建议微调配色或加大字号。
- **继续学习功能扩展**：当前只保存最近的单次学习进度。未来可扩展为"多个学习会话"（比如同时学第3课和收藏列表），让用户自由选择恢复哪个。

